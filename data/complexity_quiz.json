{
  "_comment": [
    "Question bank for the Complexity Quiz (site/pages/lc-complexity-quiz.html).",
    "",
    "Each entry shows a Python snippet and asks for its time and space complexity.",
    "Fields:",
    "  id         unique slug; the quiz keys its per-question state off it",
    "  lc         LeetCode number, or null for a pure algorithm / Python drill",
    "  title      required only when lc is null — otherwise README.md supplies it",
    "  difficulty required only when lc is null — otherwise README.md supplies it",
    "  topic      the bucket shown in the score breakdown and the topic filter",
    "  vars       what each symbol in the answer means; shown above the inputs",
    "  note       the ground rule for this question (what counts as space, etc.)",
    "  code       the snippet, one array element per line",
    "  time/space the canonical answer",
    "  accept     other answers graded correct — for genuinely debatable ones only",
    "  why        the explanation shown after answering",
    "  trap       the wrong answer people actually give, and why it is wrong",
    "",
    "site/build-quiz.js validates every entry (unique id, README-known lc, parseable",
    "answers) and fails the build rather than shipping a question nobody can score."
  ],

  "questions": [
    {
      "id": "two-sum-hash",
      "lc": 1,
      "topic": "Arrays & Hashing",
      "vars": "n = len(nums)",
      "code": [
        "def twoSum(nums, target):",
        "    seen = {}",
        "    for i, x in enumerate(nums):",
        "        if target - x in seen:",
        "            return [seen[target - x], i]",
        "        seen[x] = i",
        "    return []"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "One pass, and every dict lookup and insert is O(1) on average. The dict can end up holding all n values.",
      "trap": "`target - x in seen` is a hash lookup, not a scan — it does not make the loop quadratic. That is what separates this from the brute-force O(n^2) version."
    },
    {
      "id": "best-time-stock",
      "lc": 121,
      "topic": "Arrays & Hashing",
      "vars": "n = len(prices)",
      "code": [
        "def maxProfit(prices):",
        "    cheapest = float(\"inf\")",
        "    profit = 0",
        "    for p in prices:",
        "        cheapest = min(cheapest, p)",
        "        profit = max(profit, p - cheapest)",
        "    return profit"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "One pass over the prices, carrying two scalars. Nothing grows with the input."
    },
    {
      "id": "binary-search-iterative",
      "lc": null,
      "title": "Iterative binary search",
      "difficulty": "Easy",
      "topic": "Binary Search",
      "vars": "n = len(nums)",
      "code": [
        "def search(nums, target):",
        "    lo, hi = 0, len(nums) - 1",
        "    while lo <= hi:",
        "        mid = (lo + hi) // 2",
        "        if nums[mid] == target:",
        "            return mid",
        "        if nums[mid] < target:",
        "            lo = mid + 1",
        "        else:",
        "            hi = mid - 1",
        "    return -1"
      ],
      "time": "O(log n)",
      "space": "O(1)",
      "why": "The search window halves every iteration, so it survives log2(n) rounds. Three integers is the whole state."
    },
    {
      "id": "binary-search-slicing",
      "lc": null,
      "title": "Recursive binary search — with slicing",
      "difficulty": "Medium",
      "topic": "Python Pitfalls",
      "vars": "n = len(nums)",
      "code": [
        "def contains(nums, target):",
        "    if not nums:",
        "        return False",
        "    mid = len(nums) // 2",
        "    if nums[mid] == target:",
        "        return True",
        "    if nums[mid] < target:",
        "        return contains(nums[mid + 1:], target)",
        "    return contains(nums[:mid], target)"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "`nums[:mid]` copies the half it keeps. The copies cost n/2 + n/4 + ... = O(n), which swamps the log n comparisons; the live slices cost O(n) memory too.",
      "trap": "This looks like textbook O(log n) / O(1) binary search. Passing `lo`/`hi` indices instead of slices is what actually gets you there — this is the single most common way a correct algorithm loses its complexity in Python."
    },
    {
      "id": "three-sum",
      "lc": 15,
      "topic": "Two Pointers & Sliding Window",
      "vars": "n = len(nums)",
      "note": "Ignore the space held by the result list.",
      "code": [
        "def threeSum(nums):",
        "    nums.sort()",
        "    res = []",
        "    for i in range(len(nums) - 2):",
        "        if i > 0 and nums[i] == nums[i - 1]:",
        "            continue",
        "        lo, hi = i + 1, len(nums) - 1",
        "        while lo < hi:",
        "            s = nums[i] + nums[lo] + nums[hi]",
        "            if s < 0:",
        "                lo += 1",
        "            elif s > 0:",
        "                hi -= 1",
        "            else:",
        "                res.append([nums[i], nums[lo], nums[hi]])",
        "                while lo < hi and nums[lo] == nums[lo + 1]:",
        "                    lo += 1",
        "                lo, hi = lo + 1, hi - 1",
        "    return res"
      ],
      "time": "O(n^2)",
      "space": "O(n)",
      "accept": { "space": ["O(1)", "O(log n)"] },
      "why": "The outer loop is n and each two-pointer sweep is O(n), so O(n^2) — which dominates the O(n log n) sort. Space is whatever the sort needs: Timsort is O(n), an in-place sort would be O(log n), and the pointers themselves are O(1).",
      "trap": "The nested `while lo < hi` inside the `else` looks like a third level of nesting, but `lo` and `hi` only ever move toward each other, so the whole inner block is one linear sweep."
    },
    {
      "id": "valid-parentheses",
      "lc": 20,
      "topic": "Stack",
      "vars": "n = len(s)",
      "code": [
        "def isValid(s):",
        "    pairs = {\")\": \"(\", \"]\": \"[\", \"}\": \"{\"}",
        "    stack = []",
        "    for ch in s:",
        "        if ch in pairs:",
        "            if not stack or stack.pop() != pairs[ch]:",
        "                return False",
        "        else:",
        "            stack.append(ch)",
        "    return not stack"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "Each character is pushed and popped at most once. An all-opening string like \"((((\" puts every character on the stack."
    },
    {
      "id": "reverse-linked-list",
      "lc": 206,
      "topic": "Linked List",
      "vars": "n = number of nodes",
      "code": [
        "def reverseList(head):",
        "    prev = None",
        "    while head:",
        "        nxt = head.next",
        "        head.next = prev",
        "        prev = head",
        "        head = nxt",
        "    return prev"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Pointers are rewired in place — three references, whatever the list length.",
      "trap": "The equally common recursive version is O(n) space: it puts one stack frame per node."
    },
    {
      "id": "linked-list-cycle",
      "lc": 141,
      "topic": "Linked List",
      "vars": "n = number of nodes",
      "code": [
        "def hasCycle(head):",
        "    slow = fast = head",
        "    while fast and fast.next:",
        "        slow = slow.next",
        "        fast = fast.next.next",
        "        if slow is fast:",
        "            return True",
        "    return False"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Once both pointers are inside the cycle, the gap closes by one node per step, so they meet within n steps. Two pointers is the entire state.",
      "trap": "The `seen = set()` solution is the same O(n) time but O(n) space — Floyd's tortoise and hare is what buys you the constant space."
    },
    {
      "id": "merge-two-lists",
      "lc": 21,
      "topic": "Linked List",
      "vars": "n, m = lengths of the two lists",
      "code": [
        "def mergeTwoLists(l1, l2):",
        "    dummy = tail = ListNode()",
        "    while l1 and l2:",
        "        if l1.val <= l2.val:",
        "            tail.next, l1 = l1, l1.next",
        "        else:",
        "            tail.next, l2 = l2, l2.next",
        "        tail = tail.next",
        "    tail.next = l1 or l2",
        "    return dummy.next"
      ],
      "time": "O(n + m)",
      "space": "O(1)",
      "why": "Every node is visited once and spliced in place — no new nodes are allocated, only the dummy head."
    },
    {
      "id": "kadane",
      "lc": 53,
      "topic": "Dynamic Programming",
      "vars": "n = len(nums)",
      "code": [
        "def maxSubArray(nums):",
        "    best = cur = nums[0]",
        "    for i in range(1, len(nums)):",
        "        cur = max(nums[i], cur + nums[i])",
        "        best = max(best, cur)",
        "    return best"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Kadane's algorithm is a one-dimensional DP whose table has been collapsed to a single rolling value.",
      "trap": "Writing the loop as `for x in nums[1:]` would quietly make it O(n) space — the slice copies the array."
    },
    {
      "id": "longest-substring-no-repeat",
      "lc": 3,
      "topic": "Two Pointers & Sliding Window",
      "vars": "n = len(s), k = size of the character set",
      "code": [
        "def lengthOfLongestSubstring(s):",
        "    seen = {}",
        "    left = 0",
        "    best = 0",
        "    for right, ch in enumerate(s):",
        "        if ch in seen and seen[ch] >= left:",
        "            left = seen[ch] + 1",
        "        seen[ch] = right",
        "        best = max(best, right - left + 1)",
        "    return best"
      ],
      "time": "O(n)",
      "space": "O(k)",
      "accept": { "space": ["O(n)"] },
      "why": "`right` advances once per character and `left` never moves backwards, so it is one pass. The dict holds one entry per distinct character — bounded by the alphabet, and by n in the worst case.",
      "trap": "This window never re-scans: `left` jumps straight past the duplicate rather than walking. The naive shrink-one-at-a-time variant is still O(n) amortised, but the version that restarts the scan is O(n^2)."
    },
    {
      "id": "num-islands-dfs",
      "lc": 200,
      "topic": "Graphs",
      "vars": "m, n = grid dimensions",
      "code": [
        "def numIslands(grid):",
        "    m, n = len(grid), len(grid[0])",
        "    count = 0",
        "",
        "    def sink(i, j):",
        "        if 0 <= i < m and 0 <= j < n and grid[i][j] == \"1\":",
        "            grid[i][j] = \"0\"",
        "            sink(i + 1, j)",
        "            sink(i - 1, j)",
        "            sink(i, j + 1)",
        "            sink(i, j - 1)",
        "",
        "    for i in range(m):",
        "        for j in range(n):",
        "            if grid[i][j] == \"1\":",
        "                count += 1",
        "                sink(i, j)",
        "    return count"
      ],
      "time": "O(m * n)",
      "space": "O(m * n)",
      "why": "Each cell is sunk at most once, so the total DFS work is linear in the number of cells. Space is the recursion stack, and a snake-shaped island makes it as deep as the grid is large.",
      "trap": "Answering O(1) space because the grid is modified in place misses the call stack — the usual follow-up is \"what if the grid is 10^6 cells?\", which is asking for the iterative BFS."
    },
    {
      "id": "rotting-oranges",
      "lc": 994,
      "topic": "Graphs",
      "vars": "m, n = grid dimensions",
      "code": [
        "def orangesRotting(grid):",
        "    m, n = len(grid), len(grid[0])",
        "    q = deque((i, j) for i in range(m) for j in range(n) if grid[i][j] == 2)",
        "    fresh = sum(row.count(1) for row in grid)",
        "    minutes = 0",
        "    while q and fresh:",
        "        for _ in range(len(q)):",
        "            i, j = q.popleft()",
        "            for x, y in ((i+1, j), (i-1, j), (i, j+1), (i, j-1)):",
        "                if 0 <= x < m and 0 <= y < n and grid[x][y] == 1:",
        "                    grid[x][y] = 2",
        "                    fresh -= 1",
        "                    q.append((x, y))",
        "        minutes += 1",
        "    return -1 if fresh else minutes"
      ],
      "time": "O(m * n)",
      "space": "O(m * n)",
      "why": "Multi-source BFS enqueues each cell at most once and does O(1) work per cell. The queue can hold most of the grid when everything rots at the same time.",
      "trap": "The `for _ in range(len(q))` inner loop is the level marker, not extra work — it partitions the same O(m*n) dequeues into rounds."
    },
    {
      "id": "course-schedule",
      "lc": 207,
      "topic": "Graphs",
      "vars": "V = numCourses, E = len(prerequisites)",
      "code": [
        "def canFinish(numCourses, prerequisites):",
        "    graph = [[] for _ in range(numCourses)]",
        "    indeg = [0] * numCourses",
        "    for a, b in prerequisites:",
        "        graph[b].append(a)",
        "        indeg[a] += 1",
        "    q = deque(c for c in range(numCourses) if indeg[c] == 0)",
        "    seen = 0",
        "    while q:",
        "        c = q.popleft()",
        "        seen += 1",
        "        for nxt in graph[c]:",
        "            indeg[nxt] -= 1",
        "            if indeg[nxt] == 0:",
        "                q.append(nxt)",
        "    return seen == numCourses"
      ],
      "time": "O(V + E)",
      "space": "O(V + E)",
      "why": "Kahn's algorithm dequeues each node once and walks each edge once. The adjacency list is O(V + E); the in-degree array and queue are O(V).",
      "trap": "The nested `for nxt in graph[c]` is not a second factor of V — summed over all nodes it visits each edge exactly once, which is the E term."
    },
    {
      "id": "max-depth-tree",
      "lc": 104,
      "topic": "Trees",
      "vars": "n = number of nodes, h = tree height",
      "code": [
        "def maxDepth(root):",
        "    if not root:",
        "        return 0",
        "    return 1 + max(maxDepth(root.left), maxDepth(root.right))"
      ],
      "time": "O(n)",
      "space": "O(h)",
      "accept": { "space": ["O(n)"] },
      "why": "Every node is visited once. The stack holds one frame per level of the current path — O(log n) for a balanced tree, O(n) for a skewed one, so O(h) either way.",
      "trap": "\"O(log n) space\" is only true if the tree is balanced, and the problem never promises that. Say O(h) and then say what h is in the worst case."
    },
    {
      "id": "level-order-traversal",
      "lc": 102,
      "topic": "Trees",
      "vars": "n = number of nodes",
      "code": [
        "def levelOrder(root):",
        "    if not root:",
        "        return []",
        "    out, level = [], [root]",
        "    while level:",
        "        out.append([node.val for node in level])",
        "        level = [kid for node in level for kid in (node.left, node.right) if kid]",
        "    return out"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "Each node lands in exactly one level list. The widest level of a balanced tree holds about n/2 nodes, so the frontier alone is O(n)."
    },
    {
      "id": "top-k-frequent",
      "lc": 347,
      "topic": "Heap",
      "vars": "n = len(nums), k = the k asked for",
      "code": [
        "def topKFrequent(nums, k):",
        "    freq = Counter(nums)",
        "    heap = []",
        "    for val, cnt in freq.items():",
        "        heapq.heappush(heap, (cnt, val))",
        "        if len(heap) > k:",
        "            heapq.heappop(heap)",
        "    return [val for cnt, val in heap]"
      ],
      "time": "O(n log k)",
      "space": "O(n)",
      "why": "Counting is O(n); the heap is capped at k, so each of the (at most n) pushes costs log k. Space is dominated by the Counter, which can hold n distinct values.",
      "trap": "Capping the heap at k is the whole point — `heapq.nlargest(k, freq)` is the same idea. Pushing everything first would be O(n log n)."
    },
    {
      "id": "merge-k-lists",
      "lc": 23,
      "topic": "Heap",
      "vars": "n = total nodes across all lists, k = len(lists)",
      "code": [
        "def mergeKLists(lists):",
        "    heap = [(node.val, i, node) for i, node in enumerate(lists) if node]",
        "    heapq.heapify(heap)",
        "    dummy = tail = ListNode()",
        "    while heap:",
        "        _, i, node = heapq.heappop(heap)",
        "        tail.next = node",
        "        tail = node",
        "        if node.next:",
        "            heapq.heappush(heap, (node.next.val, i, node.next))",
        "    return dummy.next"
      ],
      "time": "O(n log k)",
      "space": "O(k)",
      "why": "Every node is pushed and popped once, and the heap never holds more than one node per list, so each operation is log k.",
      "trap": "Merging the lists pairwise down the array instead is O(n * k) — the heap is what turns the k factor into log k."
    },
    {
      "id": "kth-largest-heap",
      "lc": 215,
      "topic": "Heap",
      "vars": "n = len(nums), k = the k asked for",
      "code": [
        "def findKthLargest(nums, k):",
        "    heap = []",
        "    for x in nums:",
        "        heapq.heappush(heap, x)",
        "        if len(heap) > k:",
        "            heapq.heappop(heap)",
        "    return heap[0]"
      ],
      "time": "O(n log k)",
      "space": "O(k)",
      "why": "A min-heap of size k: n pushes at log k each, and the heap holds exactly k elements once it fills.",
      "trap": "Quickselect does the same job in O(n) average time and O(1) space — that is the follow-up this question is fishing for."
    },
    {
      "id": "permutations",
      "lc": 46,
      "topic": "Backtracking",
      "vars": "n = len(nums)",
      "note": "Count the recursion and scratch state only — the `res` list is the output.",
      "code": [
        "def permute(nums):",
        "    res = []",
        "",
        "    def backtrack(start):",
        "        if start == len(nums):",
        "            res.append(nums[:])",
        "            return",
        "        for i in range(start, len(nums)):",
        "            nums[start], nums[i] = nums[i], nums[start]",
        "            backtrack(start + 1)",
        "            nums[start], nums[i] = nums[i], nums[start]",
        "",
        "    backtrack(0)",
        "    return res"
      ],
      "time": "O(n * n!)",
      "space": "O(n)",
      "why": "There are n! permutations and copying each one out costs n, so O(n * n!). Auxiliary space is just the recursion stack, n frames deep, because the swaps mutate `nums` in place.",
      "trap": "O(n!) time undercounts: producing a permutation is not free, the `nums[:]` copy is O(n)."
    },
    {
      "id": "subsets",
      "lc": 78,
      "topic": "Backtracking",
      "vars": "n = len(nums)",
      "note": "Here the output counts — `res` is the only thing that grows.",
      "code": [
        "def subsets(nums):",
        "    res = [[]]",
        "    for x in nums:",
        "        res += [cur + [x] for cur in res]",
        "    return res"
      ],
      "time": "O(n * 2^n)",
      "space": "O(n * 2^n)",
      "why": "The result doubles on every one of the n iterations, giving 2^n subsets. Building each one costs its length, and the subsets average n/2 elements — so both the work and the memory are O(n * 2^n).",
      "trap": "O(2^n) counts the subsets but not their contents. Any answer that enumerates variable-length outputs needs the length factor."
    },
    {
      "id": "coin-change-dp",
      "lc": 322,
      "topic": "Dynamic Programming",
      "vars": "n = len(coins), a = amount",
      "code": [
        "def coinChange(coins, amount):",
        "    dp = [0] + [float(\"inf\")] * amount",
        "    for a in range(1, amount + 1):",
        "        for c in coins:",
        "            if c <= a:",
        "                dp[a] = min(dp[a], dp[a - c] + 1)",
        "    return -1 if dp[amount] == float(\"inf\") else dp[amount]"
      ],
      "time": "O(n * a)",
      "space": "O(a)",
      "why": "One table cell per amount, each filled by trying all n coins. The table is the only storage.",
      "trap": "This is pseudo-polynomial: `a` is the *value* of the input, not its length, so doubling the number of digits in `amount` squares the work. Interviewers like to push on that."
    },
    {
      "id": "climbing-stairs",
      "lc": 70,
      "topic": "Dynamic Programming",
      "vars": "n = the number of steps",
      "code": [
        "def climbStairs(n):",
        "    a, b = 1, 1",
        "    for _ in range(n - 1):",
        "        a, b = b, a + b",
        "    return b"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "The DP array has been rolled down to the two values the recurrence actually reads.",
      "trap": "Treating the big-integer additions as O(1) is the usual simplification, and it is the answer an interviewer wants — but the numbers do grow to n bits."
    },
    {
      "id": "fib-naive",
      "lc": 509,
      "topic": "Complexity Drills",
      "vars": "n = the input to fib",
      "code": [
        "def fib(n):",
        "    if n <= 1:",
        "        return n",
        "    return fib(n - 1) + fib(n - 2)"
      ],
      "time": "O(2^n)",
      "space": "O(n)",
      "why": "Each call branches in two and the recursion tree is roughly n deep, so about 2^n nodes (tightly, phi^n). But only one root-to-leaf path is live at a time, so the stack is n frames.",
      "trap": "Answering O(2^n) space confuses the size of the recursion tree with the depth of the stack. The tree is explored depth-first — the calls do not coexist."
    },
    {
      "id": "house-robber",
      "lc": 198,
      "topic": "Dynamic Programming",
      "vars": "n = len(nums)",
      "code": [
        "def rob(nums):",
        "    prev = cur = 0",
        "    for x in nums:",
        "        prev, cur = cur, max(cur, prev + x)",
        "    return cur"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "One pass, and the recurrence only ever looks two cells back — so two variables replace the table."
    },
    {
      "id": "lcs-dp",
      "lc": 1143,
      "topic": "Dynamic Programming",
      "vars": "m, n = the two string lengths",
      "code": [
        "def longestCommonSubsequence(text1, text2):",
        "    m, n = len(text1), len(text2)",
        "    dp = [[0] * (n + 1) for _ in range(m + 1)]",
        "    for i in range(1, m + 1):",
        "        for j in range(1, n + 1):",
        "            if text1[i - 1] == text2[j - 1]:",
        "                dp[i][j] = dp[i - 1][j - 1] + 1",
        "            else:",
        "                dp[i][j] = max(dp[i - 1][j], dp[i][j - 1])",
        "    return dp[m][n]"
      ],
      "time": "O(m * n)",
      "space": "O(m * n)",
      "why": "Every cell of the (m+1) x (n+1) table is filled once with O(1) work.",
      "trap": "Space drops to O(n) if you keep only the previous row — worth saying out loud, since each cell reads only the row above and the cell to its left."
    },
    {
      "id": "lis-dp",
      "lc": 300,
      "topic": "Dynamic Programming",
      "vars": "n = len(nums)",
      "code": [
        "def lengthOfLIS(nums):",
        "    dp = [1] * len(nums)",
        "    for i in range(len(nums)):",
        "        for j in range(i):",
        "            if nums[j] < nums[i]:",
        "                dp[i] = max(dp[i], dp[j] + 1)",
        "    return max(dp)"
      ],
      "time": "O(n^2)",
      "space": "O(n)",
      "why": "The inner loop runs 0 + 1 + ... + (n-1) = n(n-1)/2 times, which is O(n^2). One DP entry per index.",
      "trap": "`for j in range(i)` shrinking to nothing at i=0 does not save an order — the triangular sum is still quadratic."
    },
    {
      "id": "lis-patience",
      "lc": 300,
      "topic": "Binary Search",
      "vars": "n = len(nums)",
      "code": [
        "def lengthOfLIS(nums):",
        "    tails = []",
        "    for x in nums:",
        "        i = bisect_left(tails, x)",
        "        if i == len(tails):",
        "            tails.append(x)",
        "        else:",
        "            tails[i] = x",
        "    return len(tails)"
      ],
      "time": "O(n log n)",
      "space": "O(n)",
      "why": "One binary search per element over a list that never exceeds n entries. `tails` holds the smallest possible tail for each subsequence length.",
      "trap": "`tails` is not the increasing subsequence — only its length is meaningful. The complexity question and the correctness question both hinge on that."
    },
    {
      "id": "unique-paths-rolling",
      "lc": 62,
      "topic": "Dynamic Programming",
      "vars": "m, n = grid dimensions",
      "code": [
        "def uniquePaths(m, n):",
        "    row = [1] * n",
        "    for _ in range(m - 1):",
        "        for j in range(1, n):",
        "            row[j] += row[j - 1]",
        "    return row[-1]"
      ],
      "time": "O(m * n)",
      "space": "O(n)",
      "why": "Still one unit of work per grid cell, but only one row is ever stored — each cell needs the value above it (the old `row[j]`) and the one to its left (the already-updated `row[j-1]`).",
      "trap": "O(m * n) space is right for the two-dimensional table, not for this rolling one. Spotting that the table collapses to a row is exactly what the follow-up asks for."
    },
    {
      "id": "trapping-rain-water",
      "lc": 42,
      "topic": "Two Pointers & Sliding Window",
      "vars": "n = len(height)",
      "code": [
        "def trap(height):",
        "    lo, hi = 0, len(height) - 1",
        "    lmax = rmax = total = 0",
        "    while lo < hi:",
        "        if height[lo] < height[hi]:",
        "            lmax = max(lmax, height[lo])",
        "            total += lmax - height[lo]",
        "            lo += 1",
        "        else:",
        "            rmax = max(rmax, height[hi])",
        "            total += rmax - height[hi]",
        "            hi -= 1",
        "    return total"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "The two pointers close on each other, so the loop runs n times total, carrying four scalars.",
      "trap": "The prefix/suffix-max array version is the same O(n) time but O(n) space. The two-pointer trick is precisely what removes the arrays."
    },
    {
      "id": "daily-temperatures",
      "lc": 739,
      "topic": "Stack",
      "vars": "n = len(temperatures)",
      "code": [
        "def dailyTemperatures(temperatures):",
        "    res = [0] * len(temperatures)",
        "    stack = []",
        "    for i, t in enumerate(temperatures):",
        "        while stack and temperatures[stack[-1]] < t:",
        "            j = stack.pop()",
        "            res[j] = i - j",
        "        stack.append(i)",
        "    return res"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "Amortised: every index is pushed exactly once and popped at most once, so the inner `while` does at most n pops across the whole run.",
      "trap": "A loop inside a loop is not automatically O(n^2). This is the monotonic-stack argument — count total pushes and pops, not the shape of the nesting."
    },
    {
      "id": "min-stack",
      "lc": 155,
      "topic": "Design",
      "vars": "n = items currently on the stack",
      "note": "Time is for a single push / pop / getMin call.",
      "code": [
        "class MinStack:",
        "    def __init__(self):",
        "        self.stack = []",
        "",
        "    def push(self, val):",
        "        cur_min = min(val, self.stack[-1][1]) if self.stack else val",
        "        self.stack.append((val, cur_min))",
        "",
        "    def pop(self):",
        "        self.stack.pop()",
        "",
        "    def getMin(self):",
        "        return self.stack[-1][1]"
      ],
      "time": "O(1)",
      "space": "O(n)",
      "why": "Each entry carries the minimum of everything below it, so getMin is a peek. The price is a second value stored per element.",
      "trap": "`min(...)` here compares two values, not the whole stack — reading it as a scan is what makes people answer O(n)."
    },
    {
      "id": "lru-cache",
      "lc": 146,
      "topic": "Design",
      "vars": "c = capacity",
      "note": "Time is for a single get / put call.",
      "code": [
        "class LRUCache:",
        "    def __init__(self, capacity):",
        "        self.cache = OrderedDict()",
        "        self.capacity = capacity",
        "",
        "    def get(self, key):",
        "        if key not in self.cache:",
        "            return -1",
        "        self.cache.move_to_end(key)",
        "        return self.cache[key]",
        "",
        "    def put(self, key, value):",
        "        if key in self.cache:",
        "            self.cache.move_to_end(key)",
        "        self.cache[key] = value",
        "        if len(self.cache) > self.capacity:",
        "            self.cache.popitem(last=False)"
      ],
      "time": "O(1)",
      "space": "O(c)",
      "why": "OrderedDict is a hash map over a doubly linked list, so `move_to_end` and `popitem(last=False)` are pointer surgery, not a re-sort. The map never exceeds the capacity.",
      "trap": "`move_to_end` sounds like it shifts elements. It relinks one node — this is the whole reason the hash-map-plus-linked-list design is the expected answer."
    },
    {
      "id": "trie-insert",
      "lc": 208,
      "topic": "Tries",
      "vars": "L = len(word)",
      "note": "Time is for a single insert / search call.",
      "code": [
        "class Trie:",
        "    def __init__(self):",
        "        self.root = {}",
        "",
        "    def insert(self, word):",
        "        node = self.root",
        "        for ch in word:",
        "            node = node.setdefault(ch, {})",
        "        node[\"#\"] = True",
        "",
        "    def search(self, word):",
        "        node = self.root",
        "        for ch in word:",
        "            if ch not in node:",
        "                return False",
        "            node = node[ch]",
        "        return \"#\" in node"
      ],
      "time": "O(L)",
      "space": "O(L)",
      "why": "One dict hop per character, independent of how many words the trie already holds — that independence is the whole reason to build one. A fresh insert adds at most L nodes.",
      "trap": "Search does not depend on the number of stored words, so no `n` belongs in either answer."
    },
    {
      "id": "number-of-provinces",
      "lc": 547,
      "topic": "Graphs",
      "vars": "n = number of nodes (the matrix is n x n)",
      "code": [
        "def findCircleNum(isConnected):",
        "    n = len(isConnected)",
        "    seen = set()",
        "",
        "    def dfs(i):",
        "        for j in range(n):",
        "            if isConnected[i][j] and j not in seen:",
        "                seen.add(j)",
        "                dfs(j)",
        "",
        "    count = 0",
        "    for i in range(n):",
        "        if i not in seen:",
        "            seen.add(i)",
        "            dfs(i)",
        "            count += 1",
        "    return count"
      ],
      "time": "O(n^2)",
      "space": "O(n)",
      "why": "Each node is visited once, and visiting it scans its whole matrix row — n rows of n entries. Space is the `seen` set plus a recursion stack that is at most n deep.",
      "trap": "This is the O(V + E) DFS bound in disguise: an adjacency *matrix* makes E = n^2 whether or not the edges exist."
    },
    {
      "id": "dijkstra-network-delay",
      "lc": 743,
      "topic": "Graphs",
      "vars": "V = n nodes, E = len(times)",
      "code": [
        "def networkDelayTime(times, n, k):",
        "    graph = defaultdict(list)",
        "    for u, v, w in times:",
        "        graph[u].append((v, w))",
        "    dist = {}",
        "    heap = [(0, k)]",
        "    while heap:",
        "        d, u = heapq.heappop(heap)",
        "        if u in dist:",
        "            continue",
        "        dist[u] = d",
        "        for v, w in graph[u]:",
        "            if v not in dist:",
        "                heapq.heappush(heap, (d + w, v))",
        "    return max(dist.values()) if len(dist) == n else -1"
      ],
      "time": "O(E log V)",
      "space": "O(V + E)",
      "accept": { "time": ["O(E log E)"] },
      "why": "Lazy Dijkstra pushes once per edge, so the heap holds up to E entries and each operation is log E, which is O(log V) for a simple graph. Space is the adjacency list plus the heap.",
      "trap": "The `if u in dist: continue` line is what makes this correct without a decrease-key: stale heap entries are allowed in and skipped on the way out."
    },
    {
      "id": "search-rotated",
      "lc": 33,
      "topic": "Binary Search",
      "vars": "n = len(nums)",
      "code": [
        "def search(nums, target):",
        "    lo, hi = 0, len(nums) - 1",
        "    while lo <= hi:",
        "        mid = (lo + hi) // 2",
        "        if nums[mid] == target:",
        "            return mid",
        "        if nums[lo] <= nums[mid]:",
        "            if nums[lo] <= target < nums[mid]:",
        "                hi = mid - 1",
        "            else:",
        "                lo = mid + 1",
        "        else:",
        "            if nums[mid] < target <= nums[hi]:",
        "                lo = mid + 1",
        "            else:",
        "                hi = mid - 1",
        "    return -1"
      ],
      "time": "O(log n)",
      "space": "O(1)",
      "why": "The extra branching decides *which* half to keep; it still throws one half away each iteration.",
      "trap": "With duplicates allowed (LC 81) the `nums[lo] <= nums[mid]` test stops distinguishing the halves and the worst case degrades to O(n)."
    },
    {
      "id": "koko-bananas",
      "lc": 875,
      "topic": "Binary Search",
      "vars": "n = len(piles), m = max(piles)",
      "code": [
        "def minEatingSpeed(piles, h):",
        "    def hours(speed):",
        "        return sum((p + speed - 1) // speed for p in piles)",
        "",
        "    lo, hi = 1, max(piles)",
        "    while lo < hi:",
        "        mid = (lo + hi) // 2",
        "        if hours(mid) <= h:",
        "            hi = mid",
        "        else:",
        "            lo = mid + 1",
        "    return lo"
      ],
      "time": "O(n log m)",
      "space": "O(1)",
      "why": "Binary search on the *answer*: log m candidate speeds, and checking one costs a full O(n) pass over the piles. The generator in `sum` does not materialise a list.",
      "trap": "The log factor is over the value range, not the array length — the two are unrelated, and mixing them up is the classic slip on binary-search-on-answer problems."
    },
    {
      "id": "group-anagrams",
      "lc": 49,
      "topic": "Arrays & Hashing",
      "vars": "n = len(strs), k = length of the longest word",
      "code": [
        "def groupAnagrams(strs):",
        "    groups = defaultdict(list)",
        "    for s in strs:",
        "        groups[tuple(sorted(s))].append(s)",
        "    return list(groups.values())"
      ],
      "time": "O(n * k log k)",
      "space": "O(n * k)",
      "why": "Sorting each word is k log k, done n times. Every word plus its key is stored, so the map holds O(n * k) characters.",
      "trap": "The k factor is easy to drop. Counting characters into a 26-tuple key instead makes it O(n * k) time — a real improvement worth naming."
    },
    {
      "id": "valid-anagram-sort",
      "lc": 242,
      "topic": "Sorting",
      "vars": "n = len(s)",
      "code": [
        "def isAnagram(s, t):",
        "    return sorted(s) == sorted(t)"
      ],
      "time": "O(n log n)",
      "space": "O(n)",
      "why": "Two sorts dominate the linear comparison, and `sorted` builds two new lists of n characters.",
      "trap": "A one-liner is not automatically O(n). `sorted` on a string returns a list — both the sort and that list are real costs. `Counter(s) == Counter(t)` is the O(n) alternative."
    },
    {
      "id": "product-except-self",
      "lc": 238,
      "topic": "Arrays & Hashing",
      "vars": "n = len(nums)",
      "note": "The returned list is the output; do not count it as auxiliary space.",
      "code": [
        "def productExceptSelf(nums):",
        "    n = len(nums)",
        "    res = [1] * n",
        "    for i in range(1, n):",
        "        res[i] = res[i - 1] * nums[i - 1]",
        "    right = 1",
        "    for i in range(n - 1, -1, -1):",
        "        res[i] *= right",
        "        right *= nums[i]",
        "    return res"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Two sequential passes, not nested ones. The prefix products are staged inside the output array and the suffix product is carried in a single variable — no second array.",
      "trap": "Two loops in a row add, they do not multiply: O(n) + O(n) is O(n)."
    },
    {
      "id": "merge-sorted-array",
      "lc": 88,
      "topic": "Two Pointers & Sliding Window",
      "vars": "m, n = the two filled lengths",
      "code": [
        "def merge(nums1, m, nums2, n):",
        "    i, j, k = m - 1, n - 1, m + n - 1",
        "    while j >= 0:",
        "        if i >= 0 and nums1[i] > nums2[j]:",
        "            nums1[k] = nums1[i]",
        "            i -= 1",
        "        else:",
        "            nums1[k] = nums2[j]",
        "            j -= 1",
        "        k -= 1"
      ],
      "time": "O(m + n)",
      "space": "O(1)",
      "why": "Filling from the back means nothing is ever shifted: each of the m + n values is written exactly once, in place.",
      "trap": "Merging forwards would need either a copy (O(m + n) space) or a shift per insert (O(m * n) time). Writing backwards is what buys both."
    },
    {
      "id": "single-number-xor",
      "lc": 136,
      "topic": "Bit Manipulation",
      "vars": "n = len(nums)",
      "code": [
        "def singleNumber(nums):",
        "    out = 0",
        "    for x in nums:",
        "        out ^= x",
        "    return out"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "XOR is associative and self-cancelling, so the pairs annihilate and the loner survives — in one accumulator.",
      "trap": "The set-based solution is O(n) space. This problem is asked precisely to see whether you find the O(1)-space one."
    },
    {
      "id": "hamming-weight",
      "lc": 191,
      "topic": "Bit Manipulation",
      "vars": "n = the integer, b = its bit width (32 here)",
      "code": [
        "def hammingWeight(n):",
        "    count = 0",
        "    while n:",
        "        n &= n - 1",
        "        count += 1",
        "    return count"
      ],
      "time": "O(1)",
      "space": "O(1)",
      "accept": { "time": ["O(b)", "O(log n)"] },
      "why": "`n &= n - 1` clears the lowest set bit, so the loop runs once per set bit — at most 32 for a fixed-width integer, which is a constant. Stated in the input's bit width it is O(b), and b = log n.",
      "trap": "Calling it O(n) treats the *value* as the input size. For a fixed 32-bit word every bit trick is O(1); the honest general answer is O(b) over the word width."
    },
    {
      "id": "longest-palindrome-expand",
      "lc": 5,
      "topic": "Arrays & Hashing",
      "vars": "n = len(s)",
      "code": [
        "def longestPalindrome(s):",
        "    best = \"\"",
        "    for i in range(len(s)):",
        "        for lo, hi in ((i, i), (i, i + 1)):",
        "            while lo >= 0 and hi < len(s) and s[lo] == s[hi]:",
        "                lo, hi = lo - 1, hi + 1",
        "            if hi - lo - 1 > len(best):",
        "                best = s[lo + 1:hi]",
        "    return best"
      ],
      "time": "O(n^2)",
      "space": "O(n)",
      "accept": { "space": ["O(1)"] },
      "why": "n centres, each expanding up to n characters. The only storage is `best`, a slice of up to n characters — track indices instead and it becomes O(1).",
      "trap": "The two-entry tuple loop is a constant factor (odd and even centres), not a third n."
    },
    {
      "id": "k-closest-points",
      "lc": 973,
      "topic": "Heap",
      "vars": "n = len(points), k = the k asked for",
      "code": [
        "def kClosest(points, k):",
        "    heap = []",
        "    for x, y in points:",
        "        heapq.heappush(heap, (-(x * x + y * y), x, y))",
        "        if len(heap) > k:",
        "            heapq.heappop(heap)",
        "    return [[x, y] for _, x, y in heap]"
      ],
      "time": "O(n log k)",
      "space": "O(k)",
      "why": "A bounded max-heap of the k best so far: n pushes at log k each, k entries held.",
      "trap": "Sorting all the points is O(n log n) — worse whenever k is much smaller than n, which is the case the problem is built around."
    },
    {
      "id": "merge-intervals",
      "lc": 56,
      "topic": "Intervals",
      "vars": "n = len(intervals)",
      "code": [
        "def merge(intervals):",
        "    intervals.sort()",
        "    out = []",
        "    for start, end in intervals:",
        "        if out and start <= out[-1][1]:",
        "            out[-1][1] = max(out[-1][1], end)",
        "        else:",
        "            out.append([start, end])",
        "    return out"
      ],
      "time": "O(n log n)",
      "space": "O(n)",
      "why": "The sort dominates the single linear merge pass. Space is the output list plus Timsort's scratch — both O(n).",
      "trap": "Almost every interval problem is O(n log n) for exactly this reason: the sort, not the sweep, sets the bound."
    },
    {
      "id": "meeting-rooms-ii",
      "lc": 253,
      "topic": "Intervals",
      "vars": "n = len(intervals)",
      "code": [
        "def minMeetingRooms(intervals):",
        "    intervals.sort()",
        "    rooms = []",
        "    for start, end in intervals:",
        "        if rooms and rooms[0] <= start:",
        "            heapq.heapreplace(rooms, end)",
        "        else:",
        "            heapq.heappush(rooms, end)",
        "    return len(rooms)"
      ],
      "time": "O(n log n)",
      "space": "O(n)",
      "why": "Sort is n log n; then n heap operations at log n each — same order. The heap holds one end time per concurrent room, up to n.",
      "trap": "`rooms[0]` is the earliest ending meeting, a peek at the heap root: O(1), not a scan."
    },
    {
      "id": "merge-sort",
      "lc": 912,
      "topic": "Sorting",
      "vars": "n = len(nums)",
      "code": [
        "def sortArray(nums):",
        "    if len(nums) <= 1:",
        "        return nums",
        "    mid = len(nums) // 2",
        "    left = sortArray(nums[:mid])",
        "    right = sortArray(nums[mid:])",
        "    out = []",
        "    i = j = 0",
        "    while i < len(left) and j < len(right):",
        "        if left[i] <= right[j]:",
        "            out.append(left[i])",
        "            i += 1",
        "        else:",
        "            out.append(right[j])",
        "            j += 1",
        "    return out + left[i:] + right[j:]"
      ],
      "time": "O(n log n)",
      "space": "O(n)",
      "why": "T(n) = 2T(n/2) + O(n): log n levels of merging, O(n) work per level. Each level of the recursion holds O(n) of copied slices, and the stack is only log n deep.",
      "trap": "Unlike a recursive binary search that slices, the copying here does not change the order — merge sort already does O(n) work per level, so the slices are free in Big-O terms."
    },
    {
      "id": "word-search",
      "lc": 79,
      "topic": "Backtracking",
      "vars": "m, n = board dimensions, L = len(word)",
      "code": [
        "def exist(board, word):",
        "    m, n = len(board), len(board[0])",
        "",
        "    def dfs(i, j, k):",
        "        if k == len(word):",
        "            return True",
        "        if not (0 <= i < m and 0 <= j < n) or board[i][j] != word[k]:",
        "            return False",
        "        board[i][j] = \"#\"",
        "        found = (dfs(i + 1, j, k + 1) or dfs(i - 1, j, k + 1)",
        "                 or dfs(i, j + 1, k + 1) or dfs(i, j - 1, k + 1))",
        "        board[i][j] = word[k]",
        "        return found",
        "",
        "    return any(dfs(i, j, 0) for i in range(m) for j in range(n))"
      ],
      "time": "O(m * n * 4^L)",
      "space": "O(L)",
      "why": "Every cell can start a search, and each search branches four ways at each of L characters. Space is the recursion stack, L frames deep — the board is marked in place and restored.",
      "trap": "3^L is the tighter bound (you never walk straight back into the cell you came from), but 4^L is the answer usually expected. Either way the exponent is the *word* length, not the board size."
    },
    {
      "id": "median-data-stream",
      "lc": 295,
      "topic": "Heap",
      "vars": "n = numbers added so far",
      "note": "Time is for a single addNum call.",
      "code": [
        "class MedianFinder:",
        "    def __init__(self):",
        "        self.small = []   # max-heap, values negated",
        "        self.large = []   # min-heap",
        "",
        "    def addNum(self, num):",
        "        heapq.heappush(self.small, -num)",
        "        heapq.heappush(self.large, -heapq.heappop(self.small))",
        "        if len(self.large) > len(self.small):",
        "            heapq.heappush(self.small, -heapq.heappop(self.large))",
        "",
        "    def findMedian(self):",
        "        if len(self.small) > len(self.large):",
        "            return -self.small[0]",
        "        return (-self.small[0] + self.large[0]) / 2"
      ],
      "time": "O(log n)",
      "space": "O(n)",
      "why": "A bounded number of heap pushes and pops per call, each log n. Every number added is kept in one of the two heaps.",
      "trap": "The three heap operations are a constant factor, not a multiplier. And `findMedian` is O(1) — it only peeks at the two roots."
    },
    {
      "id": "largest-rectangle-histogram",
      "lc": 84,
      "topic": "Stack",
      "vars": "n = len(heights)",
      "code": [
        "def largestRectangleArea(heights):",
        "    heights.append(0)",
        "    stack = [-1]",
        "    best = 0",
        "    for i, h in enumerate(heights):",
        "        while stack[-1] != -1 and heights[stack[-1]] >= h:",
        "            height = heights[stack.pop()]",
        "            best = max(best, height * (i - stack[-1] - 1))",
        "        stack.append(i)",
        "    return best"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "The same amortised monotonic-stack argument: n pushes, at most n pops, O(1) work each. The stack can hold every index when the bars are strictly increasing.",
      "trap": "Being a Hard problem does not make it superlinear. The brute force is O(n^2); the stack is what removes the second factor."
    },
    {
      "id": "longest-consecutive",
      "lc": 128,
      "topic": "Arrays & Hashing",
      "vars": "n = len(nums)",
      "code": [
        "def longestConsecutive(nums):",
        "    s = set(nums)",
        "    best = 0",
        "    for x in s:",
        "        if x - 1 not in s:",
        "            length = 1",
        "            while x + length in s:",
        "                length += 1",
        "            best = max(best, length)",
        "    return best"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "The `if x - 1 not in s` guard means the inner walk only starts at a run's first element, so every value is walked exactly once across the whole loop.",
      "trap": "The nested `while` reads as O(n^2), and it would be without that guard. Drop the guard and each element of a long run re-walks the run."
    },
    {
      "id": "subarray-sum-k",
      "lc": 560,
      "topic": "Arrays & Hashing",
      "vars": "n = len(nums)",
      "code": [
        "def subarraySum(nums, k):",
        "    counts = defaultdict(int)",
        "    counts[0] = 1",
        "    total = 0",
        "    out = 0",
        "    for x in nums:",
        "        total += x",
        "        out += counts[total - k]",
        "        counts[total] += 1",
        "    return out"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "One pass, one dict lookup per element. The dict holds up to n distinct prefix sums.",
      "trap": "The counter must hold *how many times* each prefix sum occurred, not just whether it occurred — but that does not change either bound."
    },
    {
      "id": "min-window-substring",
      "lc": 76,
      "topic": "Two Pointers & Sliding Window",
      "vars": "n = len(s), m = len(t)",
      "code": [
        "def minWindow(s, t):",
        "    need = Counter(t)",
        "    missing = len(t)",
        "    best = \"\"",
        "    left = 0",
        "    for right, ch in enumerate(s, 1):",
        "        missing -= need[s[right - 1]] > 0",
        "        need[s[right - 1]] -= 1",
        "        if not missing:",
        "            while need[s[left]] < 0:",
        "                need[s[left]] += 1",
        "                left += 1",
        "            if not best or right - left < len(best):",
        "                best = s[left:right]",
        "            need[s[left]] += 1",
        "            missing += 1",
        "            left += 1",
        "    return best"
      ],
      "time": "O(n + m)",
      "space": "O(n)",
      "accept": { "time": ["O(n)"], "space": ["O(m)"] },
      "why": "`right` and `left` each traverse s once, so the window is linear despite the inner loop. Space is the counter (one entry per distinct character of t) plus `best`, which can be up to n characters.",
      "trap": "The `while need[s[left]] < 0` loop never rewinds `left`, so it is amortised O(n) over the whole run — not O(n) per outer step."
    },
    {
      "id": "string-concat-loop",
      "lc": null,
      "title": "Building a string with +=",
      "difficulty": "Easy",
      "topic": "Python Pitfalls",
      "vars": "n = len(chars)",
      "code": [
        "def build(chars):",
        "    out = \"\"",
        "    for ch in chars:",
        "        out += ch",
        "    return out"
      ],
      "time": "O(n^2)",
      "space": "O(n)",
      "why": "Python strings are immutable, so `out += ch` allocates a new string and copies the old one: 1 + 2 + ... + n character copies. `\"\".join(chars)` is the O(n) version.",
      "trap": "CPython has an in-place optimisation that can make this look linear when the string has one reference, but it is an implementation detail with no guarantee — the answer to give in an interview is O(n^2), plus the join."
    },
    {
      "id": "list-membership-loop",
      "lc": null,
      "title": "`in` on a list, inside a loop",
      "difficulty": "Easy",
      "topic": "Python Pitfalls",
      "vars": "n = len(a), m = len(b)",
      "code": [
        "def count_shared(a, b):",
        "    total = 0",
        "    for x in a:",
        "        if x in b:",
        "            total += 1",
        "    return total"
      ],
      "time": "O(n * m)",
      "space": "O(1)",
      "why": "`x in b` on a list is a linear scan, so the one-line body hides an m-step loop.",
      "trap": "The same `in` on a set or dict is O(1). `b = set(b)` before the loop turns this into O(n + m) time for O(m) space — the single highest-value one-line fix in Python interviews."
    },
    {
      "id": "pop-front-loop",
      "lc": null,
      "title": "Draining a list with pop(0)",
      "difficulty": "Easy",
      "topic": "Python Pitfalls",
      "vars": "n = len(items)",
      "code": [
        "def drain(items):",
        "    out = []",
        "    while items:",
        "        out.append(items.pop(0))",
        "    return out"
      ],
      "time": "O(n^2)",
      "space": "O(n)",
      "why": "`list.pop(0)` shifts every remaining element down one slot, so it is O(n) per call and O(n^2) over the drain.",
      "trap": "This is why BFS uses `collections.deque`: `popleft()` is O(1), while the same code on a list quietly makes the traversal quadratic. `list.pop()` from the end *is* O(1)."
    },
    {
      "id": "heapify-then-pop",
      "lc": null,
      "title": "heapify, then pop k times",
      "difficulty": "Medium",
      "topic": "Heap",
      "vars": "n = len(nums), k = how many to take",
      "code": [
        "def k_smallest(nums, k):",
        "    heapq.heapify(nums)",
        "    return [heapq.heappop(nums) for _ in range(k)]"
      ],
      "time": "O(n + k log n)",
      "space": "O(k)",
      "why": "`heapify` is O(n) — bottom-up sift-down, not n insertions — and each of the k pops is log n. The output list holds k items; the heap is `nums` itself, in place.",
      "trap": "Calling heapify O(n log n) is the classic overcount. The nodes near the leaves, which are most of them, sift down almost no distance, and the series sums to O(n)."
    },
    {
      "id": "triangular-nested-loop",
      "lc": null,
      "title": "Nested loop starting at i + 1",
      "difficulty": "Easy",
      "topic": "Complexity Drills",
      "vars": "n = len(nums)",
      "code": [
        "def count_zero_pairs(nums):",
        "    total = 0",
        "    for i in range(len(nums)):",
        "        for j in range(i + 1, len(nums)):",
        "            if nums[i] + nums[j] == 0:",
        "                total += 1",
        "    return total"
      ],
      "time": "O(n^2)",
      "space": "O(1)",
      "why": "The inner loop runs n-1, n-2, ..., 1 times: n(n-1)/2 iterations, which is O(n^2).",
      "trap": "\"It only does half the work\" is true and irrelevant — halving is a constant factor. Big-O does not see it."
    },
    {
      "id": "halving-work-loop",
      "lc": null,
      "title": "Loop whose work halves each round",
      "difficulty": "Medium",
      "topic": "Complexity Drills",
      "vars": "n = the input size",
      "code": [
        "def total_work(n):",
        "    total = 0",
        "    size = n",
        "    while size >= 1:",
        "        for _ in range(size):",
        "            total += 1",
        "        size //= 2",
        "    return total"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "n + n/2 + n/4 + ... < 2n. A geometric series with ratio 1/2 converges, so the whole thing is linear.",
      "trap": "O(n log n) is the reflex: log n rounds times \"a loop\". But the loop shrinks — only the *first* round costs n, and everything after it adds up to less than one more n."
    },
    {
      "id": "doubling-loop",
      "lc": null,
      "title": "Loop with a doubling counter",
      "difficulty": "Easy",
      "topic": "Complexity Drills",
      "vars": "n = the bound",
      "code": [
        "def rounds(n):",
        "    i = 1",
        "    count = 0",
        "    while i < n:",
        "        i *= 2",
        "        count += 1",
        "    return count"
      ],
      "time": "O(log n)",
      "space": "O(1)",
      "why": "i takes the values 1, 2, 4, 8, ... so it reaches n after log2(n) steps.",
      "trap": "Any loop whose variable is *multiplied* rather than incremented is logarithmic — the mirror image of the halving binary search."
    },
    {
      "id": "sort-inside-loop",
      "lc": null,
      "title": "Sorting inside a loop",
      "difficulty": "Medium",
      "topic": "Python Pitfalls",
      "vars": "n = len(nums)",
      "code": [
        "def running_medians(nums):",
        "    out = []",
        "    for i in range(1, len(nums) + 1):",
        "        window = sorted(nums[:i])",
        "        out.append(window[len(window) // 2])",
        "    return out"
      ],
      "time": "O(n^2 log n)",
      "space": "O(n)",
      "why": "n iterations, and iteration i slices (O(i)) then sorts (O(i log i)) — summing to O(n^2 log n). Only one window is alive at a time, so space is O(n).",
      "trap": "A `sorted()` call inside a loop is never free. The two-heap MedianFinder does this same job in O(log n) per element."
    },
    {
      "id": "contains-duplicate",
      "lc": 217,
      "topic": "Arrays & Hashing",
      "vars": "n = len(nums)",
      "code": [
        "def containsDuplicate(nums):",
        "    seen = set()",
        "    for x in nums:",
        "        if x in seen:",
        "            return True",
        "        seen.add(x)",
        "    return False"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "One pass, and `in` on a set is an O(1) average hash lookup. The set can end up holding every value, so the memory is linear.",
      "trap": "The O(1) space answer belongs to the other solution — sort first, then compare neighbours — and that one pays O(n log n) time for it. This version trades memory for the log factor."
    },
    {
      "id": "majority-element-boyer-moore",
      "lc": 169,
      "topic": "Arrays & Hashing",
      "vars": "n = len(nums)",
      "code": [
        "def majorityElement(nums):",
        "    count = 0",
        "    candidate = None",
        "    for x in nums:",
        "        if count == 0:",
        "            candidate = x",
        "        count += 1 if x == candidate else -1",
        "    return candidate"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "One pass carrying two scalars. Pairing every non-candidate off against a candidate cancels both, and a strict majority is what survives the cancelling.",
      "trap": "`Counter(nums).most_common(1)` is also O(n) time — but O(n) space. Sorting and taking the middle is O(n log n). Only Boyer-Moore is linear *and* constant."
    },
    {
      "id": "first-missing-positive",
      "lc": 41,
      "topic": "Arrays & Hashing",
      "vars": "n = len(nums)",
      "note": "The input array may be rewritten; count only extra space.",
      "code": [
        "def firstMissingPositive(nums):",
        "    n = len(nums)",
        "    for i in range(n):",
        "        while 1 <= nums[i] <= n and nums[nums[i] - 1] != nums[i]:",
        "            j = nums[i] - 1",
        "            nums[i], nums[j] = nums[j], nums[i]",
        "    for i in range(n):",
        "        if nums[i] != i + 1:",
        "            return i + 1",
        "    return n + 1"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Every swap drops one value into its final slot, and a settled value never moves again, so there are at most n swaps across the whole run. The array itself plays the part of the hash set.",
      "trap": "A `while` nested in a `for` reads as O(n^2). Count the swaps, not the loops: the inner loop's total iterations over the entire outer loop are bounded by n, which is what makes cyclic sort linear."
    },
    {
      "id": "pascals-triangle",
      "lc": 118,
      "topic": "Arrays & Hashing",
      "vars": "n = numRows",
      "note": "Count the returned triangle here — it is also the table the loop reads back.",
      "code": [
        "def generate(numRows):",
        "    res = []",
        "    for i in range(numRows):",
        "        row = [1] * (i + 1)",
        "        for j in range(1, i):",
        "            row[j] = res[i - 1][j - 1] + res[i - 1][j]",
        "        res.append(row)",
        "    return res"
      ],
      "time": "O(n^2)",
      "space": "O(n^2)",
      "why": "Row i holds i + 1 entries, and 1 + 2 + ... + n is n(n+1)/2 — quadratic in the number of rows, in cells written and cells kept alike.",
      "trap": "O(n) is the reflex because there is one outer iteration per row. The rows are not a constant size: the last one alone is n wide."
    },
    {
      "id": "valid-sudoku",
      "lc": 36,
      "topic": "Arrays & Hashing",
      "vars": "the board is always 9 x 9",
      "code": [
        "def isValidSudoku(board):",
        "    seen = set()",
        "    for i in range(9):",
        "        for j in range(9):",
        "            v = board[i][j]",
        "            if v == \".\":",
        "                continue",
        "            keys = ((\"row\", i, v), (\"col\", j, v), (\"box\", i // 3, j // 3, v))",
        "            for key in keys:",
        "                if key in seen:",
        "                    return False",
        "                seen.add(key)",
        "    return True"
      ],
      "time": "O(1)",
      "space": "O(1)",
      "why": "81 cells, three keys each. Nothing about the input can grow — a Sudoku board is 9 x 9 by definition — so both bounds are constants, however unlovely the constant.",
      "trap": "O(n^2) for the nested loop is the trained answer, and here it is wrong: there is no n. Complexity describes growth, and no dimension of this input varies. (On a general n x n board with sqrt(n) x sqrt(n) boxes it would be O(n^2) time and O(n^2) space.)"
    },
    {
      "id": "container-with-most-water",
      "lc": 11,
      "topic": "Two Pointers & Sliding Window",
      "vars": "n = len(height)",
      "code": [
        "def maxArea(height):",
        "    lo, hi = 0, len(height) - 1",
        "    best = 0",
        "    while lo < hi:",
        "        best = max(best, (hi - lo) * min(height[lo], height[hi]))",
        "        if height[lo] < height[hi]:",
        "            lo += 1",
        "        else:",
        "            hi -= 1",
        "    return best"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "The pointers only ever move toward each other, so the loop body runs at most n times whatever the heights are. Three scalars is the entire state.",
      "trap": "Trying every pair of lines is the O(n^2) version. Moving the *shorter* wall is what makes one pass sufficient: keeping it and narrowing the width can never beat what was just recorded."
    },
    {
      "id": "valid-palindrome-two-pointer",
      "lc": 125,
      "topic": "Two Pointers & Sliding Window",
      "vars": "n = len(s)",
      "code": [
        "def isPalindrome(s):",
        "    lo, hi = 0, len(s) - 1",
        "    while lo < hi:",
        "        while lo < hi and not s[lo].isalnum():",
        "            lo += 1",
        "        while lo < hi and not s[hi].isalnum():",
        "            hi -= 1",
        "        if s[lo].lower() != s[hi].lower():",
        "            return False",
        "        lo, hi = lo + 1, hi - 1",
        "    return True"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Three loops, but `lo` only ever rises and `hi` only ever falls, so between them they take n steps in total. No copy of the string is made.",
      "trap": "The one-liner — `t = \"\".join(c for c in s if c.isalnum()).lower()`, then `t == t[::-1]` — is the same O(n) time but O(n) space, because it builds two whole new strings. Nested `while`s are not automatically quadratic; monotone pointers are the tell."
    },
    {
      "id": "longest-repeating-replacement",
      "lc": 424,
      "topic": "Two Pointers & Sliding Window",
      "vars": "n = len(s); s is uppercase A-Z",
      "code": [
        "def characterReplacement(s, k):",
        "    count = {}",
        "    left = most = best = 0",
        "    for right, ch in enumerate(s):",
        "        count[ch] = count.get(ch, 0) + 1",
        "        most = max(most, count[ch])",
        "        if (right - left + 1) - most > k:",
        "            count[s[left]] -= 1",
        "            left += 1",
        "        best = max(best, right - left + 1)",
        "    return best"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "One step of `right` per character, and at most one step of `left` with it, so the window slides linearly. The dict holds one entry per distinct letter — 26 at most, a constant.",
      "trap": "`count` looks like it grows with the input, but the alphabet caps it, not n. That cap is also why a stale `most` is harmless: the window never shrinks, so an over-large `most` can only stop it growing."
    },
    {
      "id": "permutation-in-string",
      "lc": 567,
      "topic": "Two Pointers & Sliding Window",
      "vars": "m = len(s1), n = len(s2)",
      "code": [
        "def checkInclusion(s1, s2):",
        "    if len(s1) > len(s2):",
        "        return False",
        "    need, win = [0] * 26, [0] * 26",
        "    for i, ch in enumerate(s1):",
        "        need[ord(ch) - 97] += 1",
        "        win[ord(s2[i]) - 97] += 1",
        "    if need == win:",
        "        return True",
        "    for i in range(len(s1), len(s2)):",
        "        win[ord(s2[i]) - 97] += 1",
        "        win[ord(s2[i - len(s1)]) - 97] -= 1",
        "        if need == win:",
        "            return True",
        "    return False"
      ],
      "time": "O(m + n)",
      "space": "O(1)",
      "why": "Building `need` is O(m); the window then slides once across s2, each slide being two counter updates and a comparison of two fixed 26-slot lists.",
      "trap": "`need == win` inside the loop looks like the expensive part, and would be if those lists grew with the input. They do not — 26 comparisons is a constant — so this is O(m + n), not O(m * n)."
    },
    {
      "id": "min-size-subarray-sum",
      "lc": 209,
      "topic": "Two Pointers & Sliding Window",
      "vars": "n = len(nums)",
      "code": [
        "def minSubArrayLen(target, nums):",
        "    left = 0",
        "    total = 0",
        "    best = float(\"inf\")",
        "    for right, x in enumerate(nums):",
        "        total += x",
        "        while total >= target:",
        "            best = min(best, right - left + 1)",
        "            total -= nums[left]",
        "            left += 1",
        "    return 0 if best == float(\"inf\") else best"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "`right` adds each index once and `left` removes it at most once, so the inner loop runs at most n times across the whole outer loop — 2n pointer moves in total.",
      "trap": "The nested `while` invites O(n^2). Amortise instead: count how many times the inner body can *ever* run, not how many times it might run on one iteration. (Binary-searching prefix sums also solves this, at O(n log n) — strictly worse.)"
    },
    {
      "id": "move-zeroes",
      "lc": 283,
      "topic": "Two Pointers & Sliding Window",
      "vars": "n = len(nums)",
      "code": [
        "def moveZeroes(nums):",
        "    slow = 0",
        "    for fast in range(len(nums)):",
        "        if nums[fast] != 0:",
        "            nums[slow], nums[fast] = nums[fast], nums[slow]",
        "            slow += 1"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "One pass, one swap per non-zero, all in place.",
      "trap": "The tempting `while 0 in nums: nums.remove(0); nums.append(0)` is O(n^2) — every `remove` shifts the whole tail down a slot, and `in` rescans from the front."
    },
    {
      "id": "sliding-window-maximum",
      "lc": 239,
      "topic": "Two Pointers & Sliding Window",
      "vars": "n = len(nums), k = window size",
      "note": "Ignore the returned list of maxima.",
      "code": [
        "def maxSlidingWindow(nums, k):",
        "    dq = deque()          # indices, their values decreasing",
        "    res = []",
        "    for i, x in enumerate(nums):",
        "        while dq and nums[dq[-1]] <= x:",
        "            dq.pop()",
        "        dq.append(i)",
        "        if dq[0] <= i - k:",
        "            dq.popleft()",
        "        if i >= k - 1:",
        "            res.append(nums[dq[0]])",
        "    return res"
      ],
      "time": "O(n)",
      "space": "O(k)",
      "why": "Every index is appended exactly once and popped at most once, so the inner `while` costs O(n) spread over the whole run. The deque only ever holds indices inside the current window.",
      "trap": "O(n * k) is what the code looks like, and what the naive `max(nums[i:i + k])` per window really costs. A heap lands in between at O(n log k); the monotonic deque is what gets to linear."
    },
    {
      "id": "invert-binary-tree",
      "lc": 226,
      "topic": "Trees",
      "vars": "n = nodes, h = height",
      "code": [
        "def invertTree(root):",
        "    if not root:",
        "        return None",
        "    root.left, root.right = invertTree(root.right), invertTree(root.left)",
        "    return root"
      ],
      "time": "O(n)",
      "space": "O(h)",
      "accept": { "space": ["O(n)"] },
      "why": "Every node is visited once and swapped in place. The only extra memory is the call stack, one frame per level — O(log n) if the tree is balanced, O(n) if it is a chain.",
      "trap": "The swap is a single tuple assignment, not a copy of either subtree: both sides are evaluated first, then rebound. Writing it as two statements without a temporary is the version that loses a subtree."
    },
    {
      "id": "validate-bst",
      "lc": 98,
      "topic": "Trees",
      "vars": "n = nodes, h = height",
      "code": [
        "def isValidBST(root):",
        "    def check(node, lo, hi):",
        "        if not node:",
        "            return True",
        "        if not (lo < node.val < hi):",
        "            return False",
        "        return (check(node.left, lo, node.val)",
        "                and check(node.right, node.val, hi))",
        "",
        "    return check(root, float(\"-inf\"), float(\"inf\"))"
      ],
      "time": "O(n)",
      "space": "O(h)",
      "accept": { "space": ["O(n)"] },
      "why": "Each node is checked once against a numeric range that costs nothing to pass down. The stack is the height.",
      "trap": "The version that asks each node for `max(left subtree)` and `min(right subtree)` re-walks subtrees from every ancestor and is O(n^2). Carrying the bounds *down* is what buys the single pass."
    },
    {
      "id": "diameter-binary-tree",
      "lc": 543,
      "topic": "Trees",
      "vars": "n = nodes, h = height",
      "code": [
        "def diameterOfBinaryTree(root):",
        "    best = 0",
        "",
        "    def depth(node):",
        "        nonlocal best",
        "        if not node:",
        "            return 0",
        "        left, right = depth(node.left), depth(node.right)",
        "        best = max(best, left + right)",
        "        return 1 + max(left, right)",
        "",
        "    depth(root)",
        "    return best"
      ],
      "time": "O(n)",
      "space": "O(h)",
      "accept": { "space": ["O(n)"] },
      "why": "One post-order pass: each node reports its depth upward and updates the best path through itself on the way. Every node is touched once.",
      "trap": "The shape people write first calls a separate `depth()` from inside a walk over every node — that recomputes each subtree's depth once per ancestor and is O(n^2) on a chain. Returning the depth *while* recording the answer is the fix."
    },
    {
      "id": "build-tree-preorder-inorder",
      "lc": 105,
      "topic": "Trees",
      "vars": "n = nodes",
      "code": [
        "def buildTree(preorder, inorder):",
        "    pos = {v: i for i, v in enumerate(inorder)}",
        "    it = iter(preorder)",
        "",
        "    def build(lo, hi):",
        "        if lo > hi:",
        "            return None",
        "        node = TreeNode(next(it))",
        "        mid = pos[node.val]",
        "        node.left = build(lo, mid - 1)",
        "        node.right = build(mid + 1, hi)",
        "        return node",
        "",
        "    return build(0, len(inorder) - 1)"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "The index map is built once in O(n), and after that each node costs one dict lookup and one `next`. The map itself is n entries, which dominates the O(h) stack.",
      "trap": "The textbook version — `inorder.index(root_val)` plus `preorder[1:mid + 1]` slices — is O(n^2): the scan and the copies are each linear per node. Precomputing the positions and passing index bounds is what removes both."
    },
    {
      "id": "lca-binary-tree",
      "lc": 236,
      "topic": "Trees",
      "vars": "n = nodes, h = height",
      "code": [
        "def lowestCommonAncestor(root, p, q):",
        "    if root is None or root is p or root is q:",
        "        return root",
        "    left = lowestCommonAncestor(root.left, p, q)",
        "    right = lowestCommonAncestor(root.right, p, q)",
        "    if left and right:",
        "        return root",
        "    return left or right"
      ],
      "time": "O(n)",
      "space": "O(h)",
      "accept": { "space": ["O(n)"] },
      "why": "There is no ordering to exploit in a plain binary tree, so in the worst case every node is visited before the recursion unwinds. The stack is one frame per level.",
      "trap": "It feels like it should stop early once both targets are found — it does not, and cannot, because a node on the far side might still be one of them. On a *BST* (LC 235) the values steer the walk and it drops to O(h)."
    },
    {
      "id": "kth-smallest-bst",
      "lc": 230,
      "topic": "Trees",
      "vars": "n = nodes, h = height, k = the rank asked for",
      "code": [
        "def kthSmallest(root, k):",
        "    stack = []",
        "    node = root",
        "    while stack or node:",
        "        while node:",
        "            stack.append(node)",
        "            node = node.left",
        "        node = stack.pop()",
        "        k -= 1",
        "        if k == 0:",
        "            return node.val",
        "        node = node.right"
      ],
      "time": "O(h + k)",
      "space": "O(h)",
      "accept": { "time": ["O(n)"], "space": ["O(n)"] },
      "why": "Descending to the smallest value pushes h frames, and after that each of the k pops does O(1) amortised work — every node enters and leaves the stack at most once.",
      "trap": "Collecting the whole in-order traversal into a list and returning `res[k - 1]` is O(n) time and O(n) space. Stopping at k is the entire difference, and it is invisible unless you notice the early `return` inside the loop."
    },
    {
      "id": "right-side-view",
      "lc": 199,
      "topic": "Trees",
      "vars": "n = nodes, w = widest level",
      "code": [
        "def rightSideView(root):",
        "    if not root:",
        "        return []",
        "    res, q = [], deque([root])",
        "    while q:",
        "        for i in range(len(q)):",
        "            node = q.popleft()",
        "            if i == 0:",
        "                res.append(node.val)",
        "            if node.right:",
        "                q.append(node.right)",
        "            if node.left:",
        "                q.append(node.left)",
        "    return res"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "accept": { "space": ["O(w)"] },
      "why": "Every node is enqueued and dequeued exactly once. The queue holds one level at a time, and the bottom level of a complete tree is about n/2 nodes — so O(w), which is O(n).",
      "trap": "The `for` inside the `while` looks quadratic. `len(q)` is read once per level and freezes that level's size, so the two loops together still visit each node once. The DFS version of this problem is O(h) space rather than O(w) — the traversal you pick changes the memory profile, never the O(n) time."
    },
    {
      "id": "balanced-binary-tree-naive",
      "lc": 110,
      "topic": "Trees",
      "vars": "n = nodes, h = height",
      "note": "Answer for the worst case.",
      "code": [
        "def isBalanced(root):",
        "    def height(node):",
        "        if not node:",
        "            return 0",
        "        return 1 + max(height(node.left), height(node.right))",
        "",
        "    if not root:",
        "        return True",
        "    if abs(height(root.left) - height(root.right)) > 1:",
        "        return False",
        "    return isBalanced(root.left) and isBalanced(root.right)"
      ],
      "time": "O(n^2)",
      "space": "O(h)",
      "accept": { "space": ["O(n)"] },
      "why": "`height` walks a whole subtree, and it is called again from every node on the way down — so each node is visited once per ancestor. On a chain that is n + (n-1) + ... = O(n^2); on a balanced tree it is O(n log n).",
      "trap": "Two recursions in one function is the smell. The one-pass version returns the height *and* a sentinel (-1) for \"already unbalanced\", so no subtree is measured twice, and it is O(n)."
    },
    {
      "id": "word-break-dp",
      "lc": 139,
      "topic": "Dynamic Programming",
      "vars": "n = len(s), m = total characters in wordDict",
      "note": "Count one slice-and-lookup as a single unit of work.",
      "code": [
        "def wordBreak(s, wordDict):",
        "    words = set(wordDict)",
        "    dp = [False] * (len(s) + 1)",
        "    dp[0] = True",
        "    for i in range(1, len(s) + 1):",
        "        for j in range(i):",
        "            if dp[j] and s[j:i] in words:",
        "                dp[i] = True",
        "                break",
        "    return dp[len(s)]"
      ],
      "time": "O(n^2)",
      "space": "O(n + m)",
      "accept": { "space": ["O(n)"] },
      "why": "Every (j, i) pair is considered once, which is the n^2. Memory is the dp array (n + 1 booleans) plus the set of words.",
      "trap": "Dropping the `note` makes this O(n^3) in disguise: `s[j:i]` copies up to n characters and then hashes them. That hidden factor is exactly why the trie variant, or bounding the inner loop by the longest word, is worth the trouble."
    },
    {
      "id": "max-product-subarray",
      "lc": 152,
      "topic": "Dynamic Programming",
      "vars": "n = len(nums)",
      "code": [
        "def maxProduct(nums):",
        "    best = cur_max = cur_min = nums[0]",
        "    for i in range(1, len(nums)):",
        "        x = nums[i]",
        "        options = (x, cur_max * x, cur_min * x)",
        "        cur_max, cur_min = max(options), min(options)",
        "        best = max(best, cur_max)",
        "    return best"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "One pass carrying three numbers. Keeping the running *minimum* as well is what handles a negative flipping the sign — no table needed.",
      "trap": "Writing the loop as `for x in nums[1:]` would quietly make it O(n) space: the slice copies the array. Indexing from 1 costs nothing."
    },
    {
      "id": "decode-ways",
      "lc": 91,
      "topic": "Dynamic Programming",
      "vars": "n = len(s)",
      "code": [
        "def numDecodings(s):",
        "    if not s or s[0] == \"0\":",
        "        return 0",
        "    prev, cur = 1, 1",
        "    for i in range(1, len(s)):",
        "        take = cur if s[i] != \"0\" else 0",
        "        pair = prev if 10 <= int(s[i - 1:i + 1]) <= 26 else 0",
        "        if take + pair == 0:",
        "            return 0",
        "        prev, cur = cur, take + pair",
        "    return cur"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "A Fibonacci-shaped recurrence: position i only ever reads i-1 and i-2, so two variables replace the whole table.",
      "trap": "The plain recursion branches two ways at every index and is O(2^n) — this is the same problem as naive fib, wearing a string. Memoising gets O(n) time but keeps O(n) space; rolling two variables is what removes even that."
    },
    {
      "id": "house-robber-circular",
      "lc": 213,
      "topic": "Dynamic Programming",
      "vars": "n = len(nums)",
      "code": [
        "def rob(nums):",
        "    def line(vals):",
        "        take, skip = 0, 0",
        "        for x in vals:",
        "            take, skip = skip + x, max(skip, take)",
        "        return max(take, skip)",
        "",
        "    if len(nums) == 1:",
        "        return nums[0]",
        "    return max(line(nums[1:]), line(nums[:-1]))"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "Houses in a circle become two straight lines — drop the first house, or drop the last — and each line is the O(1)-space LC 198 scan. Two linear passes are still linear.",
      "trap": "Running the helper twice does not square anything: 2n is O(n). The space, though, is not O(1) — `nums[1:]` and `nums[:-1]` each copy almost the whole array. Passing index ranges instead would fix that."
    },
    {
      "id": "partition-equal-subset-sum",
      "lc": 416,
      "topic": "Dynamic Programming",
      "vars": "n = len(nums), s = sum(nums) // 2",
      "code": [
        "def canPartition(nums):",
        "    total = sum(nums)",
        "    if total % 2:",
        "        return False",
        "    target = total // 2",
        "    dp = [False] * (target + 1)",
        "    dp[0] = True",
        "    for x in nums:",
        "        for t in range(target, x - 1, -1):",
        "            dp[t] = dp[t] or dp[t - x]",
        "    return dp[target]"
      ],
      "time": "O(n * s)",
      "space": "O(s)",
      "why": "The classic 0/1 knapsack: n items against s reachable sums, one row of the table kept. Iterating `t` downward is what stops an item being used twice.",
      "trap": "This is pseudo-polynomial, not polynomial. `s` is a *value*, not an input size — the input is n numbers of about log s bits each, so doubling the numbers' magnitude doubles the running time while the input barely grows."
    },
    {
      "id": "edit-distance",
      "lc": 72,
      "topic": "Dynamic Programming",
      "vars": "m = len(word1), n = len(word2)",
      "code": [
        "def minDistance(word1, word2):",
        "    m, n = len(word1), len(word2)",
        "    dp = list(range(n + 1))",
        "    for i in range(1, m + 1):",
        "        prev, dp[0] = dp[0], i",
        "        for j in range(1, n + 1):",
        "            cur = dp[j]",
        "            if word1[i - 1] == word2[j - 1]:",
        "                dp[j] = prev",
        "            else:",
        "                dp[j] = 1 + min(prev, dp[j], dp[j - 1])",
        "            prev = cur",
        "    return dp[n]"
      ],
      "time": "O(m * n)",
      "space": "O(n)",
      "why": "Every cell of the m x n table is computed once. Each cell reads only the one above, the one to the left, and the diagonal — so one row plus a saved corner (`prev`) is all that has to be alive.",
      "trap": "O(m * n) space is the textbook answer and is what the 2-D table costs. Rolling to a single row is free to write and, since only the shorter word need be the row, gets the memory down to the smaller of the two lengths."
    },
    {
      "id": "palindromic-substrings",
      "lc": 647,
      "topic": "Dynamic Programming",
      "vars": "n = len(s)",
      "code": [
        "def countSubstrings(s):",
        "    total = 0",
        "    for center in range(2 * len(s) - 1):",
        "        lo, hi = center // 2, center // 2 + center % 2",
        "        while lo >= 0 and hi < len(s) and s[lo] == s[hi]:",
        "            total += 1",
        "            lo -= 1",
        "            hi += 1",
        "    return total"
      ],
      "time": "O(n^2)",
      "space": "O(1)",
      "why": "2n - 1 centres (n single characters, n - 1 gaps), each expanding at most n/2 steps. Only counters are kept.",
      "trap": "Enumerating every substring and testing it is O(n^3) — there are n^2 substrings and each test is linear. The DP table version is the same O(n^2) time as this but O(n^2) space; expansion needs none. (Manacher's does it in O(n).)"
    },
    {
      "id": "minimum-path-sum",
      "lc": 64,
      "topic": "Dynamic Programming",
      "vars": "m, n = grid dimensions",
      "note": "The grid is rewritten in place; count only extra space.",
      "code": [
        "def minPathSum(grid):",
        "    m, n = len(grid), len(grid[0])",
        "    for i in range(m):",
        "        for j in range(n):",
        "            if i == 0 and j == 0:",
        "                continue",
        "            if i == 0:",
        "                grid[i][j] += grid[i][j - 1]",
        "            elif j == 0:",
        "                grid[i][j] += grid[i - 1][j]",
        "            else:",
        "                grid[i][j] += min(grid[i - 1][j], grid[i][j - 1])",
        "    return grid[m - 1][n - 1]"
      ],
      "time": "O(m * n)",
      "space": "O(1)",
      "why": "Each cell is finalised once from its two already-final neighbours, and the answer is written over the grid, so nothing is allocated.",
      "trap": "Walking every path is C(m + n - 2, n - 1) routes — exponential. The DP works because a cell's best route does not depend on how you got to its neighbours, only on their values."
    },
    {
      "id": "clone-graph",
      "lc": 133,
      "topic": "Graphs",
      "vars": "V = nodes, E = edges",
      "note": "Ignore the cloned graph itself; count the bookkeeping.",
      "code": [
        "def cloneGraph(node):",
        "    clones = {}",
        "",
        "    def dfs(cur):",
        "        if cur in clones:",
        "            return clones[cur]",
        "        copy = Node(cur.val)",
        "        clones[cur] = copy",
        "        for nb in cur.neighbors:",
        "            copy.neighbors.append(dfs(nb))",
        "        return copy",
        "",
        "    return dfs(node) if node else None"
      ],
      "time": "O(V + E)",
      "space": "O(V)",
      "why": "Registering the clone *before* recursing is what stops a cycle looping forever, so each node is built once and each edge is followed once (twice, for an undirected graph — a constant).",
      "trap": "Putting `clones[cur] = copy` after the neighbour loop turns any cycle into infinite recursion. And the map is keyed by node, so it is O(V) — the edges live in the clones, which are the output."
    },
    {
      "id": "pacific-atlantic",
      "lc": 417,
      "topic": "Graphs",
      "vars": "m, n = grid dimensions",
      "code": [
        "def pacificAtlantic(heights):",
        "    m, n = len(heights), len(heights[0])",
        "",
        "    def climb(starts):",
        "        seen = set(starts)",
        "        stack = list(starts)",
        "        while stack:",
        "            i, j = stack.pop()",
        "            for x, y in ((i+1, j), (i-1, j), (i, j+1), (i, j-1)):",
        "                if (0 <= x < m and 0 <= y < n and (x, y) not in seen",
        "                        and heights[x][y] >= heights[i][j]):",
        "                    seen.add((x, y))",
        "                    stack.append((x, y))",
        "        return seen",
        "",
        "    pac = climb([(0, j) for j in range(n)] + [(i, 0) for i in range(m)])",
        "    atl = climb([(m-1, j) for j in range(n)] + [(i, n-1) for i in range(m)])",
        "    return [list(cell) for cell in pac & atl]"
      ],
      "time": "O(m * n)",
      "space": "O(m * n)",
      "why": "Two searches, each of which adds a cell to `seen` at most once and looks at its four neighbours. Two linear sweeps are still linear in the number of cells.",
      "trap": "The direct reading — run a search from every cell and see whether it reaches both oceans — is O((m * n)^2). Reversing the flow, so water climbs inland from the borders, is what turns it into two sweeps."
    },
    {
      "id": "course-schedule-ii",
      "lc": 210,
      "topic": "Graphs",
      "vars": "V = numCourses, E = len(prerequisites)",
      "code": [
        "def findOrder(numCourses, prerequisites):",
        "    graph = [[] for _ in range(numCourses)]",
        "    indeg = [0] * numCourses",
        "    for course, need in prerequisites:",
        "        graph[need].append(course)",
        "        indeg[course] += 1",
        "",
        "    q = deque(c for c in range(numCourses) if indeg[c] == 0)",
        "    order = []",
        "    while q:",
        "        c = q.popleft()",
        "        order.append(c)",
        "        for nxt in graph[c]:",
        "            indeg[nxt] -= 1",
        "            if indeg[nxt] == 0:",
        "                q.append(nxt)",
        "    return order if len(order) == numCourses else []"
      ],
      "time": "O(V + E)",
      "space": "O(V + E)",
      "why": "Kahn's algorithm dequeues each course once and decrements each edge once. The adjacency lists hold E entries and the queue, in-degrees and order hold V.",
      "trap": "The `for` inside the `while` is not a second factor: every edge is relaxed exactly once across the *entire* run. And `len(order) == numCourses` is the cycle check — a cycle simply never reaches in-degree zero, so it never enters the queue."
    },
    {
      "id": "max-area-of-island",
      "lc": 695,
      "topic": "Graphs",
      "vars": "m, n = grid dimensions",
      "code": [
        "def maxAreaOfIsland(grid):",
        "    m, n = len(grid), len(grid[0])",
        "",
        "    def sink(i, j):",
        "        if not (0 <= i < m and 0 <= j < n) or grid[i][j] == 0:",
        "            return 0",
        "        grid[i][j] = 0",
        "        return 1 + sink(i+1, j) + sink(i-1, j) + sink(i, j+1) + sink(i, j-1)",
        "",
        "    return max((sink(i, j) for i in range(m) for j in range(n)), default=0)"
      ],
      "time": "O(m * n)",
      "space": "O(m * n)",
      "why": "Sinking a cell to 0 as it is entered means no cell is ever entered twice, so the whole nested scan plus every flood fill under it is one pass over the grid.",
      "trap": "The double `for` wrapping a recursive flood fill reads as O((m * n)^2), and it would be without the marking. The space is not O(1) either: a single snaking island can put every cell on the call stack at once."
    },
    {
      "id": "word-ladder",
      "lc": 127,
      "topic": "Graphs",
      "vars": "n = len(wordList), l = len(beginWord)",
      "code": [
        "def ladderLength(beginWord, endWord, wordList):",
        "    words = set(wordList)",
        "    if endWord not in words:",
        "        return 0",
        "    q = deque([(beginWord, 1)])",
        "    while q:",
        "        word, steps = q.popleft()",
        "        if word == endWord:",
        "            return steps",
        "        for i in range(len(word)):",
        "            for c in \"abcdefghijklmnopqrstuvwxyz\":",
        "                nxt = word[:i] + c + word[i+1:]",
        "                if nxt in words:",
        "                    words.remove(nxt)",
        "                    q.append((nxt, steps + 1))",
        "    return 0"
      ],
      "time": "O(n * l^2)",
      "space": "O(n * l)",
      "why": "Each word is dequeued once and generates 26 * l candidates; building and hashing each candidate costs another l. 26 is a constant, so one word costs O(l^2) and there are n of them.",
      "trap": "O(n * l) is the usual guess because the 26 gets dropped as a constant and the *string building* gets forgotten. `word[:i] + c + word[i+1:]` allocates a fresh l-character string and then hashes it — that is the second l."
    },
    {
      "id": "connected-components-union-find",
      "lc": 323,
      "topic": "Graphs",
      "vars": "n = nodes, e = len(edges)",
      "note": "Treat one find/union as effectively constant (inverse-Ackermann).",
      "code": [
        "def countComponents(n, edges):",
        "    parent = list(range(n))",
        "",
        "    def find(x):",
        "        while parent[x] != x:",
        "            parent[x] = parent[parent[x]]     # path halving",
        "            x = parent[x]",
        "        return x",
        "",
        "    count = n",
        "    for a, b in edges:",
        "        ra, rb = find(a), find(b)",
        "        if ra != rb:",
        "            parent[ra] = rb",
        "            count -= 1",
        "    return count"
      ],
      "time": "O(n + e)",
      "space": "O(n)",
      "why": "Building `parent` is O(n) and each edge triggers two finds. Path halving keeps the trees flat, so a find is effectively constant. Only the parent array is stored — no adjacency list is ever built.",
      "trap": "Without the path-halving line, a chain of unions can grow a tree n deep and every find becomes O(n) — the whole thing degrades to O(n * e). The one-line compression is the difference between the textbook bound and a quadratic."
    },
    {
      "id": "cheapest-flights-k-stops",
      "lc": 787,
      "topic": "Graphs",
      "vars": "n = cities, e = len(flights), k = stops allowed",
      "code": [
        "def findCheapestPrice(n, flights, src, dst, k):",
        "    INF = float(\"inf\")",
        "    dist = [INF] * n",
        "    dist[src] = 0",
        "    for _ in range(k + 1):",
        "        nxt = dist[:]",
        "        for a, b, price in flights:",
        "            if dist[a] + price < nxt[b]:",
        "                nxt[b] = dist[a] + price",
        "        dist = nxt",
        "    return -1 if dist[dst] == INF else dist[dst]"
      ],
      "time": "O(k * e)",
      "space": "O(n)",
      "why": "Bellman-Ford, stopped early: k + 1 rounds, each relaxing every edge once. Round r holds the cheapest route using at most r hops, which is exactly the constraint. Two distance arrays is all the memory.",
      "trap": "Plain Dijkstra is O(E log V) and is *wrong* here — the cheapest route within k stops is often not a prefix of the cheapest route overall. The `k` factor is not a wasteful loop; it is what enforces the hop limit. Copying `dist` each round matters too: relaxing in place would let one round use two hops."
    },
    {
      "id": "find-min-rotated",
      "lc": 153,
      "topic": "Binary Search",
      "vars": "n = len(nums)",
      "code": [
        "def findMin(nums):",
        "    lo, hi = 0, len(nums) - 1",
        "    while lo < hi:",
        "        mid = (lo + hi) // 2",
        "        if nums[mid] > nums[hi]:",
        "            lo = mid + 1",
        "        else:",
        "            hi = mid",
        "    return nums[lo]"
      ],
      "time": "O(log n)",
      "space": "O(1)",
      "why": "Each comparison against `nums[hi]` says which side of the rotation point `mid` is on, so half the range is discarded every step.",
      "trap": "Comparing against `nums[lo]` instead needs an extra case and is where most off-by-one bugs live. Also note the invariant `hi = mid`, not `mid - 1`: the minimum could *be* `mid`. With duplicates allowed (LC 154) the worst case degrades to O(n)."
    },
    {
      "id": "search-2d-matrix",
      "lc": 74,
      "topic": "Binary Search",
      "vars": "m, n = matrix dimensions",
      "code": [
        "def searchMatrix(matrix, target):",
        "    m, n = len(matrix), len(matrix[0])",
        "    lo, hi = 0, m * n - 1",
        "    while lo <= hi:",
        "        mid = (lo + hi) // 2",
        "        val = matrix[mid // n][mid % n]",
        "        if val == target:",
        "            return True",
        "        if val < target:",
        "            lo = mid + 1",
        "        else:",
        "            hi = mid - 1",
        "    return False"
      ],
      "time": "O(log (m * n))",
      "space": "O(1)",
      "accept": { "time": ["O(log m + log n)"] },
      "why": "The rows are sorted and each row starts above the previous one's end, so the matrix is one sorted run of m * n values and `divmod` is the only translation needed.",
      "trap": "Two separate binary searches (find the row, then the column) is the same bound — log(m * n) = log m + log n. What is *not* the same is scanning for the row first: that is O(m + log n), and on a tall matrix it is the difference that gets noticed."
    },
    {
      "id": "search-range",
      "lc": 34,
      "topic": "Binary Search",
      "vars": "n = len(nums)",
      "code": [
        "def searchRange(nums, target):",
        "    def first_at_least(t):",
        "        lo, hi = 0, len(nums)",
        "        while lo < hi:",
        "            mid = (lo + hi) // 2",
        "            if nums[mid] < t:",
        "                lo = mid + 1",
        "            else:",
        "                hi = mid",
        "        return lo",
        "",
        "    start = first_at_least(target)",
        "    if start == len(nums) or nums[start] != target:",
        "        return [-1, -1]",
        "    return [start, first_at_least(target + 1) - 1]"
      ],
      "time": "O(log n)",
      "space": "O(1)",
      "why": "Two independent binary searches, and 2 log n is O(log n).",
      "trap": "Finding the target once and then expanding outward is O(n) — an array of all one value makes that walk the whole thing. Searching for the *boundary* rather than the value is what keeps both ends logarithmic."
    },
    {
      "id": "median-two-sorted-arrays",
      "lc": 4,
      "topic": "Binary Search",
      "vars": "m = len(the shorter array), n = len(the longer one)",
      "code": [
        "def findMedianSortedArrays(a, b):",
        "    if len(a) > len(b):",
        "        a, b = b, a",
        "    m, n = len(a), len(b)",
        "    half = (m + n + 1) // 2",
        "    lo, hi = 0, m",
        "    while lo <= hi:",
        "        i = (lo + hi) // 2          # take i from a, the rest from b",
        "        j = half - i",
        "        left_a = a[i - 1] if i else float(\"-inf\")",
        "        right_a = a[i] if i < m else float(\"inf\")",
        "        left_b = b[j - 1] if j else float(\"-inf\")",
        "        right_b = b[j] if j < n else float(\"inf\")",
        "        if left_a <= right_b and left_b <= right_a:",
        "            if (m + n) % 2:",
        "                return max(left_a, left_b)",
        "            return (max(left_a, left_b) + min(right_a, right_b)) / 2",
        "        if left_a > right_b:",
        "            hi = i - 1",
        "        else:",
        "            lo = i + 1"
      ],
      "time": "O(log m)",
      "space": "O(1)",
      "why": "The search is over *how many elements to take from the shorter array* — a range of size m — and each guess is checked in constant time. The swap on line 2 is what makes the log the smaller of the two lengths.",
      "trap": "Merging the arrays and taking the middle is O(m + n) and is the answer people give; the problem exists to rule it out. Binary-searching for the *partition* rather than for a value is the move, and dropping the swap silently costs you log of the longer array."
    },
    {
      "id": "split-array-largest-sum",
      "lc": 410,
      "topic": "Binary Search",
      "vars": "n = len(nums), s = sum(nums)",
      "code": [
        "def splitArray(nums, k):",
        "    def pieces(cap):",
        "        count, cur = 1, 0",
        "        for x in nums:",
        "            if cur + x > cap:",
        "                count += 1",
        "                cur = 0",
        "            cur += x",
        "        return count",
        "",
        "    lo, hi = max(nums), sum(nums)",
        "    while lo < hi:",
        "        mid = (lo + hi) // 2",
        "        if pieces(mid) > k:",
        "            lo = mid + 1",
        "        else:",
        "            hi = mid",
        "    return lo"
      ],
      "time": "O(n log s)",
      "space": "O(1)",
      "why": "The search runs over the *answer* — a value between the largest element and the total — so it takes log s steps, and each step is one greedy linear feasibility check.",
      "trap": "The log is over the range of sums, not over n, which is the whole point: this is binary search on the answer, not on the array. The DP formulation of the same problem is O(n^2 * k); paying log s linear checks instead is a large win whenever the values are bounded."
    },
    {
      "id": "task-scheduler",
      "lc": 621,
      "topic": "Heap",
      "vars": "t = len(tasks); the cooldown is the parameter n",
      "code": [
        "def leastInterval(tasks, n):",
        "    counts = Counter(tasks)",
        "    top = max(counts.values())",
        "    ties = sum(1 for c in counts.values() if c == top)",
        "    return max(len(tasks), (top - 1) * (n + 1) + ties)"
      ],
      "time": "O(t)",
      "space": "O(1)",
      "why": "One pass to count, then arithmetic. The counter holds one entry per distinct task letter — 26 at most.",
      "trap": "O(t * n) is the answer people give after picturing the simulation, idle slot by idle slot. The greedy heap version is real and is O(t log 26) — still linear — but the schedule's length is fixed by the most frequent task alone, so no simulation is needed at all."
    },
    {
      "id": "last-stone-weight",
      "lc": 1046,
      "topic": "Heap",
      "vars": "n = len(stones)",
      "code": [
        "def lastStoneWeight(stones):",
        "    heap = [-s for s in stones]",
        "    heapq.heapify(heap)",
        "    while len(heap) > 1:",
        "        a = -heapq.heappop(heap)",
        "        b = -heapq.heappop(heap)",
        "        if a != b:",
        "            heapq.heappush(heap, -(a - b))",
        "    return -heap[0] if heap else 0"
      ],
      "time": "O(n log n)",
      "space": "O(n)",
      "why": "`heapify` is O(n), and every iteration removes at least one stone, so there are fewer than n iterations at O(log n) each.",
      "trap": "Negating for a max-heap is the Python idiom, and the list comprehension makes the O(n) copy explicit — passing `stones` to `heapify` directly would mutate the caller's list but be O(1) extra. Re-sorting the list on each round instead of heaping is O(n^2 log n)."
    },
    {
      "id": "kth-largest-stream",
      "lc": 703,
      "topic": "Heap",
      "vars": "k = the rank tracked, n = values seen so far",
      "note": "Time is for one `add` call; the constructor is separate.",
      "code": [
        "class KthLargest:",
        "    def __init__(self, k, nums):",
        "        self.k = k",
        "        self.heap = nums[:]",
        "        heapq.heapify(self.heap)",
        "        while len(self.heap) > k:",
        "            heapq.heappop(self.heap)",
        "",
        "    def add(self, val):",
        "        heapq.heappush(self.heap, val)",
        "        if len(self.heap) > self.k:",
        "            heapq.heappop(self.heap)",
        "        return self.heap[0]"
      ],
      "time": "O(log k)",
      "space": "O(k)",
      "why": "The min-heap is capped at k entries, so a push and a pop are each log k. The kth largest is then always the root.",
      "trap": "The cost does not grow with the length of the stream — that is the point of capping the heap. Keeping every value and sorting on each `add` is O(n log n) per call, and even a k-sized *sorted list* costs O(k) per insert to shift."
    },
    {
      "id": "eval-rpn",
      "lc": 150,
      "topic": "Stack",
      "vars": "n = len(tokens)",
      "code": [
        "def evalRPN(tokens):",
        "    ops = {",
        "        \"+\": lambda a, b: a + b,",
        "        \"-\": lambda a, b: a - b,",
        "        \"*\": lambda a, b: a * b,",
        "        \"/\": lambda a, b: int(a / b),      # truncates toward zero",
        "    }",
        "    stack = []",
        "    for tok in tokens:",
        "        if tok in ops:",
        "            b, a = stack.pop(), stack.pop()",
        "            stack.append(ops[tok](a, b))",
        "        else:",
        "            stack.append(int(tok))",
        "    return stack[0]"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "One pass; every token is pushed or consumes two pushes. A run of operands with no operators leaves all n on the stack.",
      "trap": "`b, a = stack.pop(), stack.pop()` is not decoration — the second operand comes off first, and getting that backwards only shows up on `-` and `/`. The bound is unaffected either way."
    },
    {
      "id": "simplify-path",
      "lc": 71,
      "topic": "Stack",
      "vars": "n = len(path)",
      "code": [
        "def simplifyPath(path):",
        "    stack = []",
        "    for part in path.split(\"/\"):",
        "        if part == \"..\":",
        "            if stack:",
        "                stack.pop()",
        "        elif part and part != \".\":",
        "            stack.append(part)",
        "    return \"/\" + \"/\".join(stack)"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "`split` is one linear pass producing pieces that total n characters, and each piece is pushed or popped at most once. The final `join` is linear in what is left.",
      "trap": "Repeatedly rewriting the string with `replace(\"//\", \"/\")` until it stops changing is the naive route and is O(n^2). The stack is here because `..` has to undo an *arbitrary* earlier decision, which no single left-to-right rewrite can do."
    },
    {
      "id": "longest-valid-parentheses",
      "lc": 32,
      "topic": "Stack",
      "vars": "n = len(s)",
      "code": [
        "def longestValidParentheses(s):",
        "    best = 0",
        "    stack = [-1]                  # index just before the current valid run",
        "    for i, ch in enumerate(s):",
        "        if ch == \"(\":",
        "            stack.append(i)",
        "        else:",
        "            stack.pop()",
        "            if stack:",
        "                best = max(best, i - stack[-1])",
        "            else:",
        "                stack.append(i)",
        "    return best"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "Each index is pushed at most once and popped at most once. The stack holds indices, so the length of a run is a subtraction rather than a recount.",
      "trap": "Checking every substring for validity is O(n^3), and even the O(n^2) two-pass version is beaten here. The sentinel `-1` is what makes `i - stack[-1]` work without a special case for a run starting at index 0. (There is also an O(1)-space two-scan solution — left-to-right, then right-to-left, counting brackets.)"
    },
    {
      "id": "car-fleet",
      "lc": 853,
      "topic": "Stack",
      "vars": "n = number of cars",
      "code": [
        "def carFleet(target, position, speed):",
        "    fleets = 0",
        "    slowest = 0.0",
        "    for pos, sp in sorted(zip(position, speed), reverse=True):",
        "        arrival = (target - pos) / sp",
        "        if arrival > slowest:",
        "            fleets += 1",
        "            slowest = arrival",
        "    return fleets"
      ],
      "time": "O(n log n)",
      "space": "O(n)",
      "why": "The sort dominates; the sweep from the car nearest the target backwards is one linear pass. `zip` materialises n pairs, and Timsort needs O(n) working space.",
      "trap": "Simulating the cars catching each other is O(n^2). Sorting by position and asking only \"does this car arrive later than everything ahead of it?\" replaces the simulation — a car that would arrive sooner simply joins the fleet in front and inherits its time."
    },
    {
      "id": "online-stock-span",
      "lc": 901,
      "topic": "Stack",
      "vars": "n = calls to next() so far",
      "note": "Time is the amortised cost of one `next` call.",
      "code": [
        "class StockSpanner:",
        "    def __init__(self):",
        "        self.stack = []           # (price, span already absorbed)",
        "",
        "    def next(self, price):",
        "        span = 1",
        "        while self.stack and self.stack[-1][0] <= price:",
        "            span += self.stack.pop()[1]",
        "        self.stack.append((price, span))",
        "        return span"
      ],
      "time": "O(1)",
      "space": "O(n)",
      "why": "A single call can pop many entries, but each price is pushed exactly once and popped at most once over the object's whole life, so n calls cost O(n) in total — a constant each on average.",
      "trap": "O(n) is the honest *worst case* for one call, and the question asks for the amortised cost — that gap is the whole reason a monotonic stack is worth writing. The space is genuinely O(n): a strictly decreasing price series never pops anything."
    },
    {
      "id": "remove-nth-from-end",
      "lc": 19,
      "topic": "Linked List",
      "vars": "L = nodes in the list (the parameter n is the offset from the end)",
      "code": [
        "def removeNthFromEnd(head, n):",
        "    dummy = ListNode(0, head)",
        "    fast = slow = dummy",
        "    for _ in range(n):",
        "        fast = fast.next",
        "    while fast.next:",
        "        fast, slow = fast.next, slow.next",
        "    slow.next = slow.next.next",
        "    return dummy.next"
      ],
      "time": "O(L)",
      "space": "O(1)",
      "why": "One pass: the gap between the pointers is fixed at n, so when `fast` reaches the end `slow` is on the node before the target. Two pointers and a dummy node is all the state.",
      "trap": "Counting the length first and walking again is also O(L) — two passes, same class. What people get wrong is the space: pushing the nodes into a list to index from the back is O(L) memory for no gain. The dummy node exists so that deleting the *head* needs no special case."
    },
    {
      "id": "reorder-list",
      "lc": 143,
      "topic": "Linked List",
      "vars": "n = nodes",
      "code": [
        "def reorderList(head):",
        "    slow, fast = head, head.next",
        "    while fast and fast.next:",
        "        slow, fast = slow.next, fast.next.next",
        "",
        "    second = slow.next               # split",
        "    slow.next = None",
        "    prev = None",
        "    while second:                    # reverse the tail",
        "        nxt = second.next",
        "        second.next = prev",
        "        prev, second = second, nxt",
        "",
        "    first, second = head, prev       # weave",
        "    while second:",
        "        n1, n2 = first.next, second.next",
        "        first.next = second",
        "        second.next = n1",
        "        first, second = n1, n2"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Three sequential passes — find the middle, reverse the second half, interleave — and 3n is O(n). Every step relinks existing nodes; nothing is allocated.",
      "trap": "Three loops one after another are added, not multiplied. The O(n)-space shortcut is to dump the nodes into a list and re-link by index from both ends; it is the same time and is what the problem is quietly asking you not to do."
    },
    {
      "id": "copy-list-random-pointer",
      "lc": 138,
      "topic": "Linked List",
      "vars": "n = nodes",
      "note": "Ignore the copied list itself; count the bookkeeping.",
      "code": [
        "def copyRandomList(head):",
        "    clones = {None: None}",
        "    cur = head",
        "    while cur:",
        "        clones[cur] = Node(cur.val)",
        "        cur = cur.next",
        "    cur = head",
        "    while cur:",
        "        clones[cur].next = clones[cur.next]",
        "        clones[cur].random = clones[cur.random]",
        "        cur = cur.next",
        "    return clones[head]"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "Two passes: create every clone, then wire the pointers, each of which is a dict lookup. The map is one entry per node.",
      "trap": "One pass is not enough — `cur.random` may point at a node not yet cloned, which is why the map is built first. Seeding it with `{None: None}` removes every null check. The O(1)-space version interleaves each clone after its original in the same list, then unweaves."
    },
    {
      "id": "sort-list",
      "lc": 148,
      "topic": "Linked List",
      "vars": "n = nodes",
      "code": [
        "def sortList(head):",
        "    if not head or not head.next:",
        "        return head",
        "    slow, fast = head, head.next",
        "    while fast and fast.next:",
        "        slow, fast = slow.next, fast.next.next",
        "    mid, slow.next = slow.next, None",
        "    left, right = sortList(head), sortList(mid)",
        "",
        "    dummy = tail = ListNode(0)",
        "    while left and right:",
        "        if left.val <= right.val:",
        "            tail.next, left = left, left.next",
        "        else:",
        "            tail.next, right = right, right.next",
        "        tail = tail.next",
        "    tail.next = left or right",
        "    return dummy.next"
      ],
      "time": "O(n log n)",
      "space": "O(log n)",
      "why": "Merge sort: log n levels of splitting, and every level merges n nodes in total. The merging relinks in place, so the only memory is the recursion — one frame per level.",
      "trap": "\"Merge sort on a list is O(1) space\" is the half-remembered rule, and it only holds for the *bottom-up* version. This one stacks log n frames. Quicksort is the wrong tool here for a different reason: a list cannot index to a pivot in O(1)."
    },
    {
      "id": "palindrome-linked-list",
      "lc": 234,
      "topic": "Linked List",
      "vars": "n = nodes",
      "code": [
        "def isPalindrome(head):",
        "    slow = fast = head",
        "    while fast and fast.next:",
        "        slow, fast = slow.next, fast.next.next",
        "    prev = None",
        "    while slow:                          # reverse from the middle on",
        "        slow.next, prev, slow = prev, slow, slow.next",
        "    while prev:",
        "        if prev.val != head.val:",
        "            return False",
        "        prev, head = prev.next, head.next",
        "    return True"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Three sequential passes over at most n nodes, all relinking in place.",
      "trap": "`vals = [...]; return vals == vals[::-1]` is the same O(n) time and O(n) space, and it is what the follow-up asks you to beat. The tuple assignment on the reverse line reads its whole right-hand side first — writing those three moves as separate statements needs a temporary or it loses the rest of the list."
    },
    {
      "id": "find-duplicate-floyd",
      "lc": 287,
      "topic": "Linked List",
      "vars": "n = len(nums)",
      "code": [
        "def findDuplicate(nums):",
        "    slow = fast = nums[0]",
        "    while True:",
        "        slow = nums[slow]",
        "        fast = nums[nums[fast]]",
        "        if slow == fast:",
        "            break",
        "    slow = nums[0]",
        "    while slow != fast:",
        "        slow, fast = nums[slow], nums[fast]",
        "    return slow"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Reading `i -> nums[i]` as a linked list, a repeated value is a node with two ways in, so the list has a cycle and the duplicate is its entrance. Floyd's two phases are each at most n steps.",
      "trap": "The two `while` loops are sequential, not nested — O(n) + O(n). The easy answers are all ruled out by the constraints, and that is the tell: a set is O(n) space, sorting mutates the input, and counting values is O(n) space again."
    },
    {
      "id": "letter-combinations",
      "lc": 17,
      "topic": "Backtracking",
      "vars": "n = len(digits)",
      "note": "Here the output counts — `res` is the only thing that grows.",
      "code": [
        "def letterCombinations(digits):",
        "    if not digits:",
        "        return []",
        "    pad = {\"2\": \"abc\", \"3\": \"def\", \"4\": \"ghi\", \"5\": \"jkl\",",
        "           \"6\": \"mno\", \"7\": \"pqrs\", \"8\": \"tuv\", \"9\": \"wxyz\"}",
        "    res, cur = [], []",
        "",
        "    def build(i):",
        "        if i == len(digits):",
        "            res.append(\"\".join(cur))",
        "            return",
        "        for ch in pad[digits[i]]:",
        "            cur.append(ch)",
        "            build(i + 1)",
        "            cur.pop()",
        "",
        "    build(0)",
        "    return res"
      ],
      "time": "O(n * 4^n)",
      "space": "O(n * 4^n)",
      "why": "Up to 4 letters per digit gives at most 4^n leaves, and each leaf joins n characters into a new string. The recursion is only n frames deep — the size is all in `res`.",
      "trap": "O(4^n) counts the combinations but not their length; enumerating variable-length outputs always needs that extra factor. Most digits carry 3 letters, so 3^n is the typical case — the base is a constant either way, and the exponent is the number of digits."
    },
    {
      "id": "n-queens",
      "lc": 51,
      "topic": "Backtracking",
      "vars": "n = board size",
      "note": "Ignore the boards collected in `res`.",
      "code": [
        "def solveNQueens(n):",
        "    res = []",
        "    cols, diag, anti = set(), set(), set()",
        "    placed = []",
        "",
        "    def place(r):",
        "        if r == n:",
        "            res.append([\".\" * c + \"Q\" + \".\" * (n - c - 1) for c in placed])",
        "            return",
        "        for c in range(n):",
        "            if c in cols or (r - c) in diag or (r + c) in anti:",
        "                continue",
        "            cols.add(c)",
        "            diag.add(r - c)",
        "            anti.add(r + c)",
        "            placed.append(c)",
        "            place(r + 1)",
        "            placed.pop()",
        "            cols.discard(c)",
        "            diag.discard(r - c)",
        "            anti.discard(r + c)",
        "",
        "    place(0)",
        "    return res"
      ],
      "time": "O(n!)",
      "space": "O(n)",
      "why": "Row 0 has n choices, row 1 at most n - 1 that survive the column check, and so on — the search tree has at most n! leaves. The three sets and `placed` hold one entry per row.",
      "trap": "O(n^n) is the bound without pruning: n columns tried at each of n rows. The sets are what collapse it to a factorial, and they are also why the *space* is only linear — one partial placement exists at a time, and the finished boards are output."
    },
    {
      "id": "palindrome-partitioning",
      "lc": 131,
      "topic": "Backtracking",
      "vars": "n = len(s)",
      "note": "Ignore the partitions collected in `res`.",
      "code": [
        "def partition(s):",
        "    res, cur = [], []",
        "",
        "    def walk(start):",
        "        if start == len(s):",
        "            res.append(cur[:])",
        "            return",
        "        for end in range(start + 1, len(s) + 1):",
        "            piece = s[start:end]",
        "            if piece == piece[::-1]:",
        "                cur.append(piece)",
        "                walk(end)",
        "                cur.pop()",
        "",
        "    walk(0)",
        "    return res"
      ],
      "time": "O(n * 2^n)",
      "space": "O(n)",
      "why": "There are n - 1 gaps to cut or not, so 2^(n-1) candidate partitions, and the palindrome test on each piece adds the factor of n. `cur` and the recursion are each at most n deep.",
      "trap": "The palindrome check is not free: `piece == piece[::-1]` builds a reversed copy and costs its length. Precomputing an is-palindrome table (O(n^2) once) moves that cost out of the exponential part of the search."
    },
    {
      "id": "subsets-ii",
      "lc": 90,
      "topic": "Backtracking",
      "vars": "n = len(nums)",
      "note": "Here the output counts.",
      "code": [
        "def subsetsWithDup(nums):",
        "    nums.sort()",
        "    res, cur = [], []",
        "",
        "    def walk(i):",
        "        res.append(cur[:])",
        "        for j in range(i, len(nums)):",
        "            if j > i and nums[j] == nums[j - 1]:",
        "                continue",
        "            cur.append(nums[j])",
        "            walk(j + 1)",
        "            cur.pop()",
        "",
        "    walk(0)",
        "    return res"
      ],
      "time": "O(n * 2^n)",
      "space": "O(n * 2^n)",
      "why": "With all elements distinct there are 2^n subsets and each is copied at a cost of its length, so the sort's O(n log n) vanishes into the exponential.",
      "trap": "The dedup line does not improve the bound — it only helps on inputs that actually contain duplicates, and the worst case has none. Pruning that does not change the worst case does not change the complexity."
    },
    {
      "id": "combination-sum-ii",
      "lc": 40,
      "topic": "Backtracking",
      "vars": "n = len(candidates)",
      "note": "Ignore the combinations collected in `res`.",
      "code": [
        "def combinationSum2(candidates, target):",
        "    candidates.sort()",
        "    res, cur = [], []",
        "",
        "    def walk(i, left):",
        "        if left == 0:",
        "            res.append(cur[:])",
        "            return",
        "        for j in range(i, len(candidates)):",
        "            if j > i and candidates[j] == candidates[j - 1]:",
        "                continue",
        "            if candidates[j] > left:",
        "                break",
        "            cur.append(candidates[j])",
        "            walk(j + 1, left - candidates[j])",
        "            cur.pop()",
        "",
        "    walk(0, target)",
        "    return res"
      ],
      "time": "O(n * 2^n)",
      "space": "O(n)",
      "why": "Each candidate is in or out of a combination, so the search tree is 2^n in the worst case, with O(n) to copy each hit. The stack and `cur` are n deep.",
      "trap": "The `break` on `candidates[j] > left` prunes hard in practice — on a sorted list every later candidate is also too big — but it depends on the values, not on n, so the worst case is untouched. Real speedups and complexity classes are different questions."
    },
    {
      "id": "counting-bits",
      "lc": 338,
      "topic": "Bit Manipulation",
      "vars": "n = the input number",
      "note": "Count the returned list — it is also the table the loop reads back.",
      "code": [
        "def countBits(n):",
        "    dp = [0] * (n + 1)",
        "    for i in range(1, n + 1):",
        "        dp[i] = dp[i >> 1] + (i & 1)",
        "    return dp"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "`i >> 1` is i with its last bit dropped, and that answer is already in the table, so each entry costs one lookup and one add.",
      "trap": "`bin(i).count(\"1\")` per number is O(n log n) — every number has up to log n bits and `bin` builds a string for each. What removes the log is not a bit trick but a recurrence: the shift lands on a subproblem already solved. The bit work is the one `& 1`; the rest is DP."
    },
    {
      "id": "missing-number-xor",
      "lc": 268,
      "topic": "Bit Manipulation",
      "vars": "n = len(nums)",
      "code": [
        "def missingNumber(nums):",
        "    out = len(nums)",
        "    for i, x in enumerate(nums):",
        "        out ^= i ^ x",
        "    return out"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Every index 0..n and every value appears twice in the xor except the missing one, and `a ^ a == 0`.",
      "trap": "Gauss's `n * (n + 1) // 2 - sum(nums)` is the same O(n) and O(1) — but it can overflow in a fixed-width language, which is why xor is the answer usually wanted. A `set` is also O(n) time, and O(n) space for nothing."
    },
    {
      "id": "reverse-bits",
      "lc": 190,
      "topic": "Bit Manipulation",
      "vars": "n = the integer, b = its bit width (32 here)",
      "code": [
        "def reverseBits(n):",
        "    out = 0",
        "    for _ in range(32):",
        "        out = (out << 1) | (n & 1)",
        "        n >>= 1",
        "    return out"
      ],
      "time": "O(1)",
      "space": "O(1)",
      "accept": { "time": ["O(b)"] },
      "why": "Exactly 32 iterations, whatever the input. The width is fixed by the problem, so the loop count is a constant.",
      "trap": "O(log n) is the reflex from \"it shifts until n is zero\" — but this loop does not stop early, and even if it did, the width is capped. Stated over a general word width the honest answer is O(b)."
    },
    {
      "id": "time-based-kv-store",
      "lc": 981,
      "topic": "Design",
      "vars": "n = values stored under one key",
      "note": "Time is for one `get` call.",
      "code": [
        "class TimeMap:",
        "    def __init__(self):",
        "        self.store = defaultdict(list)      # key -> [(timestamp, value)]",
        "",
        "    def set(self, key, value, timestamp):",
        "        self.store[key].append((timestamp, value))",
        "",
        "    def get(self, key, timestamp):",
        "        items = self.store[key]",
        "        lo, hi = 0, len(items)",
        "        while lo < hi:",
        "            mid = (lo + hi) // 2",
        "            if items[mid][0] <= timestamp:",
        "                lo = mid + 1",
        "            else:",
        "                hi = mid",
        "        return items[lo - 1][1] if lo else \"\""
      ],
      "time": "O(log n)",
      "space": "O(n)",
      "why": "Binary search for the last timestamp at or before the one asked for. It is legal because timestamps arrive strictly increasing, so the list is sorted by construction.",
      "trap": "`set` being O(1) is the part that depends on that guarantee — if timestamps could arrive out of order, every write would need an insertion into the middle at O(n). Scanning backwards from the end is the O(n)-per-get version people write first."
    },
    {
      "id": "queue-using-stacks",
      "lc": 232,
      "topic": "Design",
      "vars": "n = items currently held",
      "note": "Time is the amortised cost of one push / pop / peek.",
      "code": [
        "class MyQueue:",
        "    def __init__(self):",
        "        self.inbox = []",
        "        self.outbox = []",
        "",
        "    def push(self, x):",
        "        self.inbox.append(x)",
        "",
        "    def pop(self):",
        "        self.peek()",
        "        return self.outbox.pop()",
        "",
        "    def peek(self):",
        "        if not self.outbox:",
        "            while self.inbox:",
        "                self.outbox.append(self.inbox.pop())",
        "        return self.outbox[-1]"
      ],
      "time": "O(1)",
      "space": "O(n)",
      "why": "One `peek` can move n items, but an element is transferred from inbox to outbox exactly once in its life. Over any sequence of operations the total work is linear, so each one averages a constant.",
      "trap": "O(n) is the right answer to a different question — the worst case for a *single* pop. Amortised and worst-case genuinely differ here, which is why the problem states the amortised requirement explicitly. Refilling the outbox while it is non-empty would break the ordering *and* the bound."
    },
    {
      "id": "range-sum-2d-immutable",
      "lc": 304,
      "topic": "Design",
      "vars": "m, n = matrix dimensions",
      "note": "Time is for one `sumRegion` call; the constructor is separate.",
      "code": [
        "class NumMatrix:",
        "    def __init__(self, matrix):",
        "        m, n = len(matrix), len(matrix[0])",
        "        self.pre = [[0] * (n + 1) for _ in range(m + 1)]",
        "        for i in range(m):",
        "            for j in range(n):",
        "                self.pre[i + 1][j + 1] = (matrix[i][j] + self.pre[i][j + 1]",
        "                                          + self.pre[i + 1][j] - self.pre[i][j])",
        "",
        "    def sumRegion(self, r1, c1, r2, c2):",
        "        p = self.pre",
        "        return (p[r2 + 1][c2 + 1] - p[r1][c2 + 1]",
        "                - p[r2 + 1][c1] + p[r1][c1])"
      ],
      "time": "O(1)",
      "space": "O(m * n)",
      "why": "Every query is inclusion-exclusion over four corners of the prefix-sum table. The table is one row and one column larger than the matrix, which is what removes the boundary cases.",
      "trap": "The constructor is O(m * n) and that is the deal being struck — pay once at build time so that any number of queries afterwards are four lookups. Summing the rectangle on demand is O(m * n) *per query*, and \"Immutable\" in the title is the hint that precomputation is safe."
    },
    {
      "id": "word-dictionary-wildcard",
      "lc": 211,
      "topic": "Tries",
      "vars": "L = len(word), t = nodes in the trie",
      "note": "Time is for one `search` call.",
      "code": [
        "class WordDictionary:",
        "    def __init__(self):",
        "        self.root = {}",
        "",
        "    def addWord(self, word):",
        "        node = self.root",
        "        for ch in word:",
        "            node = node.setdefault(ch, {})",
        "        node[\"#\"] = True",
        "",
        "    def search(self, word):",
        "        def walk(node, i):",
        "            if i == len(word):",
        "                return \"#\" in node",
        "            if word[i] == \".\":",
        "                return any(walk(kid, i + 1)",
        "                           for ch, kid in node.items() if ch != \"#\")",
        "            return word[i] in node and walk(node[word[i]], i + 1)",
        "",
        "        return walk(self.root, 0)"
      ],
      "time": "O(26^L)",
      "space": "O(L)",
      "accept": { "time": ["O(t)"] },
      "why": "A `.` forks into every child, so a word of all dots branches up to 26 ways per level. That is also capped by the trie's actual size — it can never visit more than t nodes. The extra space is the recursion, one frame per character.",
      "trap": "With no dots this is plain O(L), the same as LC 208 — the exponential is bought entirely by the wildcard. `addWord` is O(L) regardless, which is why the cost was pushed onto search."
    },
    {
      "id": "word-search-ii-trie",
      "lc": 212,
      "topic": "Tries",
      "vars": "m, n = board dimensions, L = longest word, k = total characters in words",
      "code": [
        "def findWords(board, words):",
        "    trie = {}",
        "    for w in words:",
        "        node = trie",
        "        for ch in w:",
        "            node = node.setdefault(ch, {})",
        "        node[\"#\"] = w",
        "",
        "    m, n = len(board), len(board[0])",
        "    found = []",
        "",
        "    def dfs(i, j, node):",
        "        nxt = node.get(board[i][j])",
        "        if not nxt:",
        "            return",
        "        word = nxt.pop(\"#\", None)",
        "        if word:",
        "            found.append(word)",
        "        ch, board[i][j] = board[i][j], \"*\"",
        "        for x, y in ((i+1, j), (i-1, j), (i, j+1), (i, j-1)):",
        "            if 0 <= x < m and 0 <= y < n:",
        "                dfs(x, y, nxt)",
        "        board[i][j] = ch",
        "",
        "    for i in range(m):",
        "        for j in range(n):",
        "            dfs(i, j, trie)",
        "    return found"
      ],
      "time": "O(m * n * 4^L)",
      "space": "O(k)",
      "why": "Every cell can start a walk, and each walk branches four ways per character until the trie runs out — so the *longest word* is the exponent, not the board size. The trie holds one node per distinct prefix, at most k of them.",
      "trap": "Running LC 79's word search once per word is O(w * m * n * 4^L); the trie shares every common prefix so the board is walked once for all of them. `nxt.pop(\"#\")` is the other half — deleting a word as it is found stops it being reported twice and prunes the trie as the search goes."
    },
    {
      "id": "jump-game",
      "lc": 55,
      "topic": "Greedy",
      "vars": "n = len(nums)",
      "code": [
        "def canJump(nums):",
        "    reach = 0",
        "    for i, jump in enumerate(nums):",
        "        if i > reach:",
        "            return False",
        "        reach = max(reach, i + jump)",
        "    return True"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "One pass carrying the furthest index reachable so far. If the scan ever reaches an index past that, nothing behind it can bridge the gap.",
      "trap": "The DP over \"can index j be reached from index i\" is O(n^2), and the backtracking version is exponential. One integer replaces the whole table because reachability here is a prefix property — anything up to `reach` is reachable, no exceptions to track."
    },
    {
      "id": "jump-game-ii",
      "lc": 45,
      "topic": "Greedy",
      "vars": "n = len(nums)",
      "code": [
        "def jump(nums):",
        "    jumps = end = farthest = 0",
        "    for i in range(len(nums) - 1):",
        "        farthest = max(farthest, i + nums[i])",
        "        if i == end:",
        "            jumps += 1",
        "            end = farthest",
        "    return jumps"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "This is a BFS over levels — `end` is the last index of the current level, `farthest` the last index of the next — but the levels are tracked with two integers, so there is no queue and no index is visited twice.",
      "trap": "A literal BFS with a queue is O(n^2) unless you are careful not to re-enqueue, and the DP is O(n^2) as well. Recognising the level structure is what collapses it; stopping at `len(nums) - 1` is what stops it counting one jump too many."
    },
    {
      "id": "gas-station",
      "lc": 134,
      "topic": "Greedy",
      "vars": "n = number of stations",
      "code": [
        "def canCompleteCircuit(gas, cost):",
        "    if sum(gas) < sum(cost):",
        "        return -1",
        "    start = tank = 0",
        "    for i in range(len(gas)):",
        "        tank += gas[i] - cost[i]",
        "        if tank < 0:",
        "            start = i + 1",
        "            tank = 0",
        "    return start"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Two linear sums plus one linear sweep. When the tank goes negative at station i, no station between `start` and i can be a valid start either, so the scan skips them all at once instead of retrying each.",
      "trap": "Simulating a full lap from every station is the O(n^2) version, and it is what the problem looks like it needs. The total-gas check up front is what guarantees the single surviving candidate is actually a solution — greedy only works here because existence is settled separately."
    },
    {
      "id": "partition-labels",
      "lc": 763,
      "topic": "Greedy",
      "vars": "n = len(s); s is lowercase a-z",
      "note": "Ignore the returned list of sizes.",
      "code": [
        "def partitionLabels(s):",
        "    last = {ch: i for i, ch in enumerate(s)}",
        "    res = []",
        "    start = end = 0",
        "    for i, ch in enumerate(s):",
        "        end = max(end, last[ch])",
        "        if i == end:",
        "            res.append(end - start + 1)",
        "            start = i + 1",
        "    return res"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Two passes: record where each letter last appears, then sweep, closing a part the moment the scan reaches the furthest last-appearance seen so far.",
      "trap": "`last` looks like it grows with the string, but it holds one entry per *distinct* character — 26 at most, so it is a constant, not O(n). This is the same reasoning that makes the sliding-window-with-a-counter problems O(1) space."
    },
    {
      "id": "hand-of-straights",
      "lc": 846,
      "topic": "Greedy",
      "vars": "n = len(hand), g = groupSize",
      "code": [
        "def isNStraightHand(hand, groupSize):",
        "    if len(hand) % groupSize:",
        "        return False",
        "    count = Counter(hand)",
        "    for card in sorted(count):",
        "        need = count[card]",
        "        if need <= 0:",
        "            continue",
        "        for x in range(card, card + groupSize):",
        "            if count[x] < need:",
        "                return False",
        "            count[x] -= need",
        "    return True"
      ],
      "time": "O(n log n)",
      "space": "O(n)",
      "why": "The sort dominates. The nested loop is not a second factor: each pass consumes `need * g` cards, so at most n/g passes of g steps ever run — O(n) in total.",
      "trap": "O(n * g) is the reading the code invites, and the counting argument is what rules it out. The greedy itself only works because the *smallest* remaining card has no choice — it must start a group — which is why the loop runs over `sorted(count)`."
    },
    {
      "id": "insert-interval",
      "lc": 57,
      "topic": "Intervals",
      "vars": "n = len(intervals)",
      "note": "Ignore the returned list.",
      "code": [
        "def insert(intervals, newInterval):",
        "    res = []",
        "    start, end = newInterval",
        "    i, n = 0, len(intervals)",
        "    while i < n and intervals[i][1] < start:",
        "        res.append(intervals[i])",
        "        i += 1",
        "    while i < n and intervals[i][0] <= end:",
        "        start = min(start, intervals[i][0])",
        "        end = max(end, intervals[i][1])",
        "        i += 1",
        "    res.append([start, end])",
        "    while i < n:",
        "        res.append(intervals[i])",
        "        i += 1",
        "    return res"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Three loops that share one index `i`, which only moves forward — so it is one pass split into before, overlapping, and after.",
      "trap": "Appending the new interval and re-running LC 56's merge is O(n log n): it re-sorts a list that was already sorted. The input's sortedness is the thing being exploited, and it is easy to throw away. Binary-searching for the first overlap makes the *scan* O(log n) but the copy is still linear."
    },
    {
      "id": "non-overlapping-intervals",
      "lc": 435,
      "topic": "Intervals",
      "vars": "n = len(intervals)",
      "code": [
        "def eraseOverlapIntervals(intervals):",
        "    intervals.sort(key=lambda iv: iv[1])",
        "    removed = 0",
        "    end = float(\"-inf\")",
        "    for start, finish in intervals:",
        "        if start >= end:",
        "            end = finish",
        "        else:",
        "            removed += 1",
        "    return removed"
      ],
      "time": "O(n log n)",
      "space": "O(n)",
      "accept": { "space": ["O(1)", "O(log n)"] },
      "why": "The sort dominates; the sweep is one linear pass. Space is whatever the sort needs — Timsort is O(n), an in-place sort would be O(log n).",
      "trap": "Sorting by *end* is what makes the greedy correct: keeping the interval that frees up soonest leaves the most room for the rest. Sorting by start needs a different rule (drop the longer of an overlapping pair) and is where wrong answers come from — but either way the sort, not the sweep, sets the bound."
    },
    {
      "id": "meeting-rooms",
      "lc": 252,
      "topic": "Intervals",
      "vars": "n = len(intervals)",
      "code": [
        "def canAttendMeetings(intervals):",
        "    intervals.sort()",
        "    for i in range(1, len(intervals)):",
        "        if intervals[i][0] < intervals[i - 1][1]:",
        "            return False",
        "    return True"
      ],
      "time": "O(n log n)",
      "space": "O(n)",
      "accept": { "space": ["O(1)", "O(log n)"] },
      "why": "Sort by start, then compare each meeting with the one before it. After sorting, only neighbours can conflict — an earlier meeting that overlapped would have to overlap the neighbour too.",
      "trap": "Comparing every pair is O(n^2) and is the direct reading of the question. The sort is not there to be tidy; it is what reduces \"does any pair overlap\" to \"does any adjacent pair overlap\"."
    },
    {
      "id": "sort-colors",
      "lc": 75,
      "topic": "Sorting",
      "vars": "n = len(nums)",
      "code": [
        "def sortColors(nums):",
        "    lo, i, hi = 0, 0, len(nums) - 1",
        "    while i <= hi:",
        "        if nums[i] == 0:",
        "            nums[lo], nums[i] = nums[i], nums[lo]",
        "            lo += 1",
        "            i += 1",
        "        elif nums[i] == 2:",
        "            nums[hi], nums[i] = nums[i], nums[hi]",
        "            hi -= 1",
        "        else:",
        "            i += 1"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Dutch national flag: every iteration either advances `i` or lowers `hi`, and they start n apart, so the loop runs at most n times. Three values means no comparison sort is needed.",
      "trap": "`i` deliberately does *not* advance after a swap with `hi` — the value pulled in from the right has not been examined. That does not cost the linear bound, because `hi` moves down on exactly those steps. The counting-sort two-pass is the same O(n) but touches every element twice."
    },
    {
      "id": "quickselect-kth-largest",
      "lc": 215,
      "topic": "Sorting",
      "vars": "n = len(nums)",
      "note": "Answer for the expected case, with a random pivot.",
      "code": [
        "def findKthLargest(nums, k):",
        "    target = len(nums) - k",
        "",
        "    def select(lo, hi):",
        "        pivot = nums[random.randint(lo, hi)]",
        "        lt, i, gt = lo, lo, hi",
        "        while i <= gt:                       # three-way partition",
        "            if nums[i] < pivot:",
        "                nums[lt], nums[i] = nums[i], nums[lt]",
        "                lt += 1",
        "                i += 1",
        "            elif nums[i] > pivot:",
        "                nums[gt], nums[i] = nums[i], nums[gt]",
        "                gt -= 1",
        "            else:",
        "                i += 1",
        "        if target < lt:",
        "            return select(lo, lt - 1)",
        "        if target > gt:",
        "            return select(gt + 1, hi)",
        "        return nums[target]",
        "",
        "    return select(0, len(nums) - 1)"
      ],
      "time": "O(n)",
      "space": "O(log n)",
      "accept": { "space": ["O(1)"] },
      "why": "Each partition is linear and, on average, throws away half the range — n + n/2 + n/4 + ... sums to 2n. Unlike quicksort, only *one* side is recursed into, which is what removes the log factor from the time.",
      "trap": "O(n log n) is the answer for sorting, and this does strictly less work. The worst case is O(n^2) when every pivot is an extreme — the random pivot is what makes the linear bound *expected* rather than guaranteed. Space is the recursion: log n frames on average, since Python has no tail-call elimination."
    },
    {
      "id": "rotate-image",
      "lc": 48,
      "topic": "Math & Geometry",
      "vars": "n = the matrix side",
      "code": [
        "def rotate(matrix):",
        "    n = len(matrix)",
        "    for i in range(n):",
        "        for j in range(i + 1, n):",
        "            matrix[i][j], matrix[j][i] = matrix[j][i], matrix[i][j]",
        "    for row in matrix:",
        "        row.reverse()"
      ],
      "time": "O(n^2)",
      "space": "O(1)",
      "why": "Transpose touches the ~n^2/2 cells above the diagonal, then each row is reversed in place. Constants drop, and nothing is allocated.",
      "trap": "n^2 here is *linear in the size of the input* — the matrix holds n^2 numbers and every one has to move. The one-liner `matrix[:] = zip(*matrix[::-1])` is the same time but O(n^2) space, and `j` starting at `i + 1` is what stops the transpose swapping every pair twice and undoing itself."
    },
    {
      "id": "spiral-matrix",
      "lc": 54,
      "topic": "Math & Geometry",
      "vars": "m, n = matrix dimensions",
      "note": "Ignore the returned list.",
      "code": [
        "def spiralOrder(matrix):",
        "    res = []",
        "    top, bottom = 0, len(matrix) - 1",
        "    left, right = 0, len(matrix[0]) - 1",
        "    while top <= bottom and left <= right:",
        "        for j in range(left, right + 1):",
        "            res.append(matrix[top][j])",
        "        top += 1",
        "        for i in range(top, bottom + 1):",
        "            res.append(matrix[i][right])",
        "        right -= 1",
        "        if top <= bottom:",
        "            for j in range(right, left - 1, -1):",
        "                res.append(matrix[bottom][j])",
        "            bottom -= 1",
        "        if left <= right:",
        "            for i in range(bottom, top - 1, -1):",
        "                res.append(matrix[i][left])",
        "            left += 1",
        "    return res"
      ],
      "time": "O(m * n)",
      "space": "O(1)",
      "why": "Four loops inside a while, but every cell is appended exactly once — the four boundaries close in and never cross back. Only the four bounds are kept.",
      "trap": "Loops inside a loop are not automatically multiplied; here they partition the work rather than repeat it. The two `if` guards are what stop a single leftover row or column being emitted twice on the last turn — a correctness bug, not a complexity one."
    },
    {
      "id": "set-matrix-zeroes",
      "lc": 73,
      "topic": "Math & Geometry",
      "vars": "m, n = matrix dimensions",
      "code": [
        "def setZeroes(matrix):",
        "    m, n = len(matrix), len(matrix[0])",
        "    first_col = any(matrix[i][0] == 0 for i in range(m))",
        "    for i in range(m):",
        "        for j in range(1, n):",
        "            if matrix[i][j] == 0:",
        "                matrix[i][0] = 0",
        "                matrix[0][j] = 0",
        "    for i in range(m - 1, -1, -1):",
        "        for j in range(n - 1, 0, -1):",
        "            if matrix[i][0] == 0 or matrix[0][j] == 0:",
        "                matrix[i][j] = 0",
        "        if first_col:",
        "            matrix[i][0] = 0"
      ],
      "time": "O(m * n)",
      "space": "O(1)",
      "why": "Two sweeps over the grid. Row 0 and column 0 are reused as the record of which rows and columns must be cleared, so no extra structure is allocated.",
      "trap": "The natural solution keeps two sets of indices — O(m + n) space, and the follow-up asks you to beat it. Using the first row and column as those sets costs one flag (`first_col`) to disambiguate their shared corner, and forces the second sweep to run *backwards* so the markers are read before they are overwritten."
    },
    {
      "id": "pow-fast-exponentiation",
      "lc": 50,
      "topic": "Math & Geometry",
      "vars": "n = the exponent",
      "code": [
        "def myPow(x, n):",
        "    if n < 0:",
        "        x, n = 1 / x, -n",
        "",
        "    def power(base, exp):",
        "        if exp == 0:",
        "            return 1.0",
        "        half = power(base, exp // 2)",
        "        return half * half * (base if exp % 2 else 1)",
        "",
        "    return power(x, n)"
      ],
      "time": "O(log n)",
      "space": "O(log n)",
      "why": "The exponent halves every call, so there are log n calls, each doing one multiplication. `half` is computed once and squared — calling `power` twice would be the same recursion written as O(n).",
      "trap": "Multiplying x by itself n times is O(n), and that is the loop most people write. The space is not O(1) either: log n frames sit on the stack. The iterative version — square the base while shifting the exponent right — is the O(1)-space equivalent."
    },
    {
      "id": "happy-number",
      "lc": 202,
      "topic": "Math & Geometry",
      "vars": "n = the input number",
      "code": [
        "def isHappy(n):",
        "    seen = set()",
        "    while n != 1 and n not in seen:",
        "        seen.add(n)",
        "        n = sum(int(d) ** 2 for d in str(n))",
        "    return n == 1"
      ],
      "time": "O(log n)",
      "space": "O(log n)",
      "accept": { "time": ["O(1)"], "space": ["O(1)"] },
      "why": "One step costs the digit count, which is log n. The first step is also the expensive one: any number below 10^k maps to at most 81k, so after a single step the value is stuck in a small fixed range and the loop can only run a constant number of times more.",
      "trap": "The `while` reads as unbounded — nothing obviously shrinks. The bound comes from the arithmetic, not the code: the digit-square sum collapses any large number immediately, so a cycle must appear almost at once. Floyd's tortoise and hare replaces the set and gets space down to O(1)."
    },
    {
      "id": "search-2d-matrix-ii",
      "lc": 240,
      "topic": "Math & Geometry",
      "vars": "m, n = matrix dimensions",
      "code": [
        "def searchMatrix(matrix, target):",
        "    i, j = 0, len(matrix[0]) - 1        # start at the top-right",
        "    while i < len(matrix) and j >= 0:",
        "        if matrix[i][j] == target:",
        "            return True",
        "        if matrix[i][j] > target:",
        "            j -= 1",
        "        else:",
        "            i += 1",
        "    return False"
      ],
      "time": "O(m + n)",
      "space": "O(1)",
      "why": "The top-right corner is the largest in its row and the smallest in its column, so every comparison eliminates a whole row or a whole column. At most m + n steps before the walk falls off the grid.",
      "trap": "This is not a binary search, and it still beats one: binary-searching each row is O(m log n). Unlike LC 74 the rows do not chain into one sorted sequence, so a single binary search over m * n is simply wrong here — the staircase is what replaces it."
    },
    {
      "id": "next-permutation",
      "lc": 31,
      "topic": "Greedy",
      "vars": "n = len(nums)",
      "code": [
        "def nextPermutation(nums):",
        "    i = len(nums) - 2",
        "    while i >= 0 and nums[i] >= nums[i + 1]:",
        "        i -= 1",
        "    if i >= 0:",
        "        j = len(nums) - 1",
        "        while nums[j] <= nums[i]:",
        "            j -= 1",
        "        nums[i], nums[j] = nums[j], nums[i]",
        "",
        "    lo, hi = i + 1, len(nums) - 1",
        "    while lo < hi:",
        "        nums[lo], nums[hi] = nums[hi], nums[lo]",
        "        lo += 1",
        "        hi -= 1"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Three scans that never revisit: find the pivot from the right, find the smallest value above it, reverse the tail. Each is at most n steps and all of it happens in place.",
      "trap": "Generating permutations in order until you pass the current one is factorial. The suffix after the pivot is *always* descending — that is the invariant the first loop establishes — so \"sort the tail ascending\" is only a reverse, which is why this is O(n) and not O(n log n)."
    },
    {
      "id": "valid-parenthesis-string",
      "lc": 678,
      "topic": "Greedy",
      "vars": "n = len(s)",
      "code": [
        "def checkValidString(s):",
        "    low = high = 0",
        "    for ch in s:",
        "        low += 1 if ch == \"(\" else -1",
        "        high += 1 if ch in \"(*\" else -1",
        "        if high < 0:",
        "            return False",
        "        low = max(low, 0)",
        "    return low == 0"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Rather than branching on each `*`, carry the *range* of open counts still possible. One pass, two integers.",
      "trap": "Trying all three meanings of every `*` is O(3^n), and memoising that gives an O(n^2) DP. Tracking an interval instead of a set of states is what collapses it to O(1) — legal only because the reachable counts always form a contiguous range, which is worth convincing yourself of before trusting it."
    },
    {
      "id": "candy",
      "lc": 135,
      "topic": "Greedy",
      "vars": "n = len(ratings)",
      "code": [
        "def candy(ratings):",
        "    n = len(ratings)",
        "    give = [1] * n",
        "    for i in range(1, n):",
        "        if ratings[i] > ratings[i - 1]:",
        "            give[i] = give[i - 1] + 1",
        "    for i in range(n - 2, -1, -1):",
        "        if ratings[i] > ratings[i + 1]:",
        "            give[i] = max(give[i], give[i + 1] + 1)",
        "    return sum(give)"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "Each side of the constraint gets its own sweep, and the `max` on the second keeps the first sweep's guarantee intact. Two passes and a sum.",
      "trap": "Sweeping repeatedly until nothing changes is what people write first, and it is O(n^2) on a long descending run. Splitting a two-sided constraint into two one-sided passes is the reusable idea here — the O(1)-space version (count the up- and down-slopes) exists but is far easier to get wrong."
    },
    {
      "id": "design-twitter",
      "lc": 355,
      "topic": "Heap",
      "vars": "f = accounts the user follows",
      "note": "Time is for one `getNewsFeed` call.",
      "code": [
        "class Twitter:",
        "    def __init__(self):",
        "        self.clock = 0",
        "        self.tweets = defaultdict(list)      # user -> [(time, tweet)]",
        "        self.following = defaultdict(set)",
        "",
        "    def postTweet(self, userId, tweetId):",
        "        self.clock += 1",
        "        self.tweets[userId].append((self.clock, tweetId))",
        "",
        "    def follow(self, followerId, followeeId):",
        "        self.following[followerId].add(followeeId)",
        "",
        "    def getNewsFeed(self, userId):",
        "        heap = []",
        "        for uid in self.following[userId] | {userId}:",
        "            for item in self.tweets[uid][-10:]:",
        "                heapq.heappush(heap, item)",
        "                if len(heap) > 10:",
        "                    heapq.heappop(heap)",
        "        return [tweet for _, tweet in sorted(heap, reverse=True)]"
      ],
      "time": "O(f)",
      "space": "O(1)",
      "why": "Only the last 10 tweets per followee can possibly make a feed of 10, so the work is 10f pushes into a heap capped at 10 — both of those are constants. The cost tracks the followee count, not how many tweets exist.",
      "trap": "O(n log n) over every tweet is the answer if you merge the timelines wholesale. The two caps — 10 per author, 10 in the heap — are what make a feed's cost independent of the site's size, and they are the actual design content of the problem rather than an optimisation on top of it."
    },
    {
      "id": "ipo-two-heaps",
      "lc": 502,
      "topic": "Heap",
      "vars": "n = len(profits), k = projects allowed",
      "code": [
        "def findMaximizedCapital(k, w, profits, capital):",
        "    projects = sorted(zip(capital, profits))",
        "    heap = []",
        "    i = 0",
        "    for _ in range(k):",
        "        while i < len(projects) and projects[i][0] <= w:",
        "            heapq.heappush(heap, -projects[i][1])",
        "            i += 1",
        "        if not heap:",
        "            break",
        "        w -= heapq.heappop(heap)",
        "    return w"
      ],
      "time": "O(n log n + k log n)",
      "space": "O(n)",
      "why": "Sort once by capital. Each project is pushed at most once, as it becomes affordable, and each of the k rounds pops the best one available. The `while` inside the `for` is amortised — `i` never moves backwards.",
      "trap": "O(k * n) is what re-scanning for the best affordable project each round would cost. The reason a one-way pointer is sound: `w` only ever grows, so a project that becomes affordable stays affordable and never has to be reconsidered from the start."
    },
    {
      "id": "reorganize-string",
      "lc": 767,
      "topic": "Heap",
      "vars": "n = len(s); s is lowercase a-z",
      "code": [
        "def reorganizeString(s):",
        "    heap = [(-count, ch) for ch, count in Counter(s).items()]",
        "    heapq.heapify(heap)",
        "    out = []",
        "    prev = None",
        "    while heap:",
        "        count, ch = heapq.heappop(heap)",
        "        out.append(ch)",
        "        if prev:",
        "            heapq.heappush(heap, prev)",
        "        prev = (count + 1, ch) if count + 1 else None",
        "    return \"\".join(out) if len(out) == len(s) else \"\""
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "The heap holds one entry per distinct letter — 26 at most — so every push and pop costs log 26, a constant. The loop runs once per output character.",
      "trap": "O(n log n) is the reflex whenever a heap sits inside a loop. The log is over the *heap's size*, and here the alphabet bounds it, not the string. Same reason the space is O(1). Holding the just-used letter back in `prev` is what enforces the no-two-adjacent rule without a second pass."
    },
    {
      "id": "decode-string",
      "lc": 394,
      "topic": "Stack",
      "vars": "n = len(s), d = length of the decoded string",
      "code": [
        "def decodeString(s):",
        "    stack = [([], 1)]",
        "    num = 0",
        "    for ch in s:",
        "        if ch.isdigit():",
        "            num = num * 10 + int(ch)",
        "        elif ch == \"[\":",
        "            stack.append(([], num))",
        "            num = 0",
        "        elif ch == \"]\":",
        "            chunk, times = stack.pop()",
        "            stack[-1][0].extend(chunk * times)",
        "        else:",
        "            stack[-1][0].append(ch)",
        "    return \"\".join(stack[0][0])"
      ],
      "time": "O(n * d)",
      "space": "O(d)",
      "accept": { "time": ["O(n + d)"] },
      "why": "Every `]` copies its chunk up a level, and a chunk is at most d long, so fewer than n closes times d is the safe bound. Ordinarily each output character is produced once and it behaves as O(n + d); deeply nested single-repeat groups like `1[1[1[...]]]` are what force the product.",
      "trap": "O(n) is wrong on its own terms: `3[3[3[3[a]]]]` is thirteen characters of input and eighty-one of output. Any honest bound for this problem is output-sensitive, which is unusual enough among stack problems to be worth noticing."
    },
    {
      "id": "stack-using-queues",
      "lc": 225,
      "topic": "Stack",
      "vars": "n = items currently held",
      "note": "Time is for one `push`; `pop` and `top` are O(1).",
      "code": [
        "class MyStack:",
        "    def __init__(self):",
        "        self.q = deque()",
        "",
        "    def push(self, x):",
        "        self.q.append(x)",
        "        for _ in range(len(self.q) - 1):",
        "            self.q.append(self.q.popleft())",
        "",
        "    def pop(self):",
        "        return self.q.popleft()",
        "",
        "    def top(self):",
        "        return self.q[0]"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "Push rotates the whole queue so the newest element ends up at the front, which is one full lap of the n items held.",
      "trap": "LC 232 — a queue built from two stacks — gets amortised O(1) on every operation, so the reflex is that this direction does too. It cannot: *every* push pays n, so n pushes cost O(n^2) no matter how the accounting is done. The two conversions are not symmetric, and knowing which one is cheap is the point of having both problems."
    },
    {
      "id": "max-freq-stack",
      "lc": 895,
      "topic": "Stack",
      "vars": "n = values pushed",
      "note": "Time is for one push or one pop.",
      "code": [
        "class FreqStack:",
        "    def __init__(self):",
        "        self.freq = Counter()",
        "        self.groups = defaultdict(list)     # frequency -> values, in push order",
        "        self.top = 0",
        "",
        "    def push(self, val):",
        "        self.freq[val] += 1",
        "        self.top = max(self.top, self.freq[val])",
        "        self.groups[self.freq[val]].append(val)",
        "",
        "    def pop(self):",
        "        val = self.groups[self.top].pop()",
        "        self.freq[val] -= 1",
        "        if not self.groups[self.top]:",
        "            self.top -= 1",
        "        return val"
      ],
      "time": "O(1)",
      "space": "O(n)",
      "why": "One stack per frequency level. A value pushed k times sits in groups 1..k, so the group at `top` always holds the most frequent values in push order — no heap, no re-sorting, and ties break correctly for free.",
      "trap": "A max-heap keyed on (frequency, timestamp) is the natural design and costs O(log n) per operation. Reaching O(1) leans on frequencies changing by exactly one: the maximum can only ever step down to `top - 1` when a group empties, so it is tracked with an integer instead of searched for."
    },
    {
      "id": "multiply-strings",
      "lc": 43,
      "topic": "Math & Geometry",
      "vars": "m, n = the two numbers' lengths in digits",
      "code": [
        "def multiply(num1, num2):",
        "    if num1 == \"0\" or num2 == \"0\":",
        "        return \"0\"",
        "    m, n = len(num1), len(num2)",
        "    digits = [0] * (m + n)",
        "    for i in range(m - 1, -1, -1):",
        "        for j in range(n - 1, -1, -1):",
        "            total = int(num1[i]) * int(num2[j]) + digits[i + j + 1]",
        "            digits[i + j + 1] = total % 10",
        "            digits[i + j] += total // 10",
        "    return \"\".join(map(str, digits)).lstrip(\"0\")"
      ],
      "time": "O(m * n)",
      "space": "O(m + n)",
      "why": "Long multiplication as taught at school: every digit of one number meets every digit of the other, and the product needs at most m + n digit slots.",
      "trap": "This is the bound in *digits*, not in the values — the whole premise of the problem is that the numbers do not fit in a machine word, so a single `*` is not O(1). Karatsuba does the same job in about O(n^1.585), which is where \"multiplication is constant time\" stops being a safe assumption."
    },
    {
      "id": "longest-common-prefix",
      "lc": 14,
      "topic": "Arrays & Hashing",
      "vars": "n = number of strings, s = total characters across them",
      "code": [
        "def longestCommonPrefix(strs):",
        "    first = strs[0]",
        "    for i in range(len(first)):",
        "        for j in range(1, len(strs)):",
        "            if i == len(strs[j]) or strs[j][i] != first[i]:",
        "                return first[:i]",
        "    return first"
      ],
      "time": "O(s)",
      "space": "O(1)",
      "why": "Column by column, stopping at the first mismatch. In the worst case — every string identical — it reads all s characters and never more, since the scan cannot outrun the shortest string.",
      "trap": "O(n), the number of strings, is the instinctive answer and it is the wrong variable. It is also not n * m: one very long string among short ones does not multiply the work, because the loop stops at the shortest. Sums and products are different bounds, and this one is a sum."
    },
    {
      "id": "best-time-stock-ii",
      "lc": 122,
      "topic": "Arrays & Hashing",
      "vars": "n = len(prices)",
      "code": [
        "def maxProfit(prices):",
        "    total = 0",
        "    for i in range(1, len(prices)):",
        "        if prices[i] > prices[i - 1]:",
        "            total += prices[i] - prices[i - 1]",
        "    return total"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "A profitable run of days telescopes into the sum of its daily gains, so adding up every up-step is the same as buying at each valley and selling at each peak.",
      "trap": "Hunting for peaks and valleys works, and so does a hold/free state DP — all three are O(n). The place people lose a mark is the *space*: reaching for the DP and then quoting O(n) for a table that never needed to exist."
    },
    {
      "id": "majority-element-ii",
      "lc": 229,
      "topic": "Arrays & Hashing",
      "vars": "n = len(nums)",
      "code": [
        "def majorityElement(nums):",
        "    n1, c1, n2, c2 = None, 0, None, 0",
        "    for x in nums:",
        "        if x == n1:",
        "            c1 += 1",
        "        elif x == n2:",
        "            c2 += 1",
        "        elif c1 == 0:",
        "            n1, c1 = x, 1",
        "        elif c2 == 0:",
        "            n2, c2 = x, 1",
        "        else:",
        "            c1 -= 1",
        "            c2 -= 1",
        "    return [v for v in (n1, n2)",
        "            if v is not None and nums.count(v) > len(nums) // 3]"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Boyer-Moore with two slots: one pass to find at most two survivors, one to verify them. There can be at most two elements above n/3, which is why two slots suffice.",
      "trap": "The verification pass is not optional — the first pass finds *candidates*, and an array with no element over n/3 still leaves two of them standing. `nums.count(v)` is O(n), but the comprehension runs over two items, not over `nums`; had it been the other way round this would be O(n^2)."
    },
    {
      "id": "encode-decode-strings",
      "lc": 271,
      "topic": "Design",
      "vars": "c = total characters across the strings",
      "note": "Time is for one encode or one decode call.",
      "code": [
        "class Codec:",
        "    def encode(self, strs):",
        "        return \"\".join(f\"{len(s)}#{s}\" for s in strs)",
        "",
        "    def decode(self, s):",
        "        out = []",
        "        i = 0",
        "        while i < len(s):",
        "            j = s.index(\"#\", i)",
        "            size = int(s[i:j])",
        "            out.append(s[j + 1:j + 1 + size])",
        "            i = j + 1 + size",
        "        return out"
      ],
      "time": "O(c)",
      "space": "O(1)",
      "why": "Both directions read each character a constant number of times. The length prefix means decode jumps straight past a payload instead of scanning it for a terminator.",
      "trap": "A plain delimiter is the tempting design and it is not merely slower, it is wrong — the payload can contain the delimiter, so it has to be escaped, and then decode must scan every character looking for an unescaped one. Length-prefixing turns a search into a jump, which is why it is the answer this problem is fishing for."
    },
    {
      "id": "design-hashmap",
      "lc": 706,
      "topic": "Design",
      "vars": "n = keys stored (the table has a fixed 1000 buckets)",
      "note": "Time is for one get / put / remove.",
      "code": [
        "class MyHashMap:",
        "    def __init__(self):",
        "        self.size = 1000",
        "        self.buckets = [[] for _ in range(self.size)]",
        "",
        "    def put(self, key, value):",
        "        bucket = self.buckets[key % self.size]",
        "        for i, (k, _) in enumerate(bucket):",
        "            if k == key:",
        "                bucket[i] = (key, value)",
        "                return",
        "        bucket.append((key, value))",
        "",
        "    def get(self, key):",
        "        for k, v in self.buckets[key % self.size]:",
        "            if k == key:",
        "                return v",
        "        return -1",
        "",
        "    def remove(self, key):",
        "        bucket = self.buckets[key % self.size]",
        "        for i, (k, _) in enumerate(bucket):",
        "            if k == key:",
        "                bucket.pop(i)",
        "                return"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "The bucket count is fixed and the table never resizes, so the chains grow with the keys: n/1000 is still linear in n. It only behaves like O(1) while n stays near the bucket count.",
      "trap": "\"It is a hash map, so O(1)\" is a claim about a table that *resizes*. This one does not. Real hash maps double the bucket array once the load factor passes a threshold, and it is that amortised doubling — not the hashing — that buys the constant time."
    },
    {
      "id": "detect-squares",
      "lc": 2013,
      "topic": "Design",
      "vars": "n = points added so far",
      "note": "Time is for one `count` call; `add` is O(1).",
      "code": [
        "class DetectSquares:",
        "    def __init__(self):",
        "        self.points = Counter()",
        "",
        "    def add(self, point):",
        "        self.points[tuple(point)] += 1",
        "",
        "    def count(self, point):",
        "        px, py = point",
        "        total = 0",
        "        for (x, y), n in self.points.items():",
        "            if abs(x - px) != abs(y - py) or x == px or y == py:",
        "                continue",
        "            total += n * self.points[(x, py)] * self.points[(px, y)]",
        "        return total"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "One scan over the stored points, treating each as the *diagonal* corner. That fixes the square completely, so the other two corners are two hash lookups rather than a search.",
      "trap": "Trying every pair of stored points is O(n^2) per query. Choosing the diagonal rather than an adjacent corner is what drops it to a single pass — an adjacent corner leaves the square's orientation open and puts you back to searching."
    },
    {
      "id": "reverse-integer",
      "lc": 7,
      "topic": "Bit Manipulation",
      "vars": "x = the input value, d = its number of digits",
      "code": [
        "def reverse(x):",
        "    sign = -1 if x < 0 else 1",
        "    x = abs(x)",
        "    out = 0",
        "    while x:",
        "        out = out * 10 + x % 10",
        "        x //= 10",
        "        if out > 2**31 - 1:",
        "            return 0",
        "    return sign * out"
      ],
      "time": "O(log x)",
      "space": "O(1)",
      "accept": { "time": ["O(d)", "O(1)"] },
      "why": "One iteration per digit, and a number x has about log10(x) digits. For a 32-bit input that is at most ten iterations, so a constant.",
      "trap": "O(n) treats the *value* as the input size. The loop count is the digit count — logarithmic in the value, linear in the number of digits you had to write. The overflow check inside the loop is the actual content of the problem, and it costs nothing asymptotically."
    },
    {
      "id": "bitwise-and-range",
      "lc": 201,
      "topic": "Bit Manipulation",
      "vars": "n = the upper bound, b = its bit width",
      "code": [
        "def rangeBitwiseAnd(left, right):",
        "    shift = 0",
        "    while left < right:",
        "        left >>= 1",
        "        right >>= 1",
        "        shift += 1",
        "    return left << shift"
      ],
      "time": "O(log n)",
      "space": "O(1)",
      "accept": { "time": ["O(b)", "O(1)"] },
      "why": "The answer is the common binary prefix of the two bounds: any bit where they differ is zeroed somewhere in the range. Shifting both right until they agree takes one step per bit.",
      "trap": "Looping from `left` to `right` and folding with `&` is O(right - left) — proportional to the *values*, which for a 32-bit range is billions of iterations and a guaranteed timeout. This is the sharpest form of the value-versus-size distinction: log n instead of n, on the same input."
    },
    {
      "id": "reverse-k-group",
      "lc": 25,
      "topic": "Linked List",
      "vars": "n = nodes, k = group size",
      "code": [
        "def reverseKGroup(head, k):",
        "    dummy = ListNode(0, head)",
        "    group_prev = dummy",
        "    while True:",
        "        node = group_prev",
        "        for _ in range(k):                 # is a full group left?",
        "            node = node.next",
        "            if not node:",
        "                return dummy.next",
        "        group_next = node.next",
        "",
        "        prev, cur = group_next, group_prev.next",
        "        while cur is not group_next:",
        "            nxt = cur.next",
        "            cur.next = prev",
        "            prev, cur = cur, nxt",
        "",
        "        tail = group_prev.next",
        "        group_prev.next = prev",
        "        group_prev = tail"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Each node is walked twice — once to check the group is complete, once to reverse it — so 2n steps, and every pointer change is in place.",
      "trap": "Nested loops suggest O(n * k), and they do not multiply here: the outer loop runs n/k times and each pass does O(k) work, so the product is n. The recursive version of this is far shorter but stacks n/k frames; only the iterative one meets the O(1)-space follow-up."
    },
    {
      "id": "intersection-two-lists",
      "lc": 160,
      "topic": "Linked List",
      "vars": "m, n = the two list lengths",
      "code": [
        "def getIntersectionNode(headA, headB):",
        "    a, b = headA, headB",
        "    while a is not b:",
        "        a = a.next if a else headB",
        "        b = b.next if b else headA",
        "    return a"
      ],
      "time": "O(m + n)",
      "space": "O(1)",
      "why": "Each pointer walks its own list and then the other, so both have covered m + n nodes by the time they can meet. Two pointers, nothing stored.",
      "trap": "The loop looks like it might spin forever, and it never does: after the switch both pointers are the same distance from the end, so they either land on the shared node together or hit None together. Putting one list into a set is the same time and O(m) space — this is the version that trades the memory away for a trick."
    },
    {
      "id": "lfu-cache",
      "lc": 460,
      "topic": "Design",
      "vars": "c = capacity",
      "note": "Time is for one get / put.",
      "code": [
        "class LFUCache:",
        "    def __init__(self, capacity):",
        "        self.cap = capacity",
        "        self.vals = {}                            # key -> value",
        "        self.uses = {}                            # key -> use count",
        "        self.buckets = defaultdict(OrderedDict)   # count -> keys, oldest first",
        "        self.least = 0",
        "",
        "    def _bump(self, key):",
        "        n = self.uses[key]",
        "        del self.buckets[n][key]",
        "        if not self.buckets[n]:",
        "            del self.buckets[n]",
        "            if self.least == n:",
        "                self.least = n + 1",
        "        self.uses[key] = n + 1",
        "        self.buckets[n + 1][key] = None",
        "",
        "    def get(self, key):",
        "        if key not in self.vals:",
        "            return -1",
        "        self._bump(key)",
        "        return self.vals[key]",
        "",
        "    def put(self, key, value):",
        "        if self.cap <= 0:",
        "            return",
        "        if key in self.vals:",
        "            self.vals[key] = value",
        "            self._bump(key)",
        "            return",
        "        if len(self.vals) >= self.cap:",
        "            evict, _ = self.buckets[self.least].popitem(last=False)",
        "            if not self.buckets[self.least]:",
        "                del self.buckets[self.least]",
        "            del self.vals[evict]",
        "            del self.uses[evict]",
        "        self.vals[key] = value",
        "        self.uses[key] = 1",
        "        self.buckets[1][key] = None",
        "        self.least = 1"
      ],
      "time": "O(1)",
      "space": "O(c)",
      "why": "One insertion-ordered dict per use count, so \"least frequently used, oldest first\" is a `popitem` from the bucket at `least`. Nothing is ever scanned or re-sorted.",
      "trap": "A heap keyed on (count, timestamp) is the natural design and gives O(log c). Reaching O(1) rests on counts changing by exactly one: when the minimum bucket empties the new minimum can only be `least + 1`, so it is tracked with an integer rather than searched for. Compare LC 146, where one OrderedDict is enough because there is no frequency dimension at all."
    },
    {
      "id": "add-two-numbers",
      "lc": 2,
      "topic": "Linked List",
      "vars": "m, n = the two list lengths",
      "note": "Ignore the returned list.",
      "code": [
        "def addTwoNumbers(l1, l2):",
        "    dummy = tail = ListNode(0)",
        "    carry = 0",
        "    while l1 or l2 or carry:",
        "        total = carry",
        "        if l1:",
        "            total += l1.val",
        "            l1 = l1.next",
        "        if l2:",
        "            total += l2.val",
        "            l2 = l2.next",
        "        carry, digit = divmod(total, 10)",
        "        tail.next = ListNode(digit)",
        "        tail = tail.next",
        "    return dummy.next"
      ],
      "time": "O(m + n)",
      "space": "O(1)",
      "why": "One pass, bounded by the longer list. The `or carry` in the loop condition absorbs a final carry-out without a special case after the loop.",
      "trap": "The result can be one node longer than either input, and it is the *output*, so it does not count against auxiliary space. Reading both lists into integers, adding, and rebuilding is the version that breaks: the constraints allow 100 digits, which no fixed-width integer holds."
    },
    {
      "id": "rotate-array",
      "lc": 189,
      "topic": "Two Pointers & Sliding Window",
      "vars": "n = len(nums), k = the shift",
      "code": [
        "def rotate(nums, k):",
        "    n = len(nums)",
        "    k %= n",
        "",
        "    def flip(lo, hi):",
        "        while lo < hi:",
        "            nums[lo], nums[hi] = nums[hi], nums[lo]",
        "            lo += 1",
        "            hi -= 1",
        "",
        "    flip(0, n - 1)",
        "    flip(0, k - 1)",
        "    flip(k, n - 1)"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Reverse the whole array, then reverse each of the two pieces back. Three passes, about 3n/2 swaps, nothing allocated.",
      "trap": "Rotating by one k times is O(n * k), which is the timeout. `nums[:] = nums[-k:] + nums[:-k]` is O(n) time but O(n) space, and the follow-up asks for O(1). The `k %= n` line matters for the same reason: without it a k larger than the array does whole pointless laps."
    },
    {
      "id": "two-sum-sorted",
      "lc": 167,
      "topic": "Two Pointers & Sliding Window",
      "vars": "n = len(numbers)",
      "code": [
        "def twoSum(numbers, target):",
        "    lo, hi = 0, len(numbers) - 1",
        "    while lo < hi:",
        "        total = numbers[lo] + numbers[hi]",
        "        if total == target:",
        "            return [lo + 1, hi + 1]",
        "        if total < target:",
        "            lo += 1",
        "        else:",
        "            hi -= 1",
        "    return []"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Sorted input means a sum that is too small can only be fixed by raising the low end and one that is too large by lowering the high end, so no pair is ever skipped. The pointers move n steps between them.",
      "trap": "LC 1 is the same O(n) time with a hash map and O(n) space. Sortedness buys the *constant space*, not the linear time — worth being precise about, because the reflex is to assume sorted input must buy a log. Binary-searching each complement would in fact be worse, at O(n log n)."
    },
    {
      "id": "four-sum",
      "lc": 18,
      "topic": "Two Pointers & Sliding Window",
      "vars": "n = len(nums)",
      "note": "Ignore the result list.",
      "code": [
        "def fourSum(nums, target):",
        "    nums.sort()",
        "    n = len(nums)",
        "    res = []",
        "    for i in range(n - 3):",
        "        if i and nums[i] == nums[i - 1]:",
        "            continue",
        "        for j in range(i + 1, n - 2):",
        "            if j > i + 1 and nums[j] == nums[j - 1]:",
        "                continue",
        "            lo, hi = j + 1, n - 1",
        "            while lo < hi:",
        "                total = nums[i] + nums[j] + nums[lo] + nums[hi]",
        "                if total < target:",
        "                    lo += 1",
        "                elif total > target:",
        "                    hi -= 1",
        "                else:",
        "                    res.append([nums[i], nums[j], nums[lo], nums[hi]])",
        "                    while lo < hi and nums[lo] == nums[lo + 1]:",
        "                        lo += 1",
        "                    lo, hi = lo + 1, hi - 1",
        "    return res"
      ],
      "time": "O(n^3)",
      "space": "O(n)",
      "accept": { "space": ["O(1)", "O(log n)"] },
      "why": "Two nested loops fix the first two values, then a linear two-pointer sweep finds the rest — n * n * n. The sort's O(n log n) disappears underneath. Space is whatever the sort needs.",
      "trap": "The k-sum family is O(n^(k-1)): 3Sum is n^2, 4Sum is n^3, and each extra number adds a *loop*, not a factor on the sweep. A hash map over all pairs gets 4Sum to O(n^2) time at O(n^2) space, which is the usual trade one level up."
    },
    {
      "id": "valid-palindrome-ii",
      "lc": 680,
      "topic": "Two Pointers & Sliding Window",
      "vars": "n = len(s)",
      "code": [
        "def validPalindrome(s):",
        "    def is_pal(lo, hi):",
        "        while lo < hi:",
        "            if s[lo] != s[hi]:",
        "                return False",
        "            lo += 1",
        "            hi -= 1",
        "        return True",
        "",
        "    lo, hi = 0, len(s) - 1",
        "    while lo < hi:",
        "        if s[lo] != s[hi]:",
        "            return is_pal(lo + 1, hi) or is_pal(lo, hi - 1)",
        "        lo += 1",
        "        hi -= 1",
        "    return True"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "The outer scan is linear and the branch on a mismatch fires at most once, because both calls sit inside a `return`. Two extra linear checks on top of one linear scan is still linear.",
      "trap": "`is_pal(...) or is_pal(...)` looks like it could fork again at every mismatch, which would be O(2^n). It cannot — the function is finished at that point. Allowing *two* deletions is the version that genuinely branches, and that one needs memoisation to stay under O(n^2)."
    },
    {
      "id": "k-closest-elements",
      "lc": 658,
      "topic": "Two Pointers & Sliding Window",
      "vars": "n = len(arr), k = how many to return",
      "note": "Ignore the returned slice.",
      "code": [
        "def findClosestElements(arr, k, x):",
        "    lo, hi = 0, len(arr) - k",
        "    while lo < hi:",
        "        mid = (lo + hi) // 2",
        "        if x - arr[mid] > arr[mid + k] - x:",
        "            lo = mid + 1",
        "        else:",
        "            hi = mid",
        "    return arr[lo:lo + k]"
      ],
      "time": "O(log n + k)",
      "space": "O(1)",
      "accept": { "time": ["O(log n)"] },
      "why": "The binary search runs over the window's *start position*, not over a value: compare the element about to leave the window with the one about to enter. The k in the bound is only the cost of writing the answer out.",
      "trap": "The two-pointer \"shrink from both ends until k remain\" solution is O(n - k), and sorting by distance is O(n log n). Searching over window positions is what buys the log, and it is a useful reminder that a binary search does not have to be over the array's values."
    },
    {
      "id": "contains-duplicate-ii",
      "lc": 219,
      "topic": "Two Pointers & Sliding Window",
      "vars": "n = len(nums), k = the index distance allowed",
      "code": [
        "def containsNearbyDuplicate(nums, k):",
        "    window = set()",
        "    for i, x in enumerate(nums):",
        "        if i > k:",
        "            window.remove(nums[i - k - 1])",
        "        if x in window:",
        "            return True",
        "        window.add(x)",
        "    return False"
      ],
      "time": "O(n)",
      "space": "O(k)",
      "why": "The set holds only the last k values, so membership is O(1) and the memory is capped by the window rather than by the array.",
      "trap": "The other common solution keeps a dict of value to last index and is O(n) space. The interesting answer here is the *space*: sliding a fixed-size window is what turns O(n) memory into O(k), and it is the only thing separating this from LC 217."
    },
    {
      "id": "perfect-squares",
      "lc": 279,
      "topic": "Dynamic Programming",
      "vars": "n = the input number",
      "code": [
        "def numSquares(n):",
        "    dp = [0] + [float(\"inf\")] * n",
        "    for i in range(1, n + 1):",
        "        j = 1",
        "        while j * j <= i:",
        "            dp[i] = min(dp[i], dp[i - j * j] + 1)",
        "            j += 1",
        "    return dp[n]"
      ],
      "time": "O(n * sqrt(n))",
      "space": "O(n)",
      "why": "n subproblems, and subproblem i tries every square up to i — there are sqrt(i) of those. The table is n + 1 entries.",
      "trap": "O(n^2) is the reflex for a loop inside a loop. The inner one is bounded by sqrt(i), not by i, and that square root is the whole answer. (Lagrange's four-square theorem settles the problem outright in O(sqrt(n)) — the DP is the general shape, not the fastest route.)"
    },
    {
      "id": "regex-matching",
      "lc": 10,
      "topic": "Dynamic Programming",
      "vars": "m = len(s), n = len(p)",
      "code": [
        "def isMatch(s, p):",
        "    m, n = len(s), len(p)",
        "    dp = [[False] * (n + 1) for _ in range(m + 1)]",
        "    dp[0][0] = True",
        "    for j in range(2, n + 1):",
        "        if p[j - 1] == \"*\":",
        "            dp[0][j] = dp[0][j - 2]",
        "    for i in range(1, m + 1):",
        "        for j in range(1, n + 1):",
        "            if p[j - 1] == \"*\":",
        "                dp[i][j] = (dp[i][j - 2]",
        "                            or (dp[i - 1][j] and p[j - 2] in (s[i - 1], \".\")))",
        "            else:",
        "                dp[i][j] = dp[i - 1][j - 1] and p[j - 1] in (s[i - 1], \".\")",
        "    return dp[m][n]"
      ],
      "time": "O(m * n)",
      "space": "O(m * n)",
      "why": "One cell per (prefix of s, prefix of p) pair, each decided in constant time from at most two neighbours.",
      "trap": "The plain recursion reaches the same (i, j) through many different `*` expansions and is exponential — roughly O(2^(m + n)). The table is not a speed-up of the recursion so much as a promise that each state is settled once."
    },
    {
      "id": "interleaving-string",
      "lc": 97,
      "topic": "Dynamic Programming",
      "vars": "m = len(s1), n = len(s2)",
      "code": [
        "def isInterleave(s1, s2, s3):",
        "    m, n = len(s1), len(s2)",
        "    if m + n != len(s3):",
        "        return False",
        "    dp = [False] * (n + 1)",
        "    dp[0] = True",
        "    for j in range(1, n + 1):",
        "        dp[j] = dp[j - 1] and s2[j - 1] == s3[j - 1]",
        "    for i in range(1, m + 1):",
        "        dp[0] = dp[0] and s1[i - 1] == s3[i - 1]",
        "        for j in range(1, n + 1):",
        "            dp[j] = ((dp[j] and s1[i - 1] == s3[i + j - 1])",
        "                     or (dp[j - 1] and s2[j - 1] == s3[i + j - 1]))",
        "    return dp[n]"
      ],
      "time": "O(m * n)",
      "space": "O(n)",
      "why": "The state is the *pair* of prefix lengths, so there are m * n of them. Each row reads only the row above and the cell to its left, so one row is enough to keep.",
      "trap": "Two pointers taking whichever character happens to match is wrong — a tie forces a real branch, and branching without memoisation is O(2^(m + n)). Notice `dp[j]` is read before it is overwritten (that is the row above) and `dp[j - 1]` after (that is this row)."
    },
    {
      "id": "stock-with-cooldown",
      "lc": 309,
      "topic": "Dynamic Programming",
      "vars": "n = len(prices)",
      "code": [
        "def maxProfit(prices):",
        "    hold, sold, rest = float(\"-inf\"), 0, 0",
        "    for p in prices:",
        "        hold, sold, rest = max(hold, rest - p), hold + p, max(rest, sold)",
        "    return max(sold, rest)"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "A three-state machine — holding, just sold, free — advanced once per day. The cooldown is expressed by `rest` reading yesterday's `sold`, so it needs no extra dimension.",
      "trap": "The instinct is a table indexed by day, and then by how many transactions, and then by the cooldown. There are only three states, and each depends only on yesterday's three, so the entire table collapses to three scalars."
    },
    {
      "id": "burst-balloons",
      "lc": 312,
      "topic": "Dynamic Programming",
      "vars": "n = len(nums)",
      "code": [
        "def maxCoins(nums):",
        "    vals = [1] + nums + [1]",
        "    n = len(vals)",
        "    dp = [[0] * n for _ in range(n)]",
        "    for length in range(2, n):",
        "        for lo in range(n - length):",
        "            hi = lo + length",
        "            for k in range(lo + 1, hi):",
        "                dp[lo][hi] = max(dp[lo][hi],",
        "                                 dp[lo][k] + dp[k][hi]",
        "                                 + vals[lo] * vals[k] * vals[hi])",
        "    return dp[0][n - 1]"
      ],
      "time": "O(n^3)",
      "space": "O(n^2)",
      "why": "Interval DP: there are O(n^2) intervals and each one tries every split point inside it. The table is one cell per interval.",
      "trap": "The exponent is 3 because the choice is which balloon to burst *last* in an interval, not first. Asking which to burst first leaves subproblems that are no longer contiguous ranges — nothing you can index a table by — and the search becomes n!."
    },
    {
      "id": "longest-increasing-path-matrix",
      "lc": 329,
      "topic": "Dynamic Programming",
      "vars": "m, n = grid dimensions",
      "code": [
        "def longestIncreasingPath(matrix):",
        "    m, n = len(matrix), len(matrix[0])",
        "    memo = [[0] * n for _ in range(m)]",
        "",
        "    def walk(i, j):",
        "        if memo[i][j]:",
        "            return memo[i][j]",
        "        best = 1",
        "        for x, y in ((i+1, j), (i-1, j), (i, j+1), (i, j-1)):",
        "            if 0 <= x < m and 0 <= y < n and matrix[x][y] > matrix[i][j]:",
        "                best = max(best, 1 + walk(x, y))",
        "        memo[i][j] = best",
        "        return best",
        "",
        "    return max(walk(i, j) for i in range(m) for j in range(n))"
      ],
      "time": "O(m * n)",
      "space": "O(m * n)",
      "why": "Each cell's longest path is computed once and then read from the memo, and each computation looks at four neighbours. Linear in the number of cells.",
      "trap": "A DFS from every cell without the memo is exponential. Notice there is no visited-marking or backtracking either — the strict `>` means a cell can never reach itself, so the search space is a DAG and a plain memo is sound."
    },
    {
      "id": "target-sum",
      "lc": 494,
      "topic": "Dynamic Programming",
      "vars": "n = len(nums), s = sum(nums)",
      "code": [
        "def findTargetSumWays(nums, target):",
        "    ways = {0: 1}",
        "    for x in nums:",
        "        nxt = defaultdict(int)",
        "        for total, count in ways.items():",
        "            nxt[total + x] += count",
        "            nxt[total - x] += count",
        "        ways = nxt",
        "    return ways.get(target, 0)"
      ],
      "time": "O(n * s)",
      "space": "O(s)",
      "why": "Reachable totals live in -s..s, so a layer holds at most 2s + 1 keys, and there are n layers. Only two layers are alive at a time.",
      "trap": "O(2^n) is the naive cost — one branch per sign. The totals *collide*, and merging them is the whole saving. Like LC 416 this is pseudo-polynomial: s is a value, not an input size, so doubling the numbers doubles the work while the input barely grows."
    },
    {
      "id": "circular-subarray-sum",
      "lc": 918,
      "topic": "Dynamic Programming",
      "vars": "n = len(nums)",
      "code": [
        "def maxSubarraySumCircular(nums):",
        "    total = 0",
        "    cur_max = best_max = nums[0]",
        "    cur_min = best_min = nums[0]",
        "    for i, x in enumerate(nums):",
        "        total += x",
        "        cur_max = x if i == 0 else max(x, cur_max + x)",
        "        best_max = max(best_max, cur_max)",
        "        cur_min = x if i == 0 else min(x, cur_min + x)",
        "        best_min = min(best_min, cur_min)",
        "    if best_max < 0:",
        "        return best_max",
        "    return max(best_max, total - best_min)"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "Two Kadane scans fused into one loop. A wrapping subarray is the complement of a non-wrapping one, so the best wrap is the total minus the *minimum* subarray — no second data structure needed.",
      "trap": "Trying every rotation is O(n^2). The `best_max < 0` guard is the case people lose: when every number is negative the complement trick answers with the empty subarray, which is not allowed."
    },
    {
      "id": "combination-sum",
      "lc": 39,
      "topic": "Backtracking",
      "vars": "n = len(candidates), t = target (every candidate is at least 1)",
      "note": "Ignore the combinations collected in `res`.",
      "code": [
        "def combinationSum(candidates, target):",
        "    res, cur = [], []",
        "",
        "    def walk(i, left):",
        "        if left == 0:",
        "            res.append(cur[:])",
        "            return",
        "        for j in range(i, len(candidates)):",
        "            if candidates[j] <= left:",
        "                cur.append(candidates[j])",
        "                walk(j, left - candidates[j])",
        "                cur.pop()",
        "",
        "    walk(0, target)",
        "    return res"
      ],
      "time": "O(n^t)",
      "space": "O(t)",
      "why": "Every pick subtracts at least 1, so the recursion is at most t deep and each level tries at most n candidates. With m the smallest candidate the depth is really t/m and the tighter bound is n^(t/m). The stack and `cur` are both bounded by the depth.",
      "trap": "The exponent is the *target*, not the array length — the same pseudo-polynomial trap as LC 416, wearing backtracking clothes, and the reason the problem caps target at 40. Note `walk(j, ...)` rather than `walk(j + 1, ...)`: a candidate may repeat, which is exactly what makes the depth depend on t at all."
    },
    {
      "id": "generate-parentheses",
      "lc": 22,
      "topic": "Backtracking",
      "vars": "n = pairs of parentheses",
      "note": "Here the output counts.",
      "code": [
        "def generateParenthesis(n):",
        "    res, cur = [], []",
        "",
        "    def build(opened, closed):",
        "        if len(cur) == 2 * n:",
        "            res.append(\"\".join(cur))",
        "            return",
        "        if opened < n:",
        "            cur.append(\"(\")",
        "            build(opened + 1, closed)",
        "            cur.pop()",
        "        if closed < opened:",
        "            cur.append(\")\")",
        "            build(opened, closed + 1)",
        "            cur.pop()",
        "",
        "    build(0, 0)",
        "    return res"
      ],
      "time": "O(4^n)",
      "space": "O(4^n)",
      "why": "The valid strings are counted by the nth Catalan number, about 4^n / n^1.5, and each is 2n characters long. Rounding the polynomial factors away, both the work and the output are O(4^n).",
      "trap": "Generating all 2^(2n) bracket strings and filtering is *also* O(4^n) — the pruning here removes only a polynomial factor, not an exponential one. Pruning that changes n^1.5 does not change the complexity class, however much it changes the clock."
    },
    {
      "id": "word-break-ii",
      "lc": 140,
      "topic": "Backtracking",
      "vars": "n = len(s)",
      "note": "Here the output counts — one string can break exponentially many ways.",
      "code": [
        "def wordBreak(s, wordDict):",
        "    words = set(wordDict)",
        "    memo = {}",
        "",
        "    def build(i):",
        "        if i == len(s):",
        "            return [\"\"]",
        "        if i in memo:",
        "            return memo[i]",
        "        out = []",
        "        for j in range(i + 1, len(s) + 1):",
        "            if s[i:j] in words:",
        "                for rest in build(j):",
        "                    out.append(s[i:j] + (\" \" + rest if rest else \"\"))",
        "        memo[i] = out",
        "        return out",
        "",
        "    return build(0)"
      ],
      "time": "O(n * 2^n)",
      "space": "O(n * 2^n)",
      "why": "\"aaaa...\" against {\"a\", \"aa\"} has about 2^n valid sentences and each is n characters long. The memo removes the repeated *search*; it cannot remove the writing down.",
      "trap": "Memoisation made LC 139 polynomial, so the reflex is that it does the same here. It cannot: the answer itself is exponential, and nothing can print 2^n sentences in polynomial time. A memo bounds the work per subproblem, never the number of results."
    },
    {
      "id": "partition-k-subsets",
      "lc": 698,
      "topic": "Backtracking",
      "vars": "n = len(nums), k = subsets wanted",
      "code": [
        "def canPartitionKSubsets(nums, k):",
        "    total = sum(nums)",
        "    if total % k:",
        "        return False",
        "    target = total // k",
        "    nums.sort(reverse=True)",
        "    if nums[0] > target:",
        "        return False",
        "    buckets = [0] * k",
        "",
        "    def place(i):",
        "        if i == len(nums):",
        "            return True",
        "        seen = set()",
        "        for b in range(k):",
        "            if buckets[b] + nums[i] > target or buckets[b] in seen:",
        "                continue",
        "            seen.add(buckets[b])",
        "            buckets[b] += nums[i]",
        "            if place(i + 1):",
        "                return True",
        "            buckets[b] -= nums[i]",
        "        return False",
        "",
        "    return place(0)"
      ],
      "time": "O(k^n)",
      "space": "O(n + k)",
      "why": "Each of the n numbers is tried in each of the k buckets, so the unpruned search tree has k^n leaves. The stack is n deep and the buckets are k wide.",
      "trap": "The bitmask DP over subsets is O(k * 2^n) — still exponential, but 2^n beats k^n the moment k > 2. Sorting descending and skipping buckets with an identical running sum change the running time enormously and the bound not at all; that gap between \"fast\" and \"polynomial\" is the point."
    },
    {
      "id": "alien-dictionary",
      "lc": 269,
      "topic": "Advanced Graphs",
      "vars": "c = total characters across all the words",
      "code": [
        "def alienOrder(words):",
        "    graph = {ch: set() for w in words for ch in w}",
        "    indeg = {ch: 0 for ch in graph}",
        "    for a, b in zip(words, words[1:]):",
        "        for x, y in zip(a, b):",
        "            if x != y:",
        "                if y not in graph[x]:",
        "                    graph[x].add(y)",
        "                    indeg[y] += 1",
        "                break",
        "        else:",
        "            if len(a) > len(b):",
        "                return \"\"",
        "",
        "    q = deque(ch for ch in indeg if indeg[ch] == 0)",
        "    out = []",
        "    while q:",
        "        ch = q.popleft()",
        "        out.append(ch)",
        "        for nxt in graph[ch]:",
        "            indeg[nxt] -= 1",
        "            if indeg[nxt] == 0:",
        "                q.append(nxt)",
        "    return \"\".join(out) if len(out) == len(indeg) else \"\""
      ],
      "time": "O(c)",
      "space": "O(1)",
      "why": "Building the graph reads every character once. The vertices are *letters*, so there are at most 26 and at most 26 * 25 edges — the topological sort is therefore a constant, and the scan is the whole cost.",
      "trap": "O(V + E) is the right answer to the wrong question: V and E are capped by the alphabet, so quoting them says nothing. The input's size is its total characters, and that is what the bound should name — which is also why the space is O(1)."
    },
    {
      "id": "reconstruct-itinerary",
      "lc": 332,
      "topic": "Advanced Graphs",
      "vars": "e = len(tickets)",
      "code": [
        "def findItinerary(tickets):",
        "    graph = defaultdict(list)",
        "    for src, dst in sorted(tickets, reverse=True):",
        "        graph[src].append(dst)",
        "",
        "    route, stack = [], [\"JFK\"]",
        "    while stack:",
        "        while graph[stack[-1]]:",
        "            stack.append(graph[stack[-1]].pop())",
        "        route.append(stack.pop())",
        "    return route[::-1]"
      ],
      "time": "O(e log e)",
      "space": "O(e)",
      "why": "The sort dominates. Hierholzer's walk then uses each ticket exactly once — every edge is pushed once and popped once — so the traversal itself is O(e).",
      "trap": "Backtracking over orderings until one uses every ticket is factorial in the worst case. The Eulerian-path argument removes the search entirely: a dead end can only ever be the *end* of the route, so appending on the way out and reversing at the finish is correct with no retries."
    },
    {
      "id": "swim-in-rising-water",
      "lc": 778,
      "topic": "Advanced Graphs",
      "vars": "n = the grid side",
      "code": [
        "def swimInWater(grid):",
        "    n = len(grid)",
        "    heap = [(grid[0][0], 0, 0)]",
        "    seen = {(0, 0)}",
        "    while heap:",
        "        t, i, j = heapq.heappop(heap)",
        "        if i == j == n - 1:",
        "            return t",
        "        for x, y in ((i+1, j), (i-1, j), (i, j+1), (i, j-1)):",
        "            if 0 <= x < n and 0 <= y < n and (x, y) not in seen:",
        "                seen.add((x, y))",
        "                heapq.heappush(heap, (max(t, grid[x][y]), x, y))",
        "    return -1"
      ],
      "time": "O(n^2 log n)",
      "space": "O(n^2)",
      "why": "Dijkstra with `max` in place of `+` as the path cost. There are n^2 cells, each pushed once, and each heap operation costs log(n^2).",
      "trap": "log(n^2) is 2 log n, not (log n)^2 — the constant folds away, which is why the answer is n^2 log n and not n^2 log^2 n. Written in the number of cells V = n^2 this is the familiar O(V log V); the n^2 spelling is the same bound stated in the grid's side."
    },
    {
      "id": "min-cost-connect-points",
      "lc": 1584,
      "topic": "Advanced Graphs",
      "vars": "n = len(points)",
      "code": [
        "def minCostConnectPoints(points):",
        "    n = len(points)",
        "    best = [float(\"inf\")] * n",
        "    best[0] = 0",
        "    used = [False] * n",
        "    total = 0",
        "    for _ in range(n):",
        "        u = min((i for i in range(n) if not used[i]), key=lambda i: best[i])",
        "        used[u] = True",
        "        total += best[u]",
        "        for v in range(n):",
        "            if not used[v]:",
        "                d = (abs(points[u][0] - points[v][0])",
        "                     + abs(points[u][1] - points[v][1]))",
        "                best[v] = min(best[v], d)",
        "    return total"
      ],
      "time": "O(n^2)",
      "space": "O(n)",
      "why": "The graph is complete — every pair of points is an edge — so E is n^2 and there is no adjacency list worth building. Prim's with a plain array picks the next vertex in O(n) and relaxes in O(n), n times over.",
      "trap": "O(E log V) is the heap version's bound and here it is *worse*: with E = n^2 that reads O(n^2 log n). On a dense graph the array-scan Prim wins, and this is the standard case where the fancier data structure is the wrong call."
    },
    {
      "id": "graph-valid-tree",
      "lc": 261,
      "topic": "Graphs",
      "vars": "n = nodes, e = len(edges)",
      "note": "Treat one find/union as effectively constant.",
      "code": [
        "def validTree(n, edges):",
        "    if len(edges) != n - 1:",
        "        return False",
        "    parent = list(range(n))",
        "",
        "    def find(x):",
        "        while parent[x] != x:",
        "            parent[x] = parent[parent[x]]",
        "            x = parent[x]",
        "        return x",
        "",
        "    for a, b in edges:",
        "        ra, rb = find(a), find(b)",
        "        if ra == rb:",
        "            return False",
        "        parent[ra] = rb",
        "    return True"
      ],
      "time": "O(n + e)",
      "space": "O(n)",
      "why": "Setting up `parent` is O(n) and each edge costs two finds. Only the parent array is stored — no adjacency list is ever built.",
      "trap": "The `len(edges) != n - 1` line is doing real work, not guarding an edge case. A tree is \"connected and acyclic\", and once the edge count is pinned at n - 1 those two become the *same* condition — so one cycle check settles it, instead of a traversal plus a component count."
    },
    {
      "id": "minimum-height-trees",
      "lc": 310,
      "topic": "Graphs",
      "vars": "n = nodes",
      "code": [
        "def findMinHeightTrees(n, edges):",
        "    if n == 1:",
        "        return [0]",
        "    graph = defaultdict(set)",
        "    for a, b in edges:",
        "        graph[a].add(b)",
        "        graph[b].add(a)",
        "",
        "    leaves = [x for x in graph if len(graph[x]) == 1]",
        "    remaining = n",
        "    while remaining > 2:",
        "        remaining -= len(leaves)",
        "        nxt = []",
        "        for leaf in leaves:",
        "            nb = graph[leaf].pop()",
        "            graph[nb].discard(leaf)",
        "            if len(graph[nb]) == 1:",
        "                nxt.append(nb)",
        "        leaves = nxt",
        "    return leaves"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "A tree has n - 1 edges, so O(V + E) collapses to O(n). Every node is peeled off exactly once, the moment it becomes a leaf.",
      "trap": "Measuring the height from every possible root is O(n^2) — n traversals of the whole tree. Peeling leaves inward walks to the centre directly, and a tree has one or two centres, which is why the answer never has three entries."
    },
    {
      "id": "evaluate-division",
      "lc": 399,
      "topic": "Graphs",
      "vars": "V = variables, E = len(equations), q = len(queries)",
      "code": [
        "def calcEquation(equations, values, queries):",
        "    graph = defaultdict(dict)",
        "    for (a, b), v in zip(equations, values):",
        "        graph[a][b] = v",
        "        graph[b][a] = 1 / v",
        "",
        "    def walk(src, dst, seen):",
        "        if src not in graph or dst not in graph:",
        "            return -1.0",
        "        if src == dst:",
        "            return 1.0",
        "        seen.add(src)",
        "        for nb, w in graph[src].items():",
        "            if nb not in seen:",
        "                sub = walk(nb, dst, seen)",
        "                if sub != -1.0:",
        "                    return w * sub",
        "        return -1.0",
        "",
        "    return [walk(a, b, set()) for a, b in queries]"
      ],
      "time": "O(q * (V + E))",
      "space": "O(V + E)",
      "why": "Each query runs its own DFS with a fresh `seen` set, and one DFS over the whole graph is O(V + E).",
      "trap": "The bound has to name the query count — this is a search *per* query, with nothing carried between them. Weighted union-find, or one O(V^3) Floyd-Warshall pass up front, answers each query in near O(1) afterwards, and becomes the better trade as soon as q is large."
    },
    {
      "id": "accounts-merge",
      "lc": 721,
      "topic": "Graphs",
      "vars": "s = total emails across all accounts",
      "code": [
        "def accountsMerge(accounts):",
        "    parent = {}",
        "",
        "    def find(x):",
        "        parent.setdefault(x, x)",
        "        while parent[x] != x:",
        "            parent[x] = parent[parent[x]]",
        "            x = parent[x]",
        "        return x",
        "",
        "    owner = {}",
        "    for name, *emails in accounts:",
        "        for e in emails:",
        "            parent[find(e)] = find(emails[0])",
        "            owner[e] = name",
        "",
        "    groups = defaultdict(list)",
        "    for e in list(parent):",
        "        groups[find(e)].append(e)",
        "    return [[owner[root]] + sorted(mails) for root, mails in groups.items()]"
      ],
      "time": "O(s log s)",
      "space": "O(s)",
      "why": "The union-find passes are near-linear in the emails. The `sorted` call on every group is what dominates, and since the groups' sizes sum to s, all of them together cost O(s log s).",
      "trap": "O(s) is the answer if you stop reading before the last line. Union-find is the part everyone measures and the cheap part here; the required sorted output is the expensive one."
    },
    {
      "id": "search-rotated-duplicates",
      "lc": 81,
      "topic": "Binary Search",
      "vars": "n = len(nums)",
      "note": "Answer for the worst case.",
      "code": [
        "def search(nums, target):",
        "    lo, hi = 0, len(nums) - 1",
        "    while lo <= hi:",
        "        mid = (lo + hi) // 2",
        "        if nums[mid] == target:",
        "            return True",
        "        if nums[lo] == nums[mid] == nums[hi]:",
        "            lo += 1",
        "            hi -= 1",
        "        elif nums[lo] <= nums[mid]:",
        "            if nums[lo] <= target < nums[mid]:",
        "                hi = mid - 1",
        "            else:",
        "                lo = mid + 1",
        "        else:",
        "            if nums[mid] < target <= nums[hi]:",
        "                lo = mid + 1",
        "            else:",
        "                hi = mid - 1",
        "    return False"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "When `nums[lo] == nums[mid] == nums[hi]` there is no way to tell which side is sorted, so the only safe move is to shrink by one from each end. An array of all-equal values hits that branch every single step.",
      "trap": "LC 33 without duplicates is O(log n) and the code is nearly identical — the three-way-equality case is the entire difference, and it destroys the halving guarantee. A binary search is only logarithmic if every step can throw away a constant *fraction*; discarding two elements is not a fraction."
    },
    {
      "id": "sqrt-binary-search",
      "lc": 69,
      "topic": "Binary Search",
      "vars": "x = the input value, b = its length in bits",
      "code": [
        "def mySqrt(x):",
        "    lo, hi = 0, x",
        "    while lo <= hi:",
        "        mid = (lo + hi) // 2",
        "        if mid * mid <= x:",
        "            lo = mid + 1",
        "        else:",
        "            hi = mid - 1",
        "    return hi"
      ],
      "time": "O(log x)",
      "space": "O(1)",
      "accept": { "time": ["O(b)"] },
      "why": "The search range is 0..x and halves every step, so it finishes in about log x iterations.",
      "trap": "log x reads as sublinear, and in the *value* it is. In the size of the input it is linear: x takes b = log x bits to write down, so this is one step per bit. Same accounting that makes LC 416's knapsack pseudo-polynomial rather than polynomial."
    },
    {
      "id": "minimum-interval-query",
      "lc": 1851,
      "topic": "Intervals",
      "vars": "n = len(intervals), q = len(queries)",
      "code": [
        "def minInterval(intervals, queries):",
        "    intervals.sort()",
        "    heap = []",
        "    out = {}",
        "    i = 0",
        "    for query in sorted(queries):",
        "        while i < len(intervals) and intervals[i][0] <= query:",
        "            lo, hi = intervals[i]",
        "            heapq.heappush(heap, (hi - lo + 1, hi))",
        "            i += 1",
        "        while heap and heap[0][1] < query:",
        "            heapq.heappop(heap)",
        "        out[query] = heap[0][0] if heap else -1",
        "    return [out[query] for query in queries]"
      ],
      "time": "O(n log n + q log q)",
      "space": "O(n + q)",
      "why": "Both inputs are sorted once. After that each interval is pushed and popped at most once across the entire sweep, so the heap work is O(n log n) in total rather than per query.",
      "trap": "Scanning the intervals per query is O(n * q). Sorting the *queries* is the move people miss — answering them out of order and restoring the original order at the end is what makes one sweep enough. That is offline processing, and it is only legal because the queries are all known up front."
    },
    {
      "id": "car-pooling",
      "lc": 1094,
      "topic": "Intervals",
      "vars": "n = len(trips)",
      "code": [
        "def carPooling(trips, capacity):",
        "    delta = defaultdict(int)",
        "    for num, start, end in trips:",
        "        delta[start] += num",
        "        delta[end] -= num",
        "    riders = 0",
        "    for stop in sorted(delta):",
        "        riders += delta[stop]",
        "        if riders > capacity:",
        "            return False",
        "    return True"
      ],
      "time": "O(n log n)",
      "space": "O(n)",
      "why": "A difference array: record the change at each endpoint, then sweep the endpoints in order. Only the 2n endpoints matter, so the sort is the whole cost.",
      "trap": "Simulating every kilometre is O(n * d) in the *length of the road*, which can dwarf the trip list. Going the other way, LC 1094 caps the stops at 1000, so a fixed 1001-slot array makes this O(n) time and O(1) space — the sort is only needed when the coordinates are unbounded."
    },
    {
      "id": "max-path-sum",
      "lc": 124,
      "topic": "Trees",
      "vars": "n = nodes, h = height",
      "code": [
        "def maxPathSum(root):",
        "    best = float(\"-inf\")",
        "",
        "    def gain(node):",
        "        nonlocal best",
        "        if not node:",
        "            return 0",
        "        left = max(gain(node.left), 0)",
        "        right = max(gain(node.right), 0)",
        "        best = max(best, node.val + left + right)",
        "        return node.val + max(left, right)",
        "",
        "    gain(root)",
        "    return best"
      ],
      "time": "O(n)",
      "space": "O(h)",
      "accept": { "space": ["O(n)"] },
      "why": "One post-order pass. Each node does two constant-time things given its children's answers: record the best path bending *through* it (left + right), and hand upward the best path it can extend (max of the two).",
      "trap": "The value returned is not the answer — `best` is. Computing the through-path at every node with a separate downward walk would be O(n^2), the same trap as LC 543. The `max(..., 0)` is free: it clamps a losing subtree to \"don't take it\"."
    },
    {
      "id": "lca-bst",
      "lc": 235,
      "topic": "Trees",
      "vars": "n = nodes, h = height",
      "code": [
        "def lowestCommonAncestor(root, p, q):",
        "    node = root",
        "    while node:",
        "        if p.val < node.val and q.val < node.val:",
        "            node = node.left",
        "        elif p.val > node.val and q.val > node.val:",
        "            node = node.right",
        "        else:",
        "            return node"
      ],
      "time": "O(h)",
      "space": "O(1)",
      "accept": { "time": ["O(n)"] },
      "why": "The ordering says which side both targets are on, so the walk descends a single path and never branches. Iterative, so not even a stack.",
      "trap": "LC 236 on a plain binary tree is O(n) time and O(h) space, and the code here is shorter than that one. The only difference is that the values tell you where to go — a good measure of what a sorted invariant is actually worth. The split point *is* the answer, which is why there is no bookkeeping on the way back up."
    },
    {
      "id": "serialize-deserialize-tree",
      "lc": 297,
      "topic": "Trees",
      "vars": "n = nodes",
      "note": "Time is for one serialize or one deserialize call.",
      "code": [
        "class Codec:",
        "    def serialize(self, root):",
        "        out = []",
        "",
        "        def walk(node):",
        "            if not node:",
        "                out.append(\"#\")",
        "                return",
        "            out.append(str(node.val))",
        "            walk(node.left)",
        "            walk(node.right)",
        "",
        "        walk(root)",
        "        return \",\".join(out)",
        "",
        "    def deserialize(self, data):",
        "        it = iter(data.split(\",\"))",
        "",
        "        def build():",
        "            tok = next(it)",
        "            if tok == \"#\":",
        "                return None",
        "            node = TreeNode(int(tok))",
        "            node.left = build()",
        "            node.right = build()",
        "            return node",
        "",
        "        return build()"
      ],
      "time": "O(n)",
      "space": "O(n)",
      "why": "Pre-order with an explicit null marker. Each node emits one token and each token consumes one `next`, so both directions are linear; the token list itself is O(n).",
      "trap": "`tokens.pop(0)` per node instead of an iterator is the version that quietly becomes O(n^2) — popping the front of a list shifts every remaining element. The `#` markers matter too: they are what make a *single* traversal sufficient, where without them you would need both a pre-order and an in-order to reconstruct the shape."
    },
    {
      "id": "subtree-of-another-tree",
      "lc": 572,
      "topic": "Trees",
      "vars": "m = nodes in root, n = nodes in subRoot",
      "code": [
        "def isSubtree(root, subRoot):",
        "    def same(a, b):",
        "        if not a or not b:",
        "            return a is b",
        "        return (a.val == b.val and same(a.left, b.left)",
        "                and same(a.right, b.right))",
        "",
        "    if not root:",
        "        return False",
        "    if same(root, subRoot):",
        "        return True",
        "    return isSubtree(root.left, subRoot) or isSubtree(root.right, subRoot)"
      ],
      "time": "O(m * n)",
      "space": "O(m + n)",
      "accept": { "space": ["O(m)"] },
      "why": "`same` is launched from every node of `root` and can cost up to n each time. A product, not a sum — that is the shape worth spotting. The two recursions nest, so the stacks add.",
      "trap": "O(m + n) is the instinctive answer, and it *is* achievable — serialise both trees with null markers and run a substring search — but not by this code. Comparing structure by walking it is inherently a product; getting to linear means turning the trees into strings first."
    },
    {
      "id": "house-robber-tree",
      "lc": 337,
      "topic": "Trees",
      "vars": "n = nodes, h = height",
      "code": [
        "def rob(root):",
        "    def walk(node):",
        "        if not node:",
        "            return (0, 0)                 # (rob this node, skip it)",
        "        l_rob, l_skip = walk(node.left)",
        "        r_rob, r_skip = walk(node.right)",
        "        return (node.val + l_skip + r_skip,",
        "                max(l_rob, l_skip) + max(r_rob, r_skip))",
        "",
        "    return max(walk(root))"
      ],
      "time": "O(n)",
      "space": "O(h)",
      "accept": { "space": ["O(n)"] },
      "why": "One post-order pass returning a pair per node — the best with this node robbed and the best without. Each node is visited once and does constant work on its children's pairs.",
      "trap": "The version without the pair — rob this node and recurse to the *grandchildren*, or skip it and recurse to the children — visits each node once per ancestor and is exponential. Returning both answers together is what removes the overlap; a memo keyed on the node also works and is the same O(n)."
    },
    {
      "id": "sorted-array-to-bst",
      "lc": 108,
      "topic": "Trees",
      "vars": "n = len(nums)",
      "note": "Ignore the tree that is returned.",
      "code": [
        "def sortedArrayToBST(nums):",
        "    def build(lo, hi):",
        "        if lo > hi:",
        "            return None",
        "        mid = (lo + hi) // 2",
        "        node = TreeNode(nums[mid])",
        "        node.left = build(lo, mid - 1)",
        "        node.right = build(mid + 1, hi)",
        "        return node",
        "",
        "    return build(0, len(nums) - 1)"
      ],
      "time": "O(n)",
      "space": "O(log n)",
      "why": "Every element becomes exactly one node, so the work is linear. Taking the middle each time makes the tree balanced by construction, so the recursion is only log n frames deep.",
      "trap": "O(n log n) is the answer for the version that passes `nums[:mid]` and `nums[mid + 1:]` — those slices copy, and copying at every level costs a full pass per level. Index bounds cost nothing. The O(log n) stack is also the exception among tree builders: it is log because the input is sorted, not because trees are usually balanced."
    },
    {
      "id": "flatten-tree-morris",
      "lc": 114,
      "topic": "Trees",
      "vars": "n = nodes",
      "code": [
        "def flatten(root):",
        "    node = root",
        "    while node:",
        "        if node.left:",
        "            rightmost = node.left",
        "            while rightmost.right:",
        "                rightmost = rightmost.right",
        "            rightmost.right = node.right",
        "            node.right = node.left",
        "            node.left = None",
        "        node = node.right"
      ],
      "time": "O(n)",
      "space": "O(1)",
      "why": "The outer walk visits each node once. The inner search re-walks right edges, but each edge is followed by such a search at most once before being rewired away — so the total is linear, not quadratic.",
      "trap": "A `while` inside a `while` over a tree reads as O(n^2), and the amortised argument is the entire point of the Morris technique. It is also the rare tree algorithm with no stack and no recursion at all, which is precisely what the O(1)-space follow-up is asking for."
    },
    {
      "id": "path-sum-iii",
      "lc": 437,
      "topic": "Trees",
      "vars": "n = nodes, h = height",
      "code": [
        "def pathSum(root, targetSum):",
        "    seen = defaultdict(int)",
        "    seen[0] = 1",
        "",
        "    def walk(node, running):",
        "        if not node:",
        "            return 0",
        "        running += node.val",
        "        count = seen[running - targetSum]",
        "        seen[running] += 1",
        "        count += walk(node.left, running) + walk(node.right, running)",
        "        seen[running] -= 1",
        "        return count",
        "",
        "    return walk(root, 0)"
      ],
      "time": "O(n)",
      "space": "O(h)",
      "accept": { "space": ["O(n)"] },
      "why": "LC 560's prefix-sum-in-a-hash-map, run down each root-to-node path. Every node is visited once, and the map only ever holds the sums along the current path.",
      "trap": "The obvious solution restarts a downward count from every node: O(n^2), or O(n log n) on a balanced tree. And `seen[running] -= 1` on the way out is not housekeeping — without it, prefix sums from a sibling branch leak in and inflate the count."
    },
    {
      "id": "same-tree",
      "lc": 100,
      "topic": "Trees",
      "vars": "n = nodes in the smaller tree, h = its height",
      "code": [
        "def isSameTree(p, q):",
        "    if not p or not q:",
        "        return p is q",
        "    if p.val != q.val:",
        "        return False",
        "    return (isSameTree(p.left, q.left)",
        "            and isSameTree(p.right, q.right))"
      ],
      "time": "O(n)",
      "space": "O(h)",
      "accept": { "space": ["O(n)"] },
      "why": "The walk stops at the first difference, so it never visits more nodes than the smaller tree has. `and` short-circuits, which is what makes an early mismatch cheap.",
      "trap": "The short-circuit does not improve the *bound* — two identical trees make it visit everything — but it does decide which n the bound is in: the smaller tree, since the recursion cannot outlive the shallower structure. This function is also the inner loop of LC 572, which is where its cost stops being linear."
    },
    {
      "id": "extra-characters-trie",
      "lc": 2707,
      "topic": "Tries",
      "vars": "n = len(s), k = total characters in the dictionary",
      "code": [
        "def minExtraChar(s, dictionary):",
        "    trie = {}",
        "    for w in dictionary:",
        "        node = trie",
        "        for ch in w:",
        "            node = node.setdefault(ch, {})",
        "        node[\"#\"] = True",
        "",
        "    n = len(s)",
        "    dp = [0] * (n + 1)",
        "    for i in range(n - 1, -1, -1):",
        "        dp[i] = dp[i + 1] + 1",
        "        node = trie",
        "        for j in range(i, n):",
        "            node = node.get(s[j])",
        "            if not node:",
        "                break",
        "            if \"#\" in node:",
        "                dp[i] = min(dp[i], dp[j + 1])",
        "    return dp[0]"
      ],
      "time": "O(n^2 + k)",
      "space": "O(k)",
      "why": "Building the trie costs its total characters. Then each of the n start positions walks forward at most n steps, one dict hop per step — the trie is what makes each step O(1) instead of a fresh substring hash.",
      "trap": "The set-of-words version writes `s[i:j] in dictionary`, which slices and hashes up to n characters *per pair* — the same DP, but O(n^3). This is exactly the hidden factor LC 139's trap points at, and the trie is the standard way to pay it off."
    }
  ]
}
